// JavaScript Core topics — part 1 (fundamentals, scope, functions, objects)
// Each entry: t = topic phrase, d = canonical answer text.
export default [
  { t: 'the difference between var, let and const', d: 'var is function-scoped, hoisted with an initial value of undefined, and attaches to the global object at top level. let and const are block-scoped, hoisted but uninitialized (temporal dead zone), and do not attach to window. const forbids reassignment but allows mutation of object contents. Modern code defaults to const and uses let only when reassignment is required.' },
  { t: 'hoisting', d: 'Hoisting is the engine moving declarations to the top of their scope during the creation phase of an execution context. Function declarations are fully initialized, var gets undefined, let/const stay in the temporal dead zone until evaluation. Class declarations are also in a TDZ. Understanding hoisting explains why calling code before its declaration sometimes works and sometimes throws.' },
  { t: 'the temporal dead zone', d: 'The temporal dead zone is the span between entering a scope and the line where a let/const/class declaration is evaluated. Accessing the binding in that window throws a ReferenceError instead of returning undefined. It exists to catch programming errors early and makes const initialization order predictable.' },
  { t: 'block scope vs function scope', d: 'Function scope means variables declared in a function are visible anywhere inside it (var). Block scope confines let/const to the nearest curly-brace pair — an if, for, or bare block. Block scope prevents accidental leakage, enables safe loop variables per iteration with let, and makes refactoring safer.' },
  { t: 'closures', d: 'A closure is a function that keeps a reference to the lexical environment where it was created, so it can access outer variables even after the outer function has returned. Closures power data privacy, callbacks, currying, memoization caches, and module patterns. They keep captured variables alive, which can also cause memory leaks if references outlive their usefulness.' },
  { t: 'the classic closure-in-a-loop problem', d: 'Using var in a loop shares one binding, so callbacks scheduled asynchronously all see the final value. Using let creates a fresh binding per iteration, so each callback captures its own copy. Alternatively wrap the body in an IIFE that takes the index as a parameter to force a new scope.' },
  { t: 'IIFE', d: 'An immediately invoked function expression is a function executed as soon as it is defined: (function () { ... })(). Before ES modules it was the standard way to create private scope and avoid polluting globals. Today it still appears in one-off async wrappers like (async () => { ... })() and in configuration blocks.' },
  { t: 'the this keyword', d: 'this refers to the context in which a function executes, determined by how it is called rather than where it is defined. Rules in priority order: new binding, explicit binding via call/apply/bind, implicit binding from the containing object at call site, and default binding (undefined in strict mode, global otherwise). Arrow functions ignore all these rules and lexically inherit this.' },
  { t: 'call, apply and bind', d: 'call invokes a function with a given this and arguments listed individually; apply does the same but takes arguments as an array; bind returns a new permanently-bound function without invoking it. Use call/apply for borrowing methods or variadic forwarding, bind for callbacks and event handlers that need stable context.' },
  { t: 'arrow functions', d: 'Arrow functions have lexical this (inherited from the enclosing scope), no own arguments object, cannot be used as constructors, and lack a prototype property. They shine as short callbacks where you want the surrounding this. They are wrong choices for object methods needing dynamic this, event handlers relying on this === currentTarget, or prototype methods.' },
  { t: 'implicit vs explicit binding', d: 'Implicit binding sets this to the object left of the dot at call time (obj.fn()). Explicit binding overrides it using call, apply, or bind. A subtle gotcha: passing obj.fn as a bare callback loses the implicit receiver and falls back to default binding unless you bind it first.' },
  { t: 'the new operator', d: 'new creates a fresh object whose prototype is set to ConstructorFn.prototype, invokes the constructor with this bound to the new object, and either returns that object or an explicit object returned by the constructor. Returning a primitive is ignored; returning a new object replaces the result. This explains factory-versus-constructor behavior differences.' },
  { t: 'prototypal inheritance', d: 'Objects inherit directly from other objects via the [[Prototype]] link, accessible through __proto__ or Object.create. Property lookups walk up this chain until found or null. Unlike class-based inheritance, delegation is live: changing a prototype affects existing instances immediately.' },
  { t: 'prototype chain', d: 'When reading a property, the engine checks the object itself, then its prototype, then that prototype\u2019s prototype, until reaching null. Assignments create own properties instead of walking the chain. Object.getPrototypeOf reads the link safely, and hasOwnProperty (or Object.hasOwn) distinguishes own properties from inherited ones.' },
  { t: 'ES6 classes', d: 'Classes are syntactic sugar over prototypes with stricter semantics: bodies run in strict mode, declarations are not hoisted usable before definition, methods are non-enumerable, and calling without new throws. They support extends, super, getters/setters, static methods, static blocks, and private #fields. Under the hood instances still delegate through [[Prototype]].' },
  { t: 'class fields and private fields', d: 'Public class fields initialize on each instance before the constructor body runs; private fields prefixed with # are enforced by the engine, truly inaccessible outside the class even via reflection. Static blocks allow complex one-time class-level initialization. Private fields throw TypeError on access rather than returning undefined, catching typos early.' },
  { t: 'static methods and properties', d: 'Static members belong to the class itself, not instances, and are inherited by subclasses through the class\u2019s internal prototype chain. They commonly host factories, utilities, and cached config. Calling them on an instance throws because instances do not link to the constructor function.' },
  { t: 'Object.create', d: 'Object.create(proto) builds a new object whose [[Prototype]] is proto without invoking any constructor \u2014 the purest form of prototypal inheritance. Passing null produces a dictionary object with no inherited methods, useful for safe maps. The optional second argument defines properties via descriptors just like Object.defineProperties.' },
  { t: 'property descriptors', d: 'Every property has a descriptor containing value, writable, enumerable, and configurable (plus get/set for accessors). Object.defineProperty creates or mutates them; Object.getOwnPropertyDescriptor inspects them. Non-enumerable properties hide from for-in and Object.keys; non-configurable properties can never be deleted or reconfigured.' },
  { t: 'getters and setters', d: 'Accessor properties intercept reads and writes through get/set functions, enabling computed values, validation, and logging while keeping call-site syntax natural. Defined in object literals, classes, or defineProperty. Overusing them hides side effects behind innocent-looking assignment, so keep them cheap and side-effect free.' },
  { t: 'Object.freeze', d: 'Object.freeze makes an object shallowly immutable: existing properties become non-writable and non-configurable, and no new ones can be added. Nested objects remain mutable, so deep freezing requires recursion. In non-strict mode writes fail silently; in strict mode they throw. Frozen objects signal intent and help with immutability patterns.' },
  { t: 'Object.seal', d: 'Object.seal prevents adding or removing properties but leaves existing values writable. Freeze goes further by locking values too. Both are shallow operations. Sealing suits records with a fixed shape whose contents change; freezing suits constants and shared configuration.' },
  { t: 'shallow vs deep copy', d: 'Shallow copies (spread, Object.assign, Array slice) duplicate only the top level; nested references are shared, so mutating them leaks into the original. Deep copies duplicate recursively \u2014 structuredClone handles Dates, Maps, Sets, and cycles natively, while JSON round-tripping drops functions, undefined, and Symbols and mangles Dates into strings.' },
  { t: 'structuredClone', d: 'structuredClone performs native deep cloning supporting cyclical structures, Map, Set, Date, RegExp, ArrayBuffer and typed arrays, while throwing on functions, DOM nodes, and property descriptors. It avoids JSON.stringify pitfalls and the slow JSON round-trip hack, making it the default choice for deep-copying data objects.' },
  { t: 'spread syntax', d: 'Spread (...) expands iterables into argument lists or array literals and enumerates own enumerable props into object literals. It builds concise clones and merges ({ ...a, ...b } later keys win) and converts iterables like NodeList or Set to arrays. Spread is always a shallow operation \u2014 nested objects stay shared.' },
  { t: 'rest parameters', d: 'Rest syntax (...) collects remaining arguments into a real array in function signatures, or remaining keys in destructuring. Unlike the legacy arguments object, rest is a true array with array methods, works in arrow functions, and must be the last parameter. Rest and spread share syntax but serve opposite directions.' },
  { t: 'destructuring assignment', d: 'Destructuring unpacks arrays and objects into variables with pattern matching: const { name, age = 18, ...rest } = user. Defaults apply only to undefined, nested paths work, and renamed targets use colon syntax. Common uses include function parameter options, swapping, skipping elements, and extracting from API responses safely.' },
  { t: 'template literals', d: 'Template literals use backticks, support multi-line strings, and interpolate expressions with ${}. Tagged templates pass pieces and substitutions to a function, powering libraries like styled-components and gql. They preserve whitespace exactly and escape backticks with a backslash.' },
  { t: 'tagged templates', d: 'A tagged template calls a function with (stringsArray, ...values): tag`Hello ${name}` receives [\'Hello \', \'\'] and name. Tags control final output, enabling escaping, localization, CSS-in-JS, and GraphQL parsing. Returning anything (including objects) becomes the expression result.' },
  { t: 'default parameters', d: 'Default parameter values kick in when an argument is undefined (not null or 0), and may reference earlier parameters, enabling derived defaults. Each call evaluates the expression freshly. Defaults replace many manual x = x || fallback lines while avoiding falsy-value bugs that || introduces.' },
  { t: 'higher-order functions', d: 'A higher-order function takes functions as arguments or returns them. map, filter, reduce, debounce, once, and middleware chains are everyday examples. HOFs enable abstraction over behavior, composition, and separation of data from logic \u2014 the backbone of functional style in JavaScript.' },
  { t: 'callbacks', d: 'A callback is a function handed to another function for later invocation. Synchronous callbacks run inline (map); asynchronous ones run later via the event loop (setTimeout, fetch handlers). Deeply nested async callbacks produce pyramid-of-doom code, which Promises and async/await were designed to flatten.' },
  { t: 'Promises', d: 'A Promise represents a future value with three states: pending, fulfilled, or rejected; once settled it never changes. then registers reactions, catch handles rejection, finally runs regardless. Handlers execute as microtasks after the current synchronous work, giving deterministic ordering relative to setTimeout.' },
  { t: 'Promise chaining', d: 'Each then returns a new promise, enabling flat pipelines where the returned value or thrown error flows onward. Returning a promise inside then waits for it. Errors skip ahead to the nearest catch and normal flow resumes after it \u2014 resembling try/catch semantics across steps.' },
  { t: 'async/await', d: 'async functions implicitly return promises and pause at await without blocking the thread, resuming when the awaited promise settles. await unwraps thenables; awaiting non-thenables resolves immediately. Errors surface via try/catch. Sequential awaits inside loops often serialize requests unintentionally \u2014 Promise.all parallelizes independent work.' },
  { t: 'the event loop', d: 'The event loop repeatedly takes the oldest runnable task from task queues, runs it to completion, then drains the entire microtask queue (promise reactions, queueMicrotask, MutationObserver) before rendering and picking the next task. This single-threaded cooperative model explains why tight loops freeze UIs and why microtasks starve timers if chained forever.' },
  { t: 'microtasks vs macrotasks', d: 'Microtasks (promise callbacks, queueMicrotask, MutationObserver) drain completely after each macrotask and between script segments. Macrotasks (setTimeout, setInterval, I/O, UI events) run one per loop turn. Therefore Promise.then fires before setTimeout(0) even when registered later, a classic interview output-ordering question.' },
  { t: 'setTimeout and setInterval', d: 'setTimeout schedules a macrotask after at least the given delay; nesting deeper than five levels clamps delays to 4ms. setInterval re-queues every interval but overlapping executions pile up if handlers exceed the interval \u2014 recursive setTimeout is safer. Delays are minimums, not guarantees, since busy main threads postpone execution.' },
  { t: 'debounce', d: 'Debouncing postpones invoking a function until a quiet period elapses since the last call, resetting a timer on every trigger. Ideal for search inputs, resize handlers, and autosave. Implementation stores the timer id in a closure and clearTimeouts it each invocation, optionally firing immediately on the leading edge.' },
  { t: 'throttle', d: 'Throttling guarantees a function runs at most once per time window, ignoring intermediate bursts. It differs from debounce by guaranteeing periodic execution, suiting scroll and mousemove analytics. Implementations track lastRun timestamp plus optional trailing call so the final state is not dropped.' },
  { t: 'currying', d: 'Currying transforms f(a,b,c) into f(a)(b)(c), producing specialized partially-applied versions. Benefits include reusable predicates, dependency injection, and cleaner point-free pipelines. Implement generically by collecting args recursively until enough are gathered, then applying the original function.' },
  { t: 'partial application', d: 'Partial application fixes some arguments ahead of time, returning a function accepting the remainder \u2014 unlike curry, it does not enforce one-argument-at-a-time. Function.prototype.bind is the built-in partial applier. It reduces repetition when many calls share leading arguments such as configuration or context.' },
  { t: 'function composition', d: 'Composition chains functions so one\u2019s output feeds the next: compose(f,g,h)(x) equals f(g(h(x))). Pipe is compose reversed for readability. Building transforms from small pure units simplifies testing and reasoning, mirroring how array method chains work.' },
  { t: 'memoization', d: 'Memoization caches results keyed by input so repeated calls return instantly. Pure functions are safe targets; impure or huge-keyed ones waste memory. A WeakMap keyed by object args enables garbage-collectible caches. Classic examples are memoized fibonacci turning exponential recursion into linear time.' },
  { t: 'pure functions', d: 'Pure functions return identical outputs for identical inputs and cause no observable side effects, making them trivially testable, cacheable, and parallelizable. JavaScript purity is convention: watch hidden inputs like Date.now, Math.random, globals, DOM, and mutated parameters. Impurity concentrated at boundaries keeps cores predictable.' },
  { t: 'immutability in JavaScript', d: 'Immutability treats data as unchangeable; updates create new values instead of mutating. It prevents aliasing bugs and enables change detection by reference equality, which React relies on. Tools range from spread and structuredClone to Immer\u2019s draft proxies and Object.freeze enforcement in development.' },
  { t: 'recursion', d: 'Recursion solves problems by having a function call itself on smaller inputs toward a base case. JavaScript engines mostly lack tail-call optimization in practice, so deep recursion risks stack overflow; convert hot paths to iteration or trampolines. Trees, JSON traversal, and parsers are naturally recursive domains.' },
  { t: 'generators', d: 'Generator functions (function*) yield multiple values lazily and pausably; each next() resumes until the next yield. They implement custom iterators, infinite sequences, and cooperative control flow. yield* delegates to another iterable. Generators underpinned early redux-saga effects and remain useful for streaming pipelines.' },
  { t: 'iterators and the iterator protocol', d: 'An iterator is any object with next() returning { value, done }. Iterables expose Symbol.iterator returning an iterator, unlocking for...of, spread, destructuring, and Array.from. Strings, arrays, maps, sets, NodeList are built-in iterables. Custom iterables integrate your structures seamlessly with language syntax.' },
  { t: 'Symbol', d: 'Symbols are unique primitive keys immune to collisions, invisible to for-in, Object.keys and JSON.stringify. Beyond custom keys, well-known symbols (iterator, hasInstance, toPrimitive, species) hook into engine behaviors. They provide nominal-ish typing where string keys would risk conflicts.' },
  { t: 'Map vs plain object', d: 'Map accepts any key type, preserves insertion order, exposes size, iterates directly, and performs better under frequent add/delete churn. Plain objects suit fixed schemas, JSON interchange, and structural patterns. WeakMap holds keys weakly for metadata attachment without preventing garbage collection.' },
  { t: 'WeakMap and WeakSet', d: 'WeakMap/WeakSet hold objects weakly: entries are removed automatically when the key becomes unreachable elsewhere, preventing memory leaks in caches and metadata registries. Keys must be objects, contents are not iterable, and size is unobservable \u2014 deliberate design for lifetime-safe association.' },
  { t: 'Set', d: 'Set stores unique values of any type with O(1)-ish add/has/delete, preserving insertion order. Idiomatic deduplication: [...new Set(arr)]. Because uniqueness uses SameValueZero, NaN dedupes correctly. Sets beat filter/indexOf dedupe approaches asymptotically for large arrays.' },
  { t: 'equality comparisons', d: '=== compares type plus value without coercion; == coerces first, creating famous surprises ([] == false). Object.is distinguishes +0/-0 and treats NaN equal to itself. Prefer === everywhere, Object.is for special numeric checks, and remember reference types compare by identity, not structure.' },
  { t: 'type coercion rules', d: 'Coercion converts operands when types mismatch: addition prefers string concatenation if either operand is a string; other arithmetic forces numbers; abstract equality applies ToPrimitive recursively. Knowing ToPrimitive (valueOf then toString) and ToBoolean (falsy list) defuses most == puzzles and clarifies why "5" - 3 is 2 but "5" + 3 is "53".' },
  { t: 'falsy values', d: 'Exactly six values coerce to false: false, 0, -0, 0n, "", null, undefined and NaN \u2014 everything else including [], {} and "0" is truthy. This trips developers checking presence with if(arr.length) versus if(arr). Nullish coalescing and optional chaining exist precisely to narrow checks to null/undefined only.' },
  { t: 'typeof operator pitfalls', d: 'typeof returns "object" for null (historic bug kept for compatibility), "function" for callables, and implementation-defined "unknown"/"xml" edge cases in old IE for undeclared vars. For reliable type detection combine typeof for primitives, Array.isArray for arrays, instanceof for classes, and Object.prototype.toString.call for exotic cases.' },
  { t: 'instanceof', d: 'instanceof checks whether constructor.prototype appears anywhere in the object\u2019s prototype chain, so it fails across realms (iframe) and breaks after subclassing tricks. Classes can customize it via static Symbol.hasInstance. For cross-realm arrays rely on Array.isArray, which is realm-independent.' },
  { t: 'optional chaining', d: 'Optional chaining (a?.b, a?.[k], fn?.()) short-circuits to undefined when the left side is null or undefined, avoiding TypeError walls. It composes with ?? for defaults: user?.address?.city ?? "Unknown". Note short-circuit skips subsequent property accesses and calls entirely, and assigning to the result is illegal.' },
  { t: 'nullish coalescing', d: 'Nullish coalescing (??) returns the right operand only when the left is null or undefined, unlike || which also hijacks 0, "", false, NaN. Use ?? for genuine defaults (count ?? 10) and reserve || for boolean-style fallbacks. Mixing ?? with || needs parentheses due to syntax restrictions.' },
  { t: 'logical assignment operators', d: 'ES2021 added &&=, ||= and ??=: a ||= b assigns only when a is falsy, a &&= b when truthy, a ??= b when nullish. They short-circuit, avoiding unnecessary setter invocations, and tidy idiom-heavy defaulting code. Remember ||= triggers on "" and 0, which is usually unwanted for form values.' },
  { t: 'error handling with try/catch/finally', d: 'try wraps risky code, catch receives the thrown value (any type, though Error preferred), and finally always runs \u2014 even overriding pending return values. Without catch, errors propagate up the stack. Async code requires await inside try or .catch on the promise; unhandled rejections crash Node and warn in browsers.' },
  { t: 'custom error classes', d: 'Extending Error gives typed failures with proper stack traces and instanceof checks: class ValidationError extends Error. Call super(message), set this.name, and target ES2015+ output so extending built-ins works correctly. Typed errors enable precise catch-block branching instead of string matching messages.' },
  { t: 'strict mode', d: '"use strict" opts into stricter parsing: assignments to undeclared variables throw, silent failures become errors, this is undefined in bare function calls, octal literals banned, and eval scope isolated. Modules and classes are automatically strict. It catches whole bug classes at zero runtime cost.' },
  { t: 'ES modules', d: 'ESM uses import/export with statically analyzable syntax, enabling tree shaking, circular-reference handling via live bindings, and deferred async loading. Modules are strict-mode by default and singleton per specifier. Dynamic import() returns a promise for lazy chunks, and import.meta exposes module metadata like url.' },
  { t: 'CommonJS vs ESM', d: 'CommonJS loads synchronously via require with value copies of exports, dominating legacy Node code; ESM is async, static, tree-shakeable, and standard across browsers and modern Node. Interop quirks: default export wrapping, __dirname absence in ESM (derive from import.meta.url). New libraries should ship ESM first.' },
  { t: 'dynamic import', d: 'import("./mod") returns a promise resolving to the module namespace, letting you load code conditionally, lazily, or after user interaction. Bundlers split dynamic imports into separate chunks automatically, forming the basis of route-based code splitting. Handle rejection for offline/error states.' },
  { t: 'JSON serialization pitfalls', d: 'JSON.stringify silently drops functions, undefined, and Symbol keys, converts NaN/Infinity to null, turns Dates into ISO strings, throws on circular structures, and loses Map/Set entirely. toJSON methods customize output. For lossy-safe cloning prefer structuredClone; for transport, sanitize data first.' },
  { t: 'floating point precision', d: 'JavaScript numbers are IEEE-754 doubles, so 0.1 + 0.2 === 0.300000004... fails exact comparison and 0.1+0.2 prints 0.30000000000000004. Compare within epsilon, scale integers for money (or use BigInt minor units / decimal libraries), and format with toFixed/Intl.NumberFormat for display only.' },
  { t: 'Number safe integer range', d: 'Integers above Number.MAX_SAFE_INTEGER (2^53 - 1) lose precision silently because doubles allocate bits to exponent. Use BigInt (append n) for ids from databases or crypto contexts that exceed this range, remembering BigInt cannot mix with Number in arithmetic and JSON serializes it as string manually.' },
  { t: 'NaN quirks', d: 'NaN is the only value unequal to itself, so isNaN(NaN) is true while NaN === NaN is false. Global isNaN coerces its argument ("42" passes!), whereas Number.isNaN checks strict NaN identity. NaN arises from failed parses, invalid math (0/0, Math.sqrt(-1)), and undefined arithmetic \u2014 guard boundaries before math happens.' },
  { t: 'parseInt and parseFloat', d: 'parseInt stops at the first non-digit, ignores leading whitespace, accepts signs and radix; always pass radix 10 to avoid legacy octal interpretations ("08" once parsed as 0). parseFloat handles decimals and exponents similarly stopping early. For strict validation prefer Number(), which returns NaN on any residue.' },
  { t: 'string immutability', d: 'Strings are immutable primitives: every "modification" method returns a new string, and indexing assignment silently fails (strict mode throws). This guarantees safe sharing and enables interning optimizations. Heavy concatenation in loops should use arrays plus join or template accumulation to avoid quadratic copying.' },
  { t: 'slice vs substring vs substr', d: 'slice(start, end) supports negatives and is the modern choice; substring swaps out-of-order args and clamps negatives to 0; substr(start, length) is deprecated legacy. For robust code stick to slice, mirroring Array.slice semantics, and use padStart/padEnd or repeat for construction tasks.' },
  { t: 'regular expressions basics', d: 'RegExp literals /pattern/flags compile once; flags include g (global), i, m, s (dotAll), u (unicode), y (sticky). Test with .test or exec loops honoring lastIndex statefulness under g. Named groups (?<year>\\d{4}) surface via match.groups, improving readability over positional indices.' },
  { t: 'replace with callback functions', d: 'String.replace accepts a function receiving (match, groups..., offset, string) whose return substitutes dynamically \u2014 essential for templating, escaping, and case transformations. Without the g flag only the first occurrence changes, a frequent bug. replaceAll exists precisely to make global replacement explicit.' },
  { t: 'Date handling gotchas', d: 'Date months are zero-indexed (0 = January) while days are one-indexed \u2014 a legendary trap. Parsing ISO-without-timezone treats it as local in some engines; date-only strings parse as UTC. Prefer Temporal (where available) or date-fns/dayjs; always store UTC, render local, and never do timezone math by hand.' },
  { t: 'Intl APIs', d: 'Intl.NumberFormat, DateTimeFormat, Collator and ListFormat localize numbers, dates, sorting, and lists without libraries, using BCP-47 locales. Cache formatter instances \u2014 construction is expensive. Collator fixes locale-aware alphabetical sorting that default string comparison mangles for accented characters.' },
  { t: 'for...of vs for...in', d: 'for...of iterates values of iterables (arrays, strings, maps) respecting insertion order; for...in enumerates string keys including inherited enumerable ones, historically meant for objects. Using for...in on arrays yields indices as strings and prototype noise. Default to for...of for collections and Object.entries for object data.' },
  { t: 'Array.map', d: 'map transforms each element through a callback into a same-length new array, leaving the original untouched. Callbacks receive (element, index, array) \u2014 extra params matter when feeding parseInt (index becomes radix!). Return undefined intentionally only when building sparse results; otherwise choose forEach or flatMap.' },
  { t: 'Array.filter and find', d: 'filter keeps elements passing the predicate into a new array; find returns the first match or undefined, short-circuiting early. findIndex, findLast and findLastIndex extend the family. Choosing find over filter[0] communicates intent and avoids scanning the whole collection.' },
  { t: 'Array.reduce patterns', d: 'reduce folds an array into any accumulated value: sums, groupings, frequency maps, pipe composition, even promises. Always supply an initial value to dodge empty-array TypeErrors and type instability. When logic grows gnarly, a plain loop or Object.groupBy is clearer than heroic reduce gymnastics.' },
  { t: 'sort mutability and comparator', d: 'Array.sort sorts in place and returns the same array; default comparison converts to strings, so 10 sorts before 9. Provide (a,b)=>a-b for numbers and localeCompare or Intl.Collator for strings. ES2019 mandates stable sort, preserving relative order of equal elements. Use toSorted for non-mutating copies.' },
  { t: 'splice vs slice', d: 'splice mutates, removing/inserting items at an index and returning removed elements; slice copies a section non-destructively. Confusing them causes ghost bugs. Modern immutable updates favor slice/spread/toSpliced, reserving splice for deliberate in-place edits on large arrays where copying costs matter.' },
  { t: 'flat and flatMap', d: 'flat(depth) flattens nested arrays (default depth 1, Infinity fully), while flatMap maps then flattens one level \u2014 replacing awkward [].concat(map(...)) patterns. Typical uses: normalizing trees, expanding one-to-many relations, splitting sentences into word streams in a single pass.' },
  { t: 'array deduplication techniques', d: 'Primitives: [...new Set(arr)] is simplest and fastest at scale. Objects require key strategy \u2014 Map keyed by id, filter with seen-set of serialized keys, or reduce into a Map preserving last-wins semantics. Beware NaN handling differences and key collisions when serializing.' },
  { t: 'Array.from and Array.of', d: 'Array.from converts array-likes (arguments, NodeList) and iterables to arrays, optionally mapping during conversion \u2014 handy for range generation: Array.from({length:n},(_,i)=>i). Array.of wraps arbitrary values into an array, fixing new Array(3) hole weirdness. Both respect custom iterables and this-mapping receivers.' },
  { t: 'sparse arrays and holes', d: 'Deleting elements or new Array(3) creates holes skipped by forEach, map preserves them, and join renders empty strings, causing inconsistent iteration behavior. Avoid holes: use fill, splice, or filter(Boolean) normalization. length truncation (arr.length=0) clears arrays cheaply and deliberately.' },
  { t: 'includes vs indexOf', d: 'includes finds membership with correct NaN handling and clean booleans; indexOf returns position or -1 and misreports NaN. Performance is comparable (both linear). includes reads better for existence checks, indexOf wins when you also need the position, and findIndex generalizes to predicate search.' },
  { t: 'event bubbling and capturing', d: 'DOM events propagate capturing downward, hit the target, then bubble upward. Listeners default to bubble phase; capture:true engages the descent. stopPropagation halts travel; stopImmediatePropagation also silences sibling listeners on the same node. Understanding phases underpins delegation and overlay-close-outside-click logic.' },
  { t: 'event delegation', d: 'Delegation attaches one listener on an ancestor, inspecting event.target (via closest(selector)) to react to descendants \u2014 saving memory on long lists and automatically covering dynamically added nodes. It exploits bubbling, so events that do not bubble (focus, scroll per element) need capture-phase delegation instead.' },
  { t: 'preventDefault vs stopPropagation', d: 'preventDefault cancels the browser default action (link navigation, checkbox toggle, form submit) while propagation continues; stopPropagation stops ancestor listeners without touching defaults. They answer different questions \u2014 "should the browser act?" versus "should ancestors hear?" \u2014 and frequently both are needed together.' },
  { t: 'custom events', d: 'CustomEvent with detail payload enables decoupled component communication through the DOM bus: dispatchEvent(new CustomEvent("cart:add", { detail, bubbles: true })). Listeners read event.detail. Useful for widget isolation and framework-free architecture; document the contract since types vanish at runtime.' },
  { t: 'localStorage vs sessionStorage vs cookies', d: 'localStorage persists ~5MB per origin indefinitely; sessionStorage dies with the tab; cookies (~4KB) ride every HTTP request enabling server reads with Secure/HttpOnly/SameSite attributes. Store non-sensitive UI state locally, auth in HttpOnly cookies, and never secrets in localStorage where XSS can read them.' },
  { t: 'IndexedDB', d: 'IndexedDB is a transactional, asynchronous, indexed object store supporting hundreds of megabytes, ideal for offline-first apps and large datasets beyond localStorage limits. Raw API is verbose; idb wrapper helps. Structured-clone storage means real Dates/Maps persist intact, unlike stringified localStorage values.' },
  { t: 'fetch API essentials', d: 'fetch returns a promise resolving as soon as headers arrive \u2014 network failures reject, but HTTP 404/500 resolve OK-false, so check response.ok and status manually. Reading the body consumes it once via json()/text(). Configure credentials mode for cookies, AbortController for cancellation, and handle timeouts explicitly since fetch lacks a built-in timeout.' },
  { t: 'AbortController', d: 'AbortController pairs a signal with cancellable operations: pass signal into fetch or event listeners, then abort() rejects with AbortError named reason. React effects abort stale requests preventing race-condition state overwrites. Reuse one controller to cancel many operations, and always distinguish user-cancellations from real errors in catch blocks.' },
  { t: 'CORS fundamentals', d: 'CORS is browser-enforced: cross-origin requests trigger preflight OPTIONS when carrying custom headers, JSON content-type, or non-simple methods; servers must echo Access-Control-Allow-Origin (never * with credentials) and allowed headers/methods. Failures log as network errors with opaque responses \u2014 fix server headers or proxy, since client JS cannot bypass CORS.' },
  { t: 'Web Workers', d: 'Web Workers run scripts on background threads communicating via postMessage structured-clone messaging, keeping heavy computation (parsing, image processing, crypto) off the main thread so UI stays responsive. No DOM access; importScripts or module workers allowed. SharedArrayBuffer plus Atomics enable zero-copy shared memory when COOP/COEP headers permit.' },
  { t: 'requestAnimationFrame', d: 'rAF schedules a callback right before next paint (~60fps), synchronizing animations with display refresh and auto-pausing in background tabs, unlike raw setTimeout jitter. Batch DOM mutations inside rAF to avoid layout thrash. Pair with timestamps for frame-rate-independent easing calculations.' },
  { t: 'IntersectionObserver', d: 'IntersectionObserver asynchronously reports element visibility intersections with viewport thresholds without scroll-janky geometry polling. Powers lazy-loading images, infinite scroll triggers, view-impression analytics, and header shrink effects. Callbacks deliver entries with isIntersecting and intersectionRatio; disconnect() cleans up subscriptions.' },
  { t: 'MutationObserver', d: 'MutationObserver watches DOM subtree/attribute/text changes asynchronously via microtasks, enabling third-party-script integration, undo systems, and framework-less reactive UIs. Observe with options filtering childList/attributes/subtree/characterData to limit overhead, and disconnect when done to release references.' },
  { t: 'ResizeObserver', d: 'ResizeObserver fires when an element\u2019s box size changes \u2014 including container-driven layout shifts media queries cannot see \u2014 enabling chart redraws and container-responsive components. Callbacks receive contentRect/device-pixel sizes; beware observe-loops where resizing inside callback retriggers it (guard or debounce).' },
  { t: 'History API and routing', d: 'history.pushState/replaceState mutate URL without reload; popstate fires on back/forward. SPAs layer route matching over these primitives, syncing location to views. Always handle direct-load deep links server-side (fallback to index.html) and restore scroll positions manually since browsers skip restoration for pushed entries.' },
  { t: 'URL and URLSearchParams', d: 'URL parses/builds absolute and relative hrefs exposing protocol, pathname, searchParams with typed get/set/append/delete/has and iteration. URLSearchParams encodes properly, avoiding manual encodeURIComponent mistakes. Combining them beats regex surgery on query strings and handles edge cases like encoded slashes gracefully.' },
  { t: 'FormData and File uploads', d: 'FormData serializes form fields for fetch bodies with automatic multipart encoding, supporting File/Blob entries appended programmatically for uploads. Track progress via XMLHttpRequest upload.onprogress or stream fetch with duplex halves. Server-side frameworks parse multipart automatically; setting Content-Type manually breaks boundary generation.' },
  { t: 'Blob and FileReader', d: 'Blobs represent immutable binary data; URLs via URL.createObjectURL enable downloads and media previews (revoke to free memory). FileReader asynchronously reads blobs as text/dataURL/arrayBuffer, though blob.arrayBuffer()/text() promise methods now cover most needs. Canvas.toBlob exports drawings for upload pipelines.' },
];
