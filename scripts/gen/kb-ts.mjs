// TypeScript topics
export default [
  { t: 'what TypeScript adds to JavaScript', d: 'TypeScript layers a static type system over JavaScript, erased at compile time producing plain JS. Benefits: editor intelligence, refactor safety, self-documenting APIs, and bug classes caught pre-runtime. It deliberately follows JS syntax evolution closely rather than inventing parallel language semantics.' },
  { t: 'any vs unknown', d: 'any disables all checking inviting runtime surprises; unknown accepts anything but demands narrowing before use \u2014 safe top type. Rule: unknown for inputs you will validate (API responses), any only as documented last resort at legacy seams. unknown forces honesty about shape assumptions.' },
  { t: 'never type', d: 'never is the bottom type inhabitable by nothing: functions that always throw, infinite loops, or exhaustiveness-checked switches\u2019 else branch. Assigning never in default case proves all union members handled \u2014 adding a variant then breaks compilation until handled, an elegant guard.' },
  { t: 'void vs undefined return', d: 'void means callers should not use the return value (though undefined flows through); explicit undefined demands returning it. Callbacks typed void permit any-returning implementations safely (return values ignored) \u2014 why Array.forEach callbacks may return mapped values harmlessly.' },
  { t: 'union and intersection types', d: 'Unions (A | B) model either-or values requiring narrowing before member access; intersections (A & B) merge requirements modeling combination. Unions power discriminated state machines; intersections compose mixin capabilities. Nested unions flatten automatically during checking.' },
  { t: 'type narrowing techniques', d: 'Narrowing happens via typeof/instanceof checks, truthiness, in-operator presence, equality literals, custom predicates (x is T), assertion functions, and discriminant switches. Control-flow analysis tracks narrowing through branches including early returns \u2014 write guards once, enjoy precise types everywhere after.' },
  { t: 'discriminated unions pattern', d: 'Give each union member a literal tag field (kind: "circle"|"square"); switching on kind narrows fields precisely per branch. This replaces boolean-flag soup with exhaustive, compiler-verified state machines \u2014 the single most valuable TS pattern for app architecture.' },
  { t: 'literal types', d: 'Literal types restrict values to exact primitives ("left" | "right", 1 | 2). Combined with unions they replace enums for fixed vocabularies; as const captures object/array shapes as readonly literals powering config typing. Widening rules keep inference pragmatic in mutable contexts.' },
  { t: 'as const assertions', d: 'as const freezes expressions into deepest-readonly literal types: { method: "GET" } becomes method: "GET" instead of string, arrays become readonly tuples. Essential for action-type constants, route tables, and config objects where literal precision drives downstream discrimination.' },
  { t: 'type aliases vs interfaces', d: 'Interfaces extend/merge via declaration merging and read naturally for object contracts; aliases express unions, tuples, conditionals, mapped types \u2014 everything interfaces cannot. Performance differences are negligible; team consistency matters more. Error messages sometimes differ subtly favoring one in specific scenarios.' },
  { t: 'structural typing meaning', d: 'Compatibility checks shape not name: {x:number} accepts any object carrying x:number regardless of declared origin \u2014 duck typing at compile time. Enables frictionless composition but permits accidental matches; branding adds nominal markers when identity distinctness truly matters (UserId vs OrderId).' },
  { t: 'branded types / opaque types', d: 'Branding intersects a nominal phantom tag: type UserId = string & { __brand: "UserId" }, constructible only via smart constructors. Prevents mixing structurally identical ids in wrong arguments \u2014 classic bug class eliminated at zero runtime cost.' },
  { t: 'optional properties', d: 'The ? modifier allows absence making field T | undefined on reads (with strictNullChecks). Distinguish missing versus explicitly-undefined via exactOptionalPropertyTypes when precision matters. Optional methods differ subtly from possibly-undefined function-typed fields in call-site ergonomics.' },
  { t: 'readonly modifiers', d: 'readonly forbids property reassignment (compile-time only) and Readonly<T>/ReadonlyArray<T> freeze views of data. Functions declaring readonly params promise non-mutation enabling safe shared-state passing. Deep immutability needs recursive mapped types or libraries since readonly is shallow.' },
  { t: 'tuples', d: 'Tuples type fixed-length/indexed arrays ([string, number]) capturing heterogeneous positional data like CSV rows, hook returns, function args spreads. Labeled tuples document element meanings; variadic tuples model array-prefix/suffix patterns generically for higher-kinded-ish utility authoring.' },
  { t: 'enums tradeoffs', d: 'Numeric enums reverse-map and auto-number risking surprise collisions; string enums serialize readably. Many teams prefer const-object + union-of-keys patterns avoiding enum emit quirks under isolatedModules. Enums remain valid choices for closed sets needing namespace-like grouping.' },
  { t: 'generics fundamentals', d: 'Generics parameterize types preserving relationships: identity<T>(v: T): T keeps input/output linked where any would sever it. Callers infer or annotate explicitly. Multiple parameters compose relationships across arguments (mapKeys<K,V>). Generic code writes itself following concrete examples first.' },
  { t: 'generic constraints extends', d: 'extends clauses bound type params: <T extends { id: string }> guarantees id access inside while retaining specificity. Constraints unlock operations without sacrificing caller inference. Default type parameters soften call sites: <T = string> when unannotated usage dominates.' },
  { t: 'keyof operator', d: 'keyof T yields union of T\u2019s keys enabling safe dynamic access: function get<T,K extends keyof T>(o:T,k:K):T[K]. Pairs with mapped types transforming whole shapes generically. keyof typeof config converts value-level config into its type-level key universe elegantly.' },
  { t: 'typeof type operator', d: 'Value-position typeof extracts a value\u2019s type: typeof myConfig derives the shape without manual duplication. Guard against circularity (value referencing its own derived type). Combined with as const, config files become schema sources of truth feeding validation and docs.' },
  { t: 'indexed access types', d: 'T["prop"] extracts property types including nested paths (T["a"]["b"]); arrays index numerically ([number] yields element type). Powers ReturnType/Parameters-style utilities generically. Nonexistent keys error immediately catching refactored-property drift in dependent utilities.' },
  { t: 'mapped types', d: '[K in keyof T]: ... transforms every key systematically \u2014 Readonly, Partial are built-in exemplars. Key remapping via as clauses filters/renames (omit by exclusion, template-literal renames). Mapped types are the workhorse of generic API-shaping utilities in real codebases.' },
  { t: 'template literal types', d: 'Template literal types concatenate string types enforcing formats: `${K}-${number}` validates event names/routes at compile time. Combined with key remapping they generate derived key names (getters, setters). Regex-literal types (TS 5.5+) narrow via pattern matching inference.' },
  { t: 'conditional types', d: 'T extends U ? X : Y branches types on relationships; nesting composes logic (deep flattening, unwrapping promises). Distribution over naked union params multiplies branches per member \u2014 wrap in [] brackets to disable when aggregate semantics intended.' },
  { t: 'infer keyword', d: 'Inside conditional extends, infer captures unknown positions: T extends Promise<infer V> extracts V; Parameters/ReturnType implement exactly this trick. Multiple infers solve pattern-matching puzzles (head/tail extraction). Read stdlib utility definitions to internalize idiomatic infer usage patterns.' },
  { t: 'built-in utility types tour', d: 'Partial/Required/Readonly reshape optionality; Pick/Omit project subsets; Record builds dictionaries; Exclude/Extract filter unions; NonNullable strips nullish; ReturnType/Awaited unwrap functions/promises; InstanceType grabs constructor products. Knowing these cold eliminates hand-rolled duplicates daily.' },
  { t: 'Record deep dive', d: 'Record<string, T> types arbitrary-key dictionaries; Record<Union, T> demands every union key present \u2014 exhaustive maps checked at compile time. Combine with keyof typeof constants generating aligned lookup tables. Partial<Record<...>> relaxes totality when sparse entries legitimate.' },
  { t: 'Pick and Omit implementation', d: 'Pick<T, K extends keyof T> selects keys via mapped type; Omit<T, K> = Pick<T, Exclude<keyof T, K>> removes them. Omit\u2019s string-K looseness historically allowed typos (TS strictness varies); hand-rolling stricter variants documents awareness of these sharp edges.' },
  { t: 'satisfies operator', d: 'satisfies validates expression against type while preserving inferred literal narrowness \u2014 unlike annotation which widens. Config: const routes = {...} satisfies Record<Route, Config> keeps route literal keys usable downstream. Best of both worlds for constant declarations needing both check and precision.' },
  { t: 'type assertions and their dangers', d: 'as casts silence checker without runtime verification \u2014 lies propagate crashes later. Prefer narrowing, parsing (zod), or satisfies. Double assertions (as unknown as T) signal design smells demanding refactoring. Assertions shine at genuine boundary conversions where humans know more than types can express.' },
  { t: 'non-null assertion', d: 'Postfix ! claims value is not null/undefined bypassing checks. Legitimate after external invariants (DOM getElementById with known markup); dangerous when assumptions drift. Team linting often bans it outside tests, forcing honest optional handling at true uncertainty points.' },
  { t: 'definite assignment assertion', d: 'let x!: T declares initialization happening elsewhere (framework DI, beforeEach hooks) silencing strictPropertyInitialization/used-before-assigned errors. Prefer actual initialization or refactor toward explicit lifecycle; assertions document trust in orchestration orderings that tests must pin down.' },
  { t: 'declaration merging', d: 'Same-named interfaces merge members \u2014 how lib.dom augments Window or express adds Request.user via module augmentation. Powerful extension mechanism, confusing when accidental collisions occur. Namespaced merging plus enum merging round out ambient augmentation scenarios plugin authors rely upon.' },
  { t: 'ambient declarations and .d.ts', d: '.d.ts files declare shapes without emitting JS \u2014 describing globals, modules, environments. DefinitelyTyped (@types/*) hosts community definitions. Writing minimal d.ts shims for untyped deps beats any-walls; triple-slash references wire dependencies between declaration files explicitly.' },
  { t: 'module augmentation example', d: 'declare module "lib" { interface Config { debug?: boolean } } extends third-party types in-place, keeping imports unchanged. Must live in module context (file with import/export). Use sparingly \u2014 augmentation couples you to target internals versioned independently of your code.' },
  { t: 'declare global usage', d: 'declare global { interface Window { __APP__: App } } extends global scopes from within modules. Common for analytics queues, feature flags, injected config. Runtime counterpart (script injection/d.ts ambient) must actually exist or illusions crash production users mysteriously.' },
  { t: 'function overloads', d: 'Overload signatures list call shapes preceding one implementation resolving dynamically. Prefer union params/generics when possible; overloads earn complexity with genuinely divergent returns per input shape (createElement variants, date parsers). Implementation signature stays invisible to callers.' },
  { t: 'rest parameter typing', d: 'Rest params type as tuples/arrays: (...args: [name: string, retries?: number]) labels positions. Variadic generics spread tuple types through wrappers preserving argument lists precisely \u2014 how promisify-style helpers retain full call-signature fidelity through transformation.' },
  { t: 'this parameter typing', d: 'Explicit this: Type annotates expected contexts catching lost-this bugs in standalone functions; this is void forbids this usage entirely. noImplicitThis enforces awareness. Arrow methods inherit enclosing this types lexically sidestepping most manual annotation ceremony.' },
  { t: 'contextual typing magic', d: 'Types flow backwards from expectations into lambdas: handlers receive event types without annotations because addEventListener signature contextualizes callback params. Return position contextualization shapes JSX children callbacks similarly. Understanding contextual typing explains why some annotations feel redundant.' },
  { t: 'inference limitations worth knowing', d: 'Inference struggles with: unused generic params (defaults to unknown), recursive types mid-flight, complex conditional resolution without hints, and tuple widening in loops. Annotate boundaries (public APIs) generously, internals sparingly \u2014 strategic annotation placement maximizes leverage of the inference engine.' },
  { t: 'const type parameters', d: '<const T> infers literal-preserving arrays/objects from call arguments without as const at every call site: makeRoute<const T>(r: T). Bridges ergonomic gap for helper functions receiving configuration shapes where literal fidelity matters downstream.' },
  { t: 'generic components typing', d: 'function List<T>({items, renderItem}: {items: T[]; renderItem: (item: T) => ReactNode}) preserves item types through render props end-to-end. Class components mirror via class C<T> extends Component<Props<T>>. Inference flows from props usage keeping call sites annotation-free typically.' },
  { t: 'polymorphic component pattern', d: 'as-prop polymorphism renders same component as different elements: <Button as="a" href>. Typing merges element props conditionally via generics mapping ElementType\u2192Props. Complex but reusable once authored; libraries like Radix ship polished implementations worth studying before hand-rolling.' },
  { t: 'typing hooks correctly', d: 'useState<Date | null>(null) annotates unions explicitly when initial null misleads inference; useReducer benefits from Action discriminated unions; custom hooks return tuples as const preserving positional literal types. Refs type via useRef<HTMLDivElement>(null) matching attach targets precisely.' },
  { t: 'event handler typing', d: 'React.ChangeEvent<HTMLInputElement>, FormEvent<HTMLFormElement>, KeyboardEvent etc. provide precise currentTarget types. Inline handlers contextualize automatically; extracted handlers annotate explicitly. DOM lib events differ from React synthetic ones \u2014 import React types for component props, DOM types for native listeners.' },
  { t: 'children typing nuances', d: 'ReactNode covers everything renderable (elements, strings, numbers, portals, undefined); ReactElement restricts to actual elements; JSX.Element narrows further. Accept ReactNode broadly for content slots; require ReactElement when inspecting/manipulating structure programmatically.' },
  { t: 'forwardRef typing pattern', d: 'forwardRef<HTMLDivElement, Props>((props, ref) => ...) pairs node type with props; useImperativeHandle exposes typed handle interfaces. React 19 simplifies (ref as prop) but dual-version libraries maintain forwardRef shims. Element type correctness feeds refs consumers depend upon.' },
  { t: 'reducer action typing', d: 'Discriminated union Actions with payload per kind; reducer switch exhaustively narrows accessing payload safely; never-type default catches unhandled cases at compile time. Export action creators typed individually. This trio (unions/exhaustive-switch/creators) defines robust reducer contracts.' },
  { t: 'context creation typing', d: 'createContext<State | null>(null) forces null-checks at consumers unless provider-guaranteed pattern used: createContext<T | undefined>(undefined) plus custom hook asserting existence, returning T directly. Custom-hook encapsulation centralizes the invariant keeping call sites clean and honest.' },
  { t: 'generic fetch wrapper typing', d: 'api.get<User>("/users/1"): Promise<User> parametrizes response decoding; combine with zod schemas inferring types (z.infer) so runtime validation and compile types share source. Never cast responses blindly \u2014 parse boundaries convert unknown wire data into trusted domain models.' },
  { t: 'error handling Result pattern', d: 'Result<T, E> = { ok: true, value: T } | { ok: false, error: E } forces handling via discrimination before value access. Alternatives: exceptions typed loosely at boundaries. Functional style suits pipelines; pragmatic teams adopt selectively for critical flows like payments.' },
  { t: 'Partial deep utility', d: 'Built-in Partial is shallow; deep partial recursion maps nested objects conditionally: type DeepPartial<T> = T extends object ? { [K in keyof T]?: DeepPartial<T[K]> } : T. Essential for patch/update payloads in config systems and store update actions.' },
  { t: 'DeepReadonly implementation', d: 'Recursive mapped type freezing nested structures: arrays map to readonly arrays, functions stay untouched, objects recurse. Pairs with frozen runtime enforcement for true immutability contracts. Watch performance on huge types \u2014 compiler effort scales with depth/breadth.' },
  { t: 'TupleToUnion and friends', d: 'type TupleToUnion<T extends readonly unknown[]> = T[number] collapses tuples into member unions. Inverse (union-to-tuple) impossible generally \u2014 order unknowable. These micro-utilities demonstrate indexed-access mastery frequently probed in advanced interviews.' },
  { t: 'exhaustiveness with assertNever', d: 'function assertNever(x: never): never { throw new Error() }; default: return assertNever(action) turns forgotten union members into compile errors pinpointing exactly which case went missing. Superior to silent fallthroughs guarding mission-critical state machines.' },
  { t: 'strict mode flags explained', d: 'strict bundles noImplicitAny, strictNullChecks, strictFunctionTypes, strictBindCallApply, strictPropertyInitialization, noImplicitThis, alwaysStrict, useUnknownInCatchVariables. Each closes distinct unsoundness hole. Greenfield projects enable strict always; migrations proceed flag-by-flag measuring error volume realistically.' },
  { t: 'noUncheckedIndexedAccess', d: 'Index signatures/tuple access yield T | undefined under this flag acknowledging reality that lookups miss. Forces explicit existence handling \u2014 initially noisy, ultimately honest. Map iteration (Object.entries) sidesteps noise where presence is structural rather than incidental.' },
  { t: 'exactOptionalPropertyTypes', d: 'Distinguishes {a?: string} assignments of undefined (forbidden) versus omission (allowed), matching serialization semantics where undefined-vs-missing changes JSON output. Adoption requires auditing assignment sites; worth it for API-boundary fidelity where distinction carries meaning.' },
  { t: 'isolatedModules and type exports', d: 'Transpilers process files independently so type-only imports/exports need explicit `import type` marking preventing runtime import of vanished symbols. verbatimModuleSyntax enforces consistency harder. Misconfigurations manifest as mysterious runtime errors importing pure-type modules.' },
  { t: 'tsconfig essentials walkthrough', d: 'Core keys: target/lib (syntax/runtime features), module/moduleResolution (interop strategy), strict family, jsx, paths (aliases), include/exclude scope, outDir/noEmit split (bundler handles emit). Extending shared base configs standardizes monorepo settings hierarchically.' },
  { t: 'path aliases configuration', d: 'paths {"@/*": ["./src/*"]} plus bundler alias (vite resolve.alias) mirror each other \u2014 forgetting one breaks either editor navigation or build. Aliases clarify architecture layers visually; enforce boundaries via lint rules preventing upward dependency violations creeping back.' },
  { t: 'declaration emit and package types', d: 'declaration:true generates .d.ts alongside outputs; declarationMap enables go-to-definition into sources for consumers. Package exports field maps types conditions ensuring editors pick correct entries per environment (node/browser/types). Publishing without types cripples DX immediately.' },
  { t: 'type-only imports erasure', d: '`import type { Foo }` guarantees complete erasure avoiding runtime imports of type-only modules (classes-as-types included). Improves bundle hygiene, breaks import cycles intentionally, and clarifies intent reading imports section. Consistent adoption prevents isolatedModules footguns wholesale.' },
  { t: 'namespace vs module history', d: 'Namespaces (global IIFE grouping) predate ES modules \u2014 legacy codebases and ambient declarations still use them. New code uses ES modules exclusively; namespaces survive legitimately in d.ts organizing large ambient APIs. Migrating namespaces\u2192modules proceeds incrementally file-by-file.' },
  { t: 'triple-slash directives', d: '/// <reference path/types/lib /> wires file-level dependencies predating imports \u2014 still canonical for @types inclusion ordering and lib specification in d.ts. Modern tsconfig handles most cases; directives persist in ambient declaration ecosystems where per-file precision matters.' },
  { t: 'working with untyped libraries', d: 'Options ladder: search @types first; declare module "pkg" shim minimal surfaces actually used; contribute definitions upstream; wrap library behind typed facade isolating blast radius. Avoid blanket any-declarations hiding entire dependency surfaces from checking permanently.' },
  { t: 'migration strategies JS to TS', d: 'allowJs+checkJs incremental entry; rename hotspots (.js\u2192.ts) prioritizing boundaries/data models; suppress strategically (any/todo comments tracked as issues); tighten strictness progressively per-directory configs. Types migrate bottom-up: leaf utilities first, framework glue last, tests anytime.' },
  { t: 'JSDoc-driven typing for JS files', d: 'checkJs plus JSDoc annotations ({@param {number} x}) deliver much TS value without renaming \u2014 useful during gradual migration or JS-mandated environments. Types reference .ts-authored definitions bidirectionally. Long-term convergence still favors full TS conversion for tooling depth.' },
  { t: 'circular type references handling', d: 'Mutually recursive types (tree nodes, ASTs) work naturally; value-level circular imports complicate emission order occasionally. Interface indirection breaks cycles aliases cannot (aliases eagerly resolve). Lazy conditional types defer resolution sidestepping infinite expansion traps in pathological recursive generics.' },
  { t: 'variance annotations basics', d: 'in/out variance keywords (TS 4.7+) fix variance explicitly for tricky generic types (event emitters, functors) where structural inference picks inconvenient variance causing false errors. Rarely needed but decisive when sound-but-annoying situations arise in higher-kinded simulations.' },
  { t: 'covariance contravariance intuition', d: 'Return positions covary (subtype outputs OK); parameter positions contravariant for soundness (handlers accepting broader inputs substitute safely). strictFunctionTypes enforces parameter contravariance on function types catching unsound callback substitutions bivariance previously permitted silently.' },
  { t: 'unique symbol usage', d: 'unique symbol creates distinct symbol types usable as branded keys/discriminants: const s: unique symbol = Symbol(). Enum members implicitly unique symbols. Combines with template/mapped machinery for nominal-flavored patterns where plain strings risk collision ambiguity.' },
  { t: 'abstract classes typing role', d: 'Abstract classes declare unimplemented members enforcing subclass contracts while shipping shared implementation \u2014 midway between interfaces and concrete bases. Cannot instantiate directly. Prefer interfaces + composition generally; abstract bases justify themselves sharing real plumbing across siblings.' },
  { t: 'access modifiers recap', d: 'public default, private (TS-only, erased), #private (runtime-enforced ECMAScript), protected exposing to subclasses. Choose # when runtime privacy matters (libraries hardening internals); TS private suffices for compile-time discipline. Mixing both on same member is an error.' },
  { t: 'implements vs extends distinction', d: 'implements asserts shape conformance without inheritance; extends inherits implementation. Classes may implement multiple interfaces but extend one base. Common confusion: implements alone provides nothing inherited \u2014 bodies must be written or delegated deliberately.' },
  { t: 'static side vs instance side typing', d: 'Class types describe instances; typeof Class describes static side (constructors, static members). Generic factory constraints use abstract constructors: ctor: new (...args:any[]) => Base or abstract new capturing both sides properly for injectable class tokens.' },
  { t: 'mixins typing approach', d: 'Typed mixins chain via constructor generics: type Constructor<T = {}> = new (...args:any[]) => T; function Timestamped<TBase extends Constructor>(Base: TBase) { return class extends Base { created = Date.now() } }. Expression mixins compose behaviors while preserving instance types through the chain.' },
  { t: 'symbol iterator typing', d: '[Symbol.iterator](): Iterator<T> or generator signatures make custom collections iterable under for..of/spread with full element typing. IterableIterator<T> returned by generators composes nicely. Typed iterables integrate seamlessly with destructuring preserving element precision.' },
  { t: 'JSON-safe types', d: 'type JSONValue = string|number|boolean|null|JSONValue[]|{[k:string]:JSONValue} models serializable trees constraining storage/transmission payloads. Parsing returns JSONValue demanding narrowing/validation into domain types \u2014 honest pipeline from wire to model.' },
  { t: 'date handling typing pitfalls', d: 'API dates arrive strings, become Date objects post-parse; conflating them spawns string-method-on-Date bugs. Model transport DTOs separately (createdAt: string) from domain models (createdAt: Date) converting at mapping layer. Branded ISOString types catch premature arithmetic attempts.' },
  { t: 'record iteration typing', d: 'Object.entries(obj) widens values historically; keyof-based generics preserve precision: Object.entries(obj as Record<Key, Val>) or typed helpers. for..in keys widen to string \u2014 capture via keyof assertion locally. These frictions motivate Map in precision-critical code.' },
  { t: 'index signature caveats', d: 'Index signatures demand ALL properties conform (interface StringMap { [k:string]: string; length?: number } conflicts unless compatible). They also erase excess-property protection internally. Prefer Record<> aliases, Partial records, or Map for flexible keyed collections with cleaner semantics.' },
  { t: 'zod schema-first validation', d: 'Schemas define runtime validators whose z.infer derives static types single-sourcing truth: const UserSchema = z.object({...}); type User = z.infer<typeof UserSchema>. Parse at boundaries (forms/APIs/env), fail fast with actionable issues. Replaces drift-prone duplicate manual types entirely.' },
  { t: 'env variable typing', d: 'Augment ImportMetaEnv/Vite envelope: interface ImportMetaEnv { readonly VITE_API_URL: string } giving typed process/import.meta access. Validate at boot (parse env through schema) converting silent-undefined bugs into startup failures with clear messages listing missing keys.' },
  { t: 'generic defaults and multiple params', d: 'Default type params (<T = HTMLElement>) ease common callsites; multiple params compose relationships (keyOfMap<K,V>). Parameter ordering places inferrable params first, explicit-only last, minimizing angle-bracket noise. Renaming type params descriptively (TRow vs T2) pays readability dividends.' },
  { t: 'conditional distribution gotcha', d: 'Naked type params distribute unions through conditionals: (A|B) extends U ? ... evaluates per-member. Wrap [T] extends [U] to test aggregate. This behavior powers elegant union filtering (Exclude) yet surprises when aggregate checks intended \u2014 bracket discipline communicates intent explicitly.' },
  { t: 'recursive types practical uses', d: 'JSON trees, comment threads, filesystem nodes, ASTs model recursively. Depth-limiting via numbered recursion counters prevents runaway computation in extreme cases. Compiler recursion limits (50 instantiation depth historically) surface as cryptic errors demanding structural flattening occasionally.' },
  { t: 'lookup table typing pattern', d: 'const HANDLERS: Record<EventType, Handler> enforces every event covered; adding event type breaks compile until handler added \u2014 config completeness verified statically. Pair with satisfies retaining literal keys for downstream keyof derivations. Replaces scattered switch statements with data-driven dispatch.' },
  { t: 'typed event emitter pattern', d: 'Generic emitter parameterizes events map: on<K extends keyof Events>(event: K, cb: (payload: Events[K]) => void) delivering payload typing per subscription. Removes stringly-typed listener bugs wholesale. Implementations span tiny custom classes to battle-tested mitt/eventemitter3 wrappers.' },
  { t: 'assertion functions', d: 'function assertIsUser(x: unknown): asserts x is User narrows callers\u2019 views post-call without returning boolean \u2014 throws otherwise. Complements type predicates for control-flow-based narrowing where exception semantics fit naturally (invariant violations, precondition failures).' },
  { t: 'type predicates vs assertions choice', d: 'Predicates (x is T) return booleans composing into filters/conditionals; assertions throw narrowing imperatively. Predicates suit validation pipelines; assertions suit invariants failing loud. Both eliminate redundant casts when implemented honestly atop runtime checks rather than blind faith.' },
  { t: 'overload resolution order matters', d: 'Overloads match top-down first-fit \u2014 order specific-before-general or broad signatures shadow precise ones forever. Impossible combos fall through to implementation mismatch errors. Debugging overload complaints starts by logging which signature the call site believes it hits (hover tooltips).' },
  { t: 'generic inference from return position', d: 'TS infers type params from return usage too (Promise-returning factories), though argument-position inference dominates reliability. When inference fails silently producing unwanted widened types, explicit call-site annotations pin intent: createStore<State>() immediately documenting expectations.' },
];
