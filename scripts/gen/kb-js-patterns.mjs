// JavaScript patterns, async mastery & interview classics — part 4
export default [
  { t: 'callback hell and its remedies', d: 'Nested callback pyramids grow when each step needs the previous result: login(user, u => getPosts(u, p => ...)). Remedies: named function hoisting flattening indentation, promise chains linearizing steps, and async/await reading sequentially. Error handling consolidates into single catch versus per-level error callbacks.' },
  { t: 'sequential vs parallel async decisions', d: 'Parallelize independent operations via Promise.all for latency wins; sequence dependent calls where output feeds input or APIs demand ordering/rate limits. Batch strategies chunk large arrays (map to promises, all per chunk) bounding memory and server pressure while preserving throughput.' },
  { t: 'async error propagation', d: 'Thrown errors inside async functions reject their returned promise; awaited rejections bubble like sync exceptions through try/catch. Unawaited async calls orphan errors \u2014 attach .catch immediately or use Promise.allSettled for batch tolerance. Global handlers (unhandledrejection event) catch strays for logging.' },
  { t: 'Promise constructor anti-patterns', d: 'Wrapping already-promise-returning calls in new Promise creates useless nesting; forgetting to call resolve/reject hangs callers forever; throwing inside executor rejects automatically but async executors swallow throws \u2014 keep executors synchronous, wrapping only genuinely callback-based legacy APIs.' },
  { t: 'thenable duck typing', d: 'Any object exposing then(resolve, reject) integrates with await/then machinery regardless of class origin \u2014 enabling lightweight task libraries without full Promise subclassing. Distinguish real promises via instanceof or util.types.isPromise; treat unknown thenables defensively since their resolution timing is contract-only.' },
  { t: 'cancellation patterns pre-AbortController', d: 'Historical cancellation used token flags checked between steps, wrapped promises racing a cancel promise, or generator-based cooperative schedulers (bluebird cancellation). AbortController standardized signal threading for fetch/listeners; custom long computations still need manual checkpoint polling of signal.aborted.' },
  { t: 'rate limiting client requests', d: 'Client-side limiters queue excess requests releasing them on timers/token refills (token bucket), preventing 429 storms against constrained APIs. Implementations track in-flight counts plus waiters array; p-limit library popularizes the pattern. Combine with retry-after respect for robust integrations.' },
  { t: 'request deduplication', d: 'Dedup layers key concurrent identical GETs returning the shared in-flight promise, collapsing N simultaneous mounts into one network hit \u2014 React Query/SWR do this automatically. Manual maps store promise-by-key deleting on settle. Beware caching errors: failing shared promises should clear promptly allowing retries.' },
  { t: 'optimistic updates with rollback', d: 'Optimistic flows apply local changes instantly, fire the request, and revert on failure restoring snapshot taken beforehand; success reconciles with server truth. UI stays snappy for low-risk mutations (likes, toggles). Queue conflicts serialize competing mutations on the same entity.' },
  { t: 'polling strategies', d: 'Polling loops sleep-between-fetch until condition/stopping flag; backoff widens intervals under failure or staleness; visibilitychange pauses background-tab polling saving resources; Page Lifecycle API freezes hidden pages entirely. WebSockets/SSE replace polling for push semantics when infrastructure supports them.' },
  { t: 'Server-Sent Events vs WebSockets', d: 'SSE is unidirectional HTTP streaming with auto-reconnect built-in, text-only, trivial through proxies \u2014 ideal feeds/notifications. WebSockets provide bidirectional binary messaging needing heartbeat/reconnect/queueing layers. Choose SSE when clients merely listen; sockets for chat/collaboration interactivity.' },
  { t: 'WebSocket reconnection discipline', d: 'Robust socket clients implement exponential-backoff reconnect with jitter, message buffering during disconnection, heartbeats detecting half-open connections, and state resync (fetch missed deltas) after restore. Treat the wire as unreliable \u2014 application-level acks confirm critical deliveries.' },
  { t: 'functional programming principles in JS', d: 'FP emphasizes pure functions, immutability, first-class functions, composition over inheritance, and declarative data transformation. JS supports idiomatically via const, spread, map/filter/reduce, closures; libraries add currying/lenses. Benefits concentrate around testability and predictable refactoring rather than dogma.' },
  { t: 'point-free style tradeoffs', d: 'Point-free composes named functions omitting explicit parameters: pipe(filter(isActive), map(getName)). It highlights transformation pipelines but can obfuscate when arity hides \u2014 judiciously mix styles, prioritizing readability. Debugging benefits from occasional tap() side-logging within otherwise pure chains.' },
  { t: 'immutability performance costs', d: 'Structural sharing (persistent data structures) makes immutable updates cheap despite copies; naive deep clones cost O(n) per change. Immer\u2019s proxies copy-on-write only touched paths. Measure before assuming \u2014 small states copy trivially, huge nested stores benefit from normalization (flat entities by id).' },
  { t: 'normalizing nested API state', d: 'Flatten relational payloads into entities-by-id tables plus ordered id arrays (Redux normalize style), eliminating duplication and enabling O(1) updates/lookups. Denormalize at selectors joining views. Prevents cascading immutable-update gymnastics through deeply nested comment threads or org charts.' },
  { t: 'selector memoization patterns', d: 'Selectors derive view models from raw state cached by input references (reselect-style), recomputing only when dependencies change. Cheap reference checks gate expensive joins/sorts. Keep selectors pure and colocated near consumers; parametrized selectors use per-args memo caches keyed appropriately.' },
  { t: 'middleware pattern architecture', d: 'Middleware chains wrap core dispatch/process functions adding cross-cutting concerns \u2014 logging, persistence, async orchestration \u2014 each layer calling next(). Redux/thunk, Express, Koa exemplify. Order matters: earlier middleware wraps later ones. Compose via reduceRight building onion-shaped invocation stacks.' },
  { t: 'dependency injection in JavaScript', d: 'DI passes collaborators through constructors/factories instead of importing globals, unlocking substitution in tests and environment swaps (real vs mock fetch). Container libraries optional \u2014 plain parameter passing often suffices. Module mocking (jest.mock/vi.mock) offers alternative seam when refactor cost is high.' },
  { t: 'strategy pattern via first-class functions', d: 'Instead of strategy class hierarchies, pass functions directly: sort(comparator), pricing(tierFn). Maps of named strategies replace switch statements extensibly: handlers[type]?.(payload) ?? fallback. JS\u2019s function portability dissolves ceremony classical languages need for behavioral variation.' },
  { t: 'observer pattern in practice', d: 'Subjects maintain subscriber lists notified on change; JS realizes it via EventTarget, RxJS streams, or hand-rolled emitters. Contrast with pub/sub\u2019s broker decoupling topics from sources. Unsubscribe hygiene prevents leaks; consider AbortSignal-abortable listeners modernizing cleanup ergonomics.' },
  { t: 'singleton pattern caveats', d: 'Singletons (module-scope instances, static getInstance) simplify access but hide dependencies, complicate testing resets, and smuggle mutable global state across imports. ESM modules are natural singletons anyway. Prefer explicit injection; reserve true singletons for genuine app-wide services (config loaders, telemetry clients).' },
  { t: 'builder pattern for complex objects', d: 'Builders chain configuration before construction: new RequestBuilder().url(u).method("POST").body(b).build(), taming telescoping constructors and centralizing validation/invariants at build time. Immutability-friendly variants return new builders per call. Query-string builders and test-data factories showcase everyday utility.' },
  { t: 'adapter and facade utilities', d: 'Adapters translate incompatible interfaces (normalizing vendor response shapes into internal DTOs); facades simplify subsystem clusters behind one ergonomic entry (api.get wrapping auth+retry+parse). Both isolate third-party churn at boundaries \u2014 swap payment providers by rewriting one adapter, not forty call sites.' },
  { t: 'lazy initialization patterns', d: 'Lazy getters defer expensive construction until first access: function heavy(){ if(!cache) cache = compute(); return cache }. Memoized thunks export pre-computed promises kicking work early without blocking import. Lazy route/component loading extends the idea to code itself via dynamic import.' },
  { t: 'feature detection vs user-agent sniffing', d: 'Feature detection ("IntersectionObserver" in window) branches on actual capabilities, surviving new browsers and lying UAs; UA parsing breaks perpetually and should inform analytics only. Modern CSS @supports mirrors this philosophy declaratively. Progressive enhancement layers experiences atop detected baselines.' },
  { t: 'polyfill strategy and core-js costs', d: 'Polyfills patch missing natives conditionally (if (!Array.at) ...). Bundlers inject targeted polyfills from browserslist targets \u2014 aggressive legacy support inflates bundles dramatically (core-js). Audit usage: drop IE-era targets, prefer transpile-only where possible, load polyfills asynchronously non-blocking when optional.' },
  { t: 'transpilation vs polyfilling distinction', d: 'Transpilers rewrite new syntax to old equivalents (arrow\u2192function); polyfills supply missing runtime APIs (Promise). Babel handles syntax via plugins, runtime methods via core-js preset-env integration. Missing either piece breaks differently \u2014 syntax errors parse-fail loudly, absent APIs fail mysteriously at runtime.' },
  { t: 'tree shaking requirements', d: 'Tree shaking removes unused exports only with ESM static structure, sideEffects:false declarations, and import paths avoiding namespace grabs (import * as). CommonJS defeats it dynamically. Verify via bundle analyzers; barrel files exporting everything can accidentally retain modules through side-effect ambiguity.' },
  { t: 'barrel files tradeoffs', d: 'index.ts re-export barrels shorten import paths but can create circular-import hazards, slow cold builds, and tree-shaking friction when mixed with side effects. Large monorepos increasingly favor direct module imports. Keep barrels thin, side-effect-free, or skip them where tooling suffers.' },
  { t: 'circular dependency handling', d: 'ESM tolerates cycles via live bindings \u2014 accessing not-yet-initialized bindings during evaluation throws TDZ errors, making cycle direction matter. Break cycles extracting shared logic into leaf modules, inverting dependencies via callbacks/events, or deferring requires lazily inside functions. Circular imports usually signal design smell.' },
  { t: 'live bindings meaning', d: 'Exported let counters update in importers because ESM exports bind names, not snapshot values \u2014 reading importedVar reflects latest assignment. CommonJS copies primitive exports at require time instead. Live bindings enable observable module state but surprise developers expecting frozen constants; prefer const exports for clarity.' },
  { t: 'side effect imports', d: 'import "./styles.css" or import "./registerHooks" executes modules purely for effects without bindings. Bundlers honor them verbatim even under sideEffects flags listing them explicitly. Ordering matters (CSS cascade, polyfill registration) \u2014 document sequencing assumptions where fragile.' },
  { t: 'default vs named export guidance', d: 'Named exports tree-shake, rename safely, and autocomplete reliably; default exports allow arbitrary importer naming hurting grep-ability and causing inconsistent casing drift. Communities diverge (React default, lodash named). Pick team-consistent policy; components commonly default while utilities lean named.' },
  { t: 're-export aggregation patterns', d: 'export * from "./mod" forwards everything except default; explicit export {} lists control surface deliberately. Namespaced re-exports (import * as Utils) risk bundler inclusion of whole namespaces. Public package entry points (exports field) hide internals, stabilizing API while refactoring freely internally.' },
  { t: 'typeof guards for optional globals', d: 'Safely probing possibly-absent globals uses typeof window !== "undefined" (no ReferenceError unlike direct reference), feature-in operators, or optional chaining window?.crypto?.randomUUID. SSR/isomorphic code branches environment capabilities this way, keeping single bundles safe across node/browser execution.' },
  { t: 'isomorphic/universal code considerations', d: 'Universal code runs both server and client: guard DOM/BOM access, avoid window-scoped caches leaking cross-request state, use platform-agnostic fetch (undici/node18+), and handle env config differences. Hydration expects deterministic renders \u2014 randomness/time must serialize identically or gate behind effects.' },
  { t: 'hydration mismatch debugging', d: 'Mismatches arise from Date.now/random rendering, locale-dependent formatting, invalid HTML nesting (p>div auto-correction), or conditional browser extensions mutating markup pre-hydration. Suppress via useEffect-time client-only values, suppressHydrationWarning sparingly, and validate server HTML determinism in CI snapshots.' },
  { t: 'stack safety with trampolines', d: 'Trampolining converts tail recursion into loops: recursive fn returns thunk; driver loop invokes until non-thunk result \u2014 eliminating stack growth for deep recursions on engines lacking TCO (all majors effectively). Useful for interpreters/parsers processing adversarially deep inputs.' },
  { t: 'continuation-passing style basics', d: 'CPS passes "what happens next" as continuation argument instead of returning \u2014 the conceptual basis of callbacks and how compilers transform async. Understanding CPS demystifies promise chaining as CPS sugar and clarifies why early returns cannot escape asynchronous boundaries.' },
  { t: 'defensive copying at boundaries', d: 'Freeze/copy inputs entering your module (structuredClone or shallow+freeze) so external mutations cannot corrupt internal invariants mid-operation; equally return copies protecting internal state from caller mutation. Costs are real \u2014 reserve for security/trust boundaries and public library surfaces.' },
  { t: 'input validation patterns', d: 'Validate at trust boundaries (API responses, form submits, URL params) via schema libraries (zod/yup) producing parsed typed outputs, failing fast with actionable messages. Never trust types alone at runtime \u2014 JSON erases them. Centralize validators beside types keeping single source of truth.' },
  { t: 'error messages that help debugging', d: 'Good errors state what failed, expected vs received (JSON.stringify diffs), and remediation hints; include context ids and operation names. Wrap low-level failures adding domain context while preserving cause chains. Consistent error taxonomy enables programmatic handling beyond string matching.' },
  { t: 'logging strategy front-end', d: 'Structured logging (level, msg, context object) routed through buffered transport with sampling controls noise/cost; never log PII/secrets/tokens. Correlate sessions via ids; ship console.error parity for dev DX. Gate verbose levels behind debug flags toggled via query params/localStorage.' },
  { t: 'debugging techniques toolbox', d: 'Beyond console.log: breakpoints/conditional breakpoints, watch expressions, black-boxed framework files, async stack traces, logpoints (non-breaking logs), performance profiler flame charts, memory heap comparisons, network throttling, and override/local overrides. Mastering debugger beats print-loop iteration speed-wise on gnarly bugs.' },
  { t: 'console API beyond log', d: 'console.table(arrayOfObjects) tabulates collections; group/groupEnd nests output; time/timeEnd measures durations inline; assert logs on falsy; dir inspects DOM/objects deeply; trace prints stack. Counting occurrences via count(label) aids loop sanity checks \u2014 all devtools-only, strip or gate in production.' },
  { t: 'source maps fundamentals', d: 'Source maps map minified positions back to originals enabling readable stack traces; generate in build, upload privately to error trackers, avoid shipping publicly (exposes source). Browser devtools load linked .map automatically. Version-pin maps to exact deploys or traces misalign post-release.' },
  { t: 'unit testing pure logic', d: 'Pure functions test trivially \u2014 arrange inputs, assert outputs including edges (empty, huge, unicode, NaN). Table-driven tests iterate case matrices compactly. Extract impurity (time/random/io) behind injectable seams converting integration pain into unit-testable cores. Aim tests documenting behavior, not implementation trivia.' },
  { t: 'mocking time in tests', d: 'Fake timers (vi.useFakeTimers/jest) control setTimeout/Date deterministically, advancing virtually to test debounce/throttle/expiry without real waits. Restore after each test preventing bleed. For date-sensitive logic prefer injecting clock/clockNow params \u2014 simpler than global fakes and immune to timer-library drift.' },
  { t: 'testing async code', d: 'Await promises in assertions (test frameworks fail silently on floating assertions), flush microtask queues with awaits between steps, fake network via msw interceptors asserting request shapes, and test rejection paths explicitly with expect(...).rejects. Race-prone flakiness signals underspecified synchronization, fix scheduling rather than adding sleeps.' },
  { t: 'property-based testing introduction', d: 'Property-based frameworks generate randomized inputs asserting invariants (roundtrip encode-decode equals identity; sorted output is monotonic; parser accepts serializer output). Shrinking minimizes failing cases automatically. Complements example tests catching edge classes humans miss: empty strings, unicode, negative zeros.' },
  { t: 'code coverage honest interpretation', d: 'Coverage measures executed lines, not asserted behavior \u2014 100% achievable with assertion-free runs. Use thresholds as tripwires against drops, review uncovered branches manually, prioritize mutation-testing spot checks for critical algorithms. Chase meaningful scenarios over metric vanity; untested glue code still ships bugs.' },
  { t: 'semver and lockfiles discipline', d: 'Semantic versioning communicates breaking(^major)/minor(~patch) intent; lockfiles pin exact trees ensuring reproducible CI/dev parity \u2014 commit them always. Automated dependency bots batch upgrades with changelog reviews; audit tools flag CVEs demanding prompt triage. Caret ranges plus committed locks balance freshness and stability.' },
  { t: 'environment variables handling', d: 'Bundlers inline import.meta.env/VITE_* prefixed vars at build time \u2014 secrets leak into shipped JS, so backend proxies hold private keys. Runtime-configurable settings belong in served /config.json fetched at boot. Validate required vars at startup failing fast with clear messages rather than undefined-propagating mysteries.' },
  { t: 'feature flags implementation', d: 'Flags decouple deploy from release: evaluate flags early (boot), expose via context/hook, clean up dead flags routinely. Percent-rollouts plus targeting segments de-risk launches; kill-switches retire features instantly during incidents. Avoid flag explosion \u2014 debt compounds faster than features.' },
  { t: 'A/B testing instrumentation basics', d: 'Experiments hash stable user ids into buckets ensuring consistent variants; instrument conversion metrics alongside variant exposure events; guard flicker by resolving assignments pre-render. Statistical significance needs adequate sample/duration planning. Never let experiment scripts block critical rendering paths.' },
  { t: 'analytics event naming conventions', d: 'Consistent schemas (object_action tense-free snake_case) keep dashboards queryable: checkout_completed, filter_applied. Enrich events with canonical properties (page, component_id) while respecting privacy consent gates. Version taxonomies centrally \u2014 ad-hoc strings rot into unqueryable soup quickly.' },
  { t: 'progressive enhancement examples', d: 'Baseline HTML forms/links work without JS; enhancement layers add autocomplete, optimistic UI, offline. Compare with SPA-only fragility under CDN hiccups or strict CSP. Framework apps adopt pragmatically: server-render shells, hydrate interactivity, ensure noscript paths for critical journeys where feasible.' },
  { t: 'graceful degradation definition', d: 'Graceful degradation builds for modern capability first then provides fallbacks downward; progressive enhancement ascends oppositely from baseline. Practical middle ground: feature-detect enhancements, provide usable fallback content (image in canvas absence), communicate constraints honestly rather than blank screens.' },
  { t: 'browser storage quota management', d: 'Storage APIs estimate quotas (navigator.storage.estimate) varying wildly by origin/browser; localStorage fills throw QuotaExceededError requiring eviction UX. Persist important data via IndexedDB plus StorageManager.persist requesting exemption from automatic eviction under pressure. Sync-critical subsets stay small and serialized efficiently.' },
  { t: 'offline-first architecture overview', d: 'Offline-first treats connectivity as enhancement: local database (IndexedDB) is source of truth, sync queues mutations replaying with conflict policies (last-write-wins, CRDTs for merges), UI reflects sync status honestly. Service workers cache app shell; background sync retries queued sends opportunistically.' },
  { t: 'service worker lifecycle', d: 'SWs install (precache), wait until old clients close unless skipWaiting forces activation, controlling page only after claim/reload \u2014 versioned caches enable atomic swaps. Update flows detect registration.update, activate new SW, message clients to reload. Misconfigured lifecycles cause infamous stale-asset ghosts.' },
  { t: 'cache strategies in service workers', d: 'Strategies match resource temperament: cache-first for hashed assets (immutable), network-first for HTML/API freshness, stale-while-revalidate balancing speed/update, cache-only for offline shells. Workbox expresses these declaratively with expiration/broadcast plugins. Wrong strategy manifests as stuck old UI or needless data churn.' },
  { t: 'push notifications flow overview', d: 'Permission prompt precedes subscription (PushManager) storing endpoint server-side; server posts encrypted payloads via VAPID keys; SW push event wakes showing notification requiring user gesture navigation. iOS support arrived gated to installed PWAs. Respect permission denials permanently \u2014 nagging erodes trust and OS may block.' },
  { t: 'web share and file system APIs', d: 'navigator.share invokes native share sheets for links/files/text on supporting platforms, replacing bespoke social buttons; File System Access API opens/saves real files via showOpenFilePicker with handles persisted in IndexedDB. Feature-detect both \u2014 desktop Safari/Firefox gaps demand download-attribute fallbacks.' },
  { t: 'web components overview', d: 'Custom elements, shadow DOM, and templates compose framework-agnostic encapsulated widgets with native lifecycle hooks (connectedCallback etc.). Shadow DOM scopes styles but complicates global theming and e2e querying. Adoption thrives in design-system distribution contexts; app teams often prefer framework-native components for DX.' },
  { t: 'shadow DOM styling implications', d: 'Shadow boundaries isolate styles both ways: outer CSS misses inner elements; inheritable properties (color, font) pierce through. CSS custom properties and ::part() provide sanctioned theming channels. Constructable stylesheets enable shared performant style reuse across many shadow roots.' },
  { t: 'template literals for mini-DSLs', d: 'Tags transform raw pieces enabling embedded languages: sql`SELECT * WHERE id=${id}` auto-parameterizes preventing injection; html`` escapes interpolations; css`` compiles styles. Libraries intercept values positionally \u2014 understand strings-array interleaving to author safe DSL tags with proper escaping.' },
  { t: 'internationalization string externalization', d: 'Externalize user-facing strings into locale catalogs keyed semantically (checkout.submit.button) via ICU MessageFormat handling plurals/gender/select. Avoid concatenating translated fragments \u2014 grammar order varies. Pseudo-localization testing exposes hardcoded strings early; RTL locales flip layouts via logical CSS properties.' },
  { t: 'locale-aware formatting recap', d: 'Intl covers numbers (units, currency, notation compact), dates (calendars, time zones, relative time via RelativeTimeFormat), list conjunctions, plural rules, and collation. Cache formatters per locale; format at render boundaries not storage. Testing spans locales matrix \u2014 snapshot representative set including RTL and CJK.' },
  { t: 'timezone correctness rules', d: 'Store UTC instants (ISO strings) plus optional IANA zone metadata; convert only for display using Intl/zoned-time libs. Arithmetic across DST transitions shifts wall-clock unexpectedly \u2014 add days via calendar-aware APIs, not ms math. Never infer zones from offsets alone (ambiguity during fall-back).' },
  { t: 'encoding essentials UTF-16', d: 'Strings are sequences of UTF-16 code units; astral chars pair surrogates affecting length/index/regex (fixed by u flag/code points). Escape sequences \\u{1F600} target code points with u flag. Normalization (NFC/NFD) matters comparing user text \u2014 composed accents differ byte-wise yet visually identical.' },
  { t: 'URL encoding pitfalls', d: 'encodeURIComponent escapes component-safe (slashes become %2F) versus encodeURI preserving structural characters \u2014 mixing them corrupts URLs. Plus signs mean space only in form-encoding contexts. URLSearchParams encodes correctly per spec; hand-rolling invites broken queries containing ampersands/unicode emoji.' },
];
