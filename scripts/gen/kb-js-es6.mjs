// JavaScript ES6+ / modern language features — part 2
export default [
  { t: 'let in for loops', d: 'let creates a per-iteration binding, so closures inside the loop body capture distinct copies of the counter \u2014 fixing the classic setTimeout-printing-5 bug without IIFEs. The update expression runs between iterations copying into the fresh binding, which is why break/continue semantics stay intuitive.' },
  { t: 'const objects and mutation', d: 'const freezes the binding, not the value: properties of a const object can be added/deleted/changed freely; only reassignment throws. To achieve real immutability combine const with Object.freeze (shallow) or recursive deep-freeze. This distinction is a favorite trick question because intuition says const means constant.' },
  { t: 'object shorthand and computed properties', d: 'ES6 lets { name } stand in for { name: name }, and computed keys [expr] evaluate dynamic property names at literal creation: { [`${prefix}Id`]: 1 }. Together they slim config/factory code dramatically. Computed keys cannot appear in destructuring patterns with variables on the left.' },
  { t: 'method shorthand syntax', d: 'Object literals allow methods without the function keyword: { greet() {} }. These create regular functions (dynamic this), unlike arrow-property shorthand { greet: () => {} } whose this stays lexical \u2014 mixing them up breaks event handlers expecting instance context.' },
  { t: 'spread vs rest confusion', d: 'Same three dots, opposite directions: spread expands an iterable/object where values are consumed (calls, literals); rest collects remaining values into arrays/objects (params, destructuring). Position disambiguates: leading dots before a name in a parameter list mean gather; inline in arguments/literals mean expand.' },
  { t: 'nested destructuring patterns', d: 'Destructuring supports arbitrarily nested paths mixing arrays and objects: const { meta: { tags: [first] } = [] } = payload. Defaults apply per-level, so guarding intermediate undefined nodes needs defaults at each nesting step. Overly clever one-line extraction hurts readability more than it saves typing.' },
  { t: 'array destructuring tricks', d: 'Skip holes to ignore elements: const [, second] = arr. Swap without temp: [a, b] = [b, a]. Collect tails with rest: const [head, ...tail] = arr. Defaults pair with undefined only. These idioms replace verbose index juggling and read like pattern-matching from functional languages.' },
  { t: 'string padding and trimming methods', d: 'padStart/padEnd pad to target length with optional filler (default spaces) \u2014 perfect for aligning output and zero-filling ids. trimStart/trimEnd (plus alias trimLeft/right) remove edge whitespace. All return new strings since strings are immutable. repeat(n) builds repeated segments cheaply.' },
  { t: 'startsWith, endsWith and includes', d: 'Modern string predicates replace brittle indexOf(...)===0 checks with readable startsWith/endsWith/includes, all accepting optional position offsets and handling empty search strings predictably. They are case-sensitive; normalize case first when needed. Regex remains the tool for positional or patterned matching.' },
  { t: 'codePointAt and unicode awareness', d: 'JS strings are UTF-16 units, so astral symbols (emoji) span two code units, breaking length, indexing, and naive iteration. codePointAt/fromCodePoint handle full code points; spreading or [...str] iterates code points correctly; u-flag regexes match astral characters. Intl.Segmenter goes further for grapheme clusters like flag emoji.' },
  { t: 'exponentiation operator', d: '** raises numbers right-associatively: 2 ** 3 ** 2 equals 512 because the right side evaluates first. It mirrors Math.pow but accepts BigInt operands too. Unary minus binds tighter (**), so -2 ** 2 is a SyntaxError requiring parentheses \u2014 deliberate ambiguity avoidance.' },
  { t: 'numeric separators', d: 'Underscores improve numeric readability: 1_000_000, 0xFF_FF, 1_000n. They are purely syntactic sugar stripped during parsing, cannot lead/trail or sit adjacent to dots, and work in all numeric literal forms including BigInt. Ideal for constants like timeouts and magic thresholds.' },
  { t: 'Promise.all behavior', d: 'Promise.all resolves with ordered array of fulfilled values or rejects immediately with the first rejection (fail-fast). Non-promise inputs pass through resolved. Use it when every result matters and partial failure invalidates the batch; remember input order maps to output order regardless of settle order.' },
  { t: 'Promise.allSettled', d: 'allSettled waits for every promise, resolving with status/value|reason descriptors so partial failures become data instead of exceptions. Perfect for dashboard fan-outs where independent widgets render what succeeded and show retry states for failures. Never rejects itself (unless input is not iterable).' },
  { t: 'Promise.race and timeout patterns', d: 'race settles with whichever promise settles first \u2014 success or failure. Classic uses: timeout wrappers racing fetch against a reject-after timer, and first-responsive-mirror selection. Since losers keep running, attach abort signals to actually cancel losing work instead of merely ignoring it.' },
  { t: 'Promise.any', d: 'any resolves with the first fulfillment, rejecting only when everything fails with an AggregateError containing individual errors array. It differs from race by ignoring early rejections \u2014 ideal for multi-CDN fallbacks and fastest-success queries. Empty iterable rejects with AggregateError immediately.' },
  { t: 'combining promises with thenable interop', d: 'Promises flatten any thenable: returning custom { then(resolve,reject) } inside async flows integrates legacy deferred libraries seamlessly. Beware malicious/circular thenables causing infinite resolution loops \u2014 the spec guards via called-once enforcement. Promise.resolve(obj) adopts existing promises rather than wrapping twice.' },
  { t: 'async iteration and for await...of', d: 'Async iterables implement Symbol.asyncIterator; for await...of consumes streams page-by-page, pausing between chunks without callback pyramids. Async generators (async function*) produce them naturally, ideal for paginated APIs and event streams. Errors propagate like sync loops; breaking calls iterator.return cleanup.' },
  { t: 'async generators', d: 'async function* yields promises consumed sequentially by for-await loops, enabling lazy pipelines over time-based or network sources: reading NDJSON lines, polling until condition, consuming SSE. Each next() returns a promise; combining yield with await interleaves production and IO naturally.' },
  { t: 'top-level await', d: 'Modules may await directly at top level, simplifying config loading and WASM instantiation bootstraps. The module\u2019s evaluation blocks importers \u2014 powerful yet risky for circular deps or startup latency, so prefer lazy dynamic imports when delay matters. Only ESM supports it; CommonJS does not.' },
  { t: 'import.meta', d: 'import.meta exposes module-scoped metadata: url (file location), resolve(specifier) for URL resolution, plus bundler extensions like env in Vite. Unlike process/env globals it works in browsers natively. Useful for asset path building, feature flags injection, and worker construction relative to module URL.' },
  { t: 'Proxy', d: 'Proxy wraps targets intercepting fundamental operations (get, set, has, deleteProperty, apply...) through handler traps, powering validation layers, reactive frameworks (Vue 3), negative array indices shims, and logging. Revocable proxies sever access later. Traps must forward invariants or throw TypeError, keeping engine guarantees intact.' },
  { t: 'Reflect', d: 'Reflect mirrors Object/operation APIs as functions returning explicit results instead of throwing, pairing with Proxy traps for correct default forwarding: Reflect.get(target, key, receiver). It makes metaprogramming uniform and avoids awkward try/catch around defineProperty-style operations when testing capabilities.' },
  { t: 'WeakRef and FinalizationRegistry', d: 'WeakRef holds a weak reference queried via deref(), letting caches observe liveness without pinning memory; FinalizationRegistry schedules callbacks after collection. Both are advanced GC-cooperative tools with no guarantees about timing \u2014 never rely on finalizers for correctness, only opportunistic cleanup like cache pruning.' },
  { t: 'Array.prototype.at', d: 'at(index) accepts negative indices counting from the end: arr.at(-1) replaces arr[arr.length-1]. Works on strings and typed arrays too. Returns undefined for out-of-range rather than throwing, matching indexOf-style tolerance. Small addition, big readability win for last-element access everywhere.' },
  { t: 'Object.hasOwn', d: 'Object.hasOwn(obj, key) is the robust replacement for obj.hasOwnProperty() calls that break when objects lack the method or shadow it (Object.create(null), JSON-parsed weirdness). Static invocation avoids prototype dependence entirely, checking own enumerable and non-enumerable properties alike.' },
  { t: 'replaceAll', d: 'replaceAll(search, replacement) swaps every occurrence without the g-flag ceremony, accepting strings (literal, not regex semantics) or global regexes \u2014 non-global regex throws deliberately to prevent accidental single-replace bugs. Replacement patterns like $& still apply. Cleaner than split/join hacks.' },
  { t: 'matchAll for global regex results', d: 'matchAll(/pattern/g) returns an iterator of match objects including named groups and indices, unlike match with g flag which returns plain substrings only. Because it clones regex state, lastIndex pollution disappears. Loop with for...of or spread into arrays for structured scraping tasks.' },
  { t: 'Promise error cause chaining', d: 'new Error(msg, { cause: originalErr }) attaches root causes without subclass gymnastics, surfacing full chains in logs: err.cause.cause... Standardize catch-and-rewrap boundaries (network layer wrapping low-level fetch errors with context while preserving cause for debugging depth).' },
  { t: 'toSorted, toReversed, toSpliced, with', d: 'ES2023 added change-returning twins: toSorted/toReversed/toSpliced copy then mutate the copy, and with(index, value) returns a new element-replaced array \u2014 completing immutable update ergonomics alongside sort/splice/reverse. Great fit for React state updates avoiding manual slice choreography.' },
  { t: 'Object.groupBy and Map.groupBy', d: 'Grouping helpers partition collections by callback key: Object.groupBy(users, u => u.role) returns null-prototype object; Map.groupBy allows non-string keys. They retire hand-rolled reduce grouping boilerplate and pair well with flatMap for multi-key membership scenarios.' },
  { t: 'structuredClone vs JSON round trip', d: 'JSON.parse(JSON.stringify(x)) mangles Dates to strings, drops undefined/functions/Symbol keys, converts NaN to null, and explodes on cycles. structuredClone preserves types, cycles, Maps/Sets/typed arrays, failing loudly on functions instead of silently corrupting. Default to structuredClone; fall back to JSON only for wire-format shaping.' },
  { t: 'globalThis', d: 'globalThis uniformly references the global object across browsers (window/self), workers (self), Node (globalThis), and unknown environments, replacing fragile typeof window !== "undefined" chains. Code targeting multiple runtimes should bind library roots to globalThis and detect environment capabilities via feature checks instead.' },
  { t: 'BigInt essentials', d: 'BigInt represents arbitrary-precision integers via suffix n or BigInt(); arithmetic requires both operands BigInt (mixing Number throws), division truncates toward zero, and Math methods do not accept it. JSON needs manual string serialization. Perfect for crypto tokens, 64-bit ids, and exact integer math beyond 2^53.' },
  { t: 'typed arrays overview', d: 'Typed arrays (Int8Array...Float64Array) view raw ArrayBuffer bytes providing fixed-type, fixed-length numeric access with near-native performance \u2014 essential for WebGL, audio processing, binary protocols, and file parsing. DataView adds mixed-endianness unaligned reads. They share Array methods partially; sort is numeric-aware within type bounds.' },
  { t: 'SharedArrayBuffer and Atomics', d: 'SharedArrayBuffer shares raw bytes across threads; Atomics supplies synchronized read-modify-write ops (add, compareExchange, wait/notify) preventing torn reads and enabling worker coordination. Browsers require cross-origin isolation headers (COOP/COEP) due to Spectre mitigations. Use for parallel number crunching, not general app state.' },
  { t: 'optional catch binding', d: 'catch {} omits the unused error parameter when the fact of failure suffices: try { JSON.parse(s) } catch { return fallback }. Reduces lint noise and accidental variable shadowing. Pair with intentional swallowing comments explaining why ignoring is safe \u2014 silent catches remain a top debugging hazard.' },
  { t: 'trailing commas everywhere', d: 'ES2017 allows trailing commas in function parameter lists and calls; ES2022 in class/method bodies. Consistent trailing commas minimize diff noise and reorder-safety in version control. JSON forbids them, so watch config-vs-code drift. Formatters enforce style automatically now, ending most debates.' },
  { t: 'async function return value rules', d: 'An async function always returns a promise; returned values get wrapped, thrown errors reject, and returning a promise awaits it transparently. Therefore calling .then twice on the same async result sees identical data, and forgetting await inside another async call silently produces unresolved promise bugs \u2014 enable no-floating-promises linting.' },
  { t: 'await inside loops pitfalls', d: 'Sequential awaits in forEach callbacks break entirely (forEach ignores promises), and for-loop awaits serialize independent requests multiplying latency. Parallelize with Promise.all(map(fetch)), or use for-await-of when order/dependency demands sequence. Measure batches: N sequential 100ms calls cost N*100ms versus ~100ms parallel.' },
  { t: 'queueMicrotask', d: 'queueMicrotask schedules a callback onto the microtask queue directly \u2014 earlier than setTimeout macrotasks, after current synchronous code \u2014 useful for deferring to post-sync phase while keeping ordering deterministic relative to promise reactions. Cheaper than setTimeout(0) and immune to clamp delays.' },
  { t: 'Symbol.iterator customization example', d: 'Implementing Symbol.iterator on your class unlocks for-of, spread, and destructuring: *[Symbol.iterator]() { yield* this.items } using generator shorthand. Range classes, tree walkers, matrix rows benefit. Keep iteration stable under concurrent mutation or document snapshot semantics to avoid surprising consumers.' },
  { t: 'well-known Symbol.toPrimitive', d: 'Defining Symbol.toPrimitive(hint) controls object conversion for string/number/default hints, overriding valueOf/toString ordering. Libraries use it for money/date formatting inside template literals and arithmetic. Returning primitives keeps operators predictable; returning objects triggers TypeError, enforcing correctness.' },
  { t: 'static initialization blocks', d: 'Static blocks run once during class evaluation with access to private static state, enabling complex setup impossible in field initializers (try/catch around external resources, conditional registration). Multiple blocks execute in declaration order interleaved with static field initializers. Errors abort class definition entirely.' },
  { t: 'private field presence check', d: '#field in obj (ergonomic brand check) tests private membership safely outside the class via static helper methods, enabling duck-typing with hard guarantees: static isFoo(x) { return #secretField in x }. Previously required try/catch around access. Works only within the declaring class lexical scope.' },
  { t: 'getter/setter in class inheritance', d: 'Overriding only a getter while parent defines setter (or vice versa) silently drops half the accessor pair unless you redefine both \u2014 accessors live on prototypes as units. super.x reads route through parent getters. Document paired accessors and prefer fields + explicit methods when asymmetric logic appears.' },
  { t: 'class expression and mixin patterns', d: 'Classes are expressions too, enabling anonymous subclasses and mixins: const Mixin = Base => class extends Base { ... }; class Foo extends Mixin(Bar). Compose behaviors horizontally avoiding diamond problems of multiple inheritance. Keep mixins state-light and interface-narrow to prevent hidden coupling.' },
];
