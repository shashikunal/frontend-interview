// CSS topics
export default [
  { t: 'the CSS cascade', d: 'The cascade resolves competing declarations by origin/importance, layer, specificity, source order, and inheritance. Later same-specificity rules win; !important inverts author order within its origin. Cascade layers (@layer) now insert an explicit precedence tier between importance and specificity for architecture-level control.' },
  { t: 'specificity calculation', d: 'Specificity counts (inline, id, class/attribute/pseudo-class, element/pseudo-element) as tuples compared left-to-right; universal selector adds nothing. 0-2-1 beats 0-1-9. :where() contributes zero keeping defaults overridable; :is() adopts its most specific argument \u2014 a common surprise.' },
  { t: 'inheritance in CSS', d: 'Inherited properties (color, font, line-height, text-align) flow parent-to-child unless overridden; box-model and layout properties do not inherit. inherit/initial/unset/revert keywords control the behavior explicitly per property. Design systems exploit inheritance by setting typography once on body.' },
  { t: 'initial vs unset vs revert', d: 'initial forces the spec default even where inherited normally; unset means inherit if naturally inherited else initial; revert rolls back to user-agent/browser style bypassing author rules entirely. These keywords give surgical reset semantics beyond blanket resets.' },
  { t: 'the box model', d: 'Every element renders as content + padding + border + margin. content-box (default historically) sizes width to content excluding padding/border causing arithmetic surprises; border-box includes them making intuitive sizing universal \u2014 hence the global * { box-sizing: border-box } convention.' },
  { t: 'box-sizing border-box rationale', d: 'border-box makes width include padding+border so columns add up predictably inside grids/flex layouts. Legacy projects mix modes accidentally when partial resets apply. Declare it universally early; size exceptions explicitly with content-box where intrinsic math is genuinely wanted.' },
  { t: 'margin collapsing', d: 'Adjacent vertical margins between block siblings collapse to the larger value; parent-child first/last margins escape outward unless padding/border/overflow/BFC intervenes. Collapsing never happens horizontally, across flex/grid containers, or through elements establishing new formatting contexts.' },
  { t: 'percentage margins and padding', d: 'Vertical AND horizontal percentage margins/paddings resolve against the containing block\u2019s WIDTH \u2014 a vertical padding of 50% creates aspect-ratio boxes, a classic trick pre-aspect-ratio. Confusing vertical-percentage-height intuition causes layout bugs worth internalizing.' },
  { t: 'em vs rem units', d: 'em scales from element\u2019s own font-size compounding through nesting \u2014 ideal component-relative spacing; rem anchors to root font-size giving predictable scaling and honoring user browser zoom preferences. Accessibility guidance prefers rem for type so user font preferences propagate.' },
  { t: 'viewport units vw vh dvh', d: 'vw/vh track viewport ignoring scrollbars/mobile chrome; dvh adjusts dynamically as mobile toolbars collapse fixing the classic 100vh-jump issue; svh/lvh pin smallest/largest states. Use dvh for mobile full-screen sections; beware horizontal overflow via 100vw including scrollbar width.' },
  { t: 'min max clamp functions', d: 'min()/max() pick extremes; clamp(min, preferred, max) bounds fluid values \u2014 clamp(1rem, 0.5rem + 2vw, 2rem) yields fluid type without media queries. Math functions compose with calc() enabling design-token-driven responsive systems declaratively.' },
  { t: 'fluid typography technique', d: 'Scale text smoothly between breakpoints using clamp with viewport-proportional preferred value; compute slopes from desired min/max sizes at chosen viewports (utopia.fyi generators popularized this). Pair with rem bounds preserving accessibility zoom beyond fluid range.' },
  { t: 'flexbox container properties', d: 'display:flex enables flex context; flex-direction sets main axis; justify-content distributes along main axis; align-items cross-axis; gap replaces child margin hacks; flex-wrap allows multi-line; align-content positions wrapped lines. Container choices precede item-level tuning in debugging order.' },
  { t: 'flex-grow shrink basis mechanics', d: 'flex-basis sets starting main-size (auto = content); free space distributes by grow ratios, negative space shrinks by shrink*ratio weighted by basis. Default flex:0 1 auto explains squishy buttons; flex:1 shorthand means 1 1 0% equalizing columns regardless of content.' },
  { t: 'flex shorthand gotcha', d: 'Single-number flex:1 expands to 1 1 0% \u2014 basis zero ignores content width distributing purely proportionally; flex:auto would be 1 1 auto respecting content. Choosing wrong one produces mysteriously unequal columns when contents differ wildly.' },
  { t: 'align-self and order', d: 'align-self overrides container alignment per item enabling mixed baselines; order reorders visually without DOM changes \u2014 powerful yet screen-reader/DOM-order divergence hazard. Prefer source-order solutions; use order only for genuinely presentational rearrangement.' },
  { t: 'gap in flex vs margins', d: 'Gap applies uniformly between items only (no leading/trailing), simplifying spacing math and wrap edges versus nth-child margin removal hacks. Row-gap/column-gap split axes independently. Browser support matured everywhere modern \u2014 legacy-only codebases retain margin patterns.' },
  { t: 'grid template areas', d: 'grid-template-areas names regions laid out ASCII-art style: "header header" "sidebar main"; items place via grid-area: header. Reflowing layouts becomes renaming strings \u2014 unmatched area shapes invalidate the declaration silently, so validate carefully during refactors.' },
  { t: 'implicit vs explicit grid tracks', d: 'Explicit tracks come from grid-template-columns/rows; overflowing auto-placed items create implicit tracks sized by grid-auto-rows/columns. Auto-placement fills rows first by default (row dense packs holes). Understanding placement algorithm demystifies mysterious empty gaps.' },
  { t: 'auto-fill vs auto-fit', d: 'repeat(auto-fill, minmax(200px, 1fr)) creates as many tracks as fit leaving empties; auto-fit collapses empty tracks letting items stretch full-width responsively \u2014 card grids want fill (stable columns), hero lists want fit. One word changes entire responsive behavior.' },
  { t: 'minmax and fr distribution', d: 'minmax(min, max) bounds track sizing; fr distributes leftover space after fixed/min-content demands \u2014 not pure fractions of total. minmax(0,1fr) rescues overflowing children (min-content floor defeated) in equal-column layouts containing wide tables.' },
  { t: 'grid item placement lines', d: 'Items position via grid-line numbers (grid-column: 1 / 3), spans (span 2), or named lines [-main-start]. Overlapping placements create layered cells without z-index ceremony. Line-based control powers editorial/magazine layouts impossible in flexbox.' },
  { t: 'subgrid', d: 'subgrid lets nested grids inherit parent tracks aligning card internals across siblings \u2014 solving the classic uneven-card-footer alignment elegantly. Apply on grid-template-columns/rows: subgrid. Support matured in all evergreen browsers finally enabling robust component-level grid composition.' },
  { t: 'masonry layout status', d: 'Native masonry (grid-template-rows: masonry / grid-auto-flow: dense workarounds / columns CSS multicol) trades exist: multicol orders column-wise breaking reading order; dense packing reorders visually. JS libraries (masonry.js) remain pragmatic until native masonry ships broadly.' },
  { t: 'position sticky behavior', d: 'Sticky elements behave relative until crossing a threshold within nearest scrolling ancestor, then pin \u2014 requiring top/right/etc offset and an ancestor tall enough to travel within. Failures stem from overflow:hidden ancestors creating clipping scroll containers unknowingly.' },
  { t: 'absolute positioning containing blocks', d: 'Absolute elements position against nearest positioned ancestor (relative/absolute/fixed/sticky) \u2014 or initial containing block if none. transform/filter/will-change ancestors also become containing blocks surprising developers animating parents of absolutely-positioned children.' },
  { t: 'position fixed quirks', d: 'Fixed pins to viewport except under transformed/filtered ancestors (containing-block capture) and behaves oddly with virtual keyboards on mobile. Backdrop-filter ancestors similarly trap fixed descendants. Debug ghost-fixed bugs by auditing ancestor filter/transform stacks first.' },
  { t: 'stacking contexts', d: 'Stacking contexts form on positioned+z-index, opacity<1, transforms, filters, isolation, will-change, and more; z-index compares only within the same context \u2014 explaining why raising z-index fails mysteriously. isolation:isolate deliberately fences contexts preventing external bleed.' },
  { t: 'z-index management strategy', d: 'Scales/tokens (z-10..z-50 tiers: dropdown < sticky < modal < toast) beat magic numbers; audit stacking-context creation before blaming values. Negative z-index hides behind backgrounds of contexts. Document layers in design tokens keeping overlays predictable app-wide.' },
  { t: 'floats legacy and clearfix', d: 'Floats wrapped text originally then carried page layouts pre-flexbox; clearing required clearfix pseudo-element hacks (display:flow-root now native). Modern usage survives in text-wrap-around-image cases only \u2014 reach for flow-root instead of clearfix folklore when floats leak height collapse.' },
  { t: 'inline-block whitespace gap', d: 'Inline-block elements render HTML-source whitespace between them creating ~4px gaps; fixes included font-size:0 parents or comment removals. Flex/grid gap solves spacing properly today; understanding the artifact aids archaeology in legacy stylesheets.' },
  { t: 'display table centering trick', d: 'Legacy vertical centering used display:table/table-cell + vertical-align:middle before flexbox. Still functional in email HTML where client support lags. Modern answer: display:flex + align-items:center or grid place-items:center one-liner.' },
  { t: 'centering techniques matrix', d: 'Horizontal: margin-inline:auto for blocks, text-align for inline. Vertical+horizontal: grid place-items:center (parent knows), flex + margin:auto (item knows), absolute + inset:0 + margin:auto, translate(-50%,-50%) legacy. Grid one-liner is today\u2019s canonical interview answer.' },
  { t: 'aspect-ratio property', d: 'aspect-ratio: 16 / 9 sizes boxes proportionally when one dimension known \u2014 replacing padding-top percentage hacks. Content can force overflow overriding ratio unless min-height:0/min-width constraints apply. Video embeds, cards, avatars standardize instantly.' },
  { t: 'object-fit cover vs contain', d: 'For replaced elements (img/video): cover fills box cropping overflow (hero images); contain letterboxes preserving whole asset (product shots). object-position biases crop focal point. Without object-fit images stretch/distort whenever intrinsic ratio mismatches container.' },
  { t: 'background-size and attachment', d: 'background-size cover/contain mirror object-fit for background images; attachment:fixed creates parallax but janks/disables on iOS Safari \u2014 prefer transform-based parallax. Multiple backgrounds layer comma-separated with independent sizing/positioning enabling composite artwork purely in CSS.' },
  { t: 'gradients linear radial conic', d: 'linear-gradient angles/directions fade between stops (transparent-to-color overlays on heroes); radial-gradient spotlights/vignettes; conic-gradient sweeps angle-wise building pie charts/color wheels natively. Repeating variants tile bands infinitely \u2014 stripes without images.' },
  { t: 'gradient tricks text and borders', d: 'Gradient text: background gradient + color:transparent + background-clip:text. Gradient borders: border-image or double-background (padding-box vs border-box layers). These composites replace image assets and enable animated gradients via background-position transitions.' },
  { t: 'blend modes in CSS', d: 'mix-blend-mode blends element against backdrop (multiply duotones, difference inverting); background-blend-mode mixes stacked backgrounds. Isolation limits blend scope. Blend modes unlock photographic effects (duotone heroes, knockout text) without Photoshop exports.' },
  { t: 'filters and backdrop-filter', d: 'filter applies blur/brightness/grayscale to element contents; backdrop-filter processes what lies BEHIND (glassmorphism frosted panels). Both create stacking/containing contexts affecting fixed positioning. Performance: large blurred areas cost paint \u2014 test low-end devices.' },
  { t: 'box-shadow layering craft', d: 'Multiple comma-separated shadows build realistic elevation (tight dark contact shadow + soft ambient spread). Inset shadows simulate pressed states/inner glows. Shadow tokens encode elevation scale consistently; colored shadows tint brand glow effects subtly rather than neon abuse.' },
  { t: 'outline vs border differences', d: 'Outlines draw outside layout flow (no space taken), don\u2019t follow border-radius historically (now they do), and serve focus indicators accessibly. outline-offset spaces indicator from edge. Never remove focus outlines without :focus-visible styled replacements \u2014 keyboard users depend on them.' },
  { t: 'text truncation single line', d: 'white-space:nowrap + overflow:hidden + text-overflow:ellipsis clips gracefully with ellipsis. Works on block containers with constrained width. Table cells need max-width set. Provide title/full-text affordances since truncation hides information.' },
  { t: 'multi-line clamping', d: '-webkit-line-clamp: N with display:-webkit-box + -webkit-box-orient:vertical became standardized as line-clamp; widely supported despite vendor prefixes lingering. Fallbacks show unclamped or fade masks. Combine with aria-label carrying full text for accessibility.' },
  { t: 'custom properties var fallbacks', d: '--tokens store reusable values consumed via var(--token, fallback); fallback handles undefined tokens gracefully. Properties inherit and cascade like normal values, are runtime-editable via JS enabling theme switching without recompilation, and cannot hold media-query logic themselves.' },
  { t: 'theme switching with data attributes', d: '[data-theme="dark"] { --bg: ... } swaps token values scoped per attribute; toggle attribute on html element persisting choice. Transitions animate theme changes smoothly (transition on color-consuming props). Respects system preference initially via prefers-color-scheme matchMedia detection.' },
  { t: 'dark mode implementation', d: 'Layers: semantic tokens (--surface, --text) abstracted above raw palette; @media (prefers-color-scheme: dark) defaults plus manual override attribute; contrast verification both themes (4.5:1 text minimum); images/icons adapt via currentColor/CSS filters where assets lack dark variants.' },
  { t: 'prefers-reduced-motion respect', d: '@media (prefers-reduced-motion: reduce) disables/shortens animations/transitions/parallax preventing vestibular distress. Wrap motion behind the query globally via shared mixins/utilities. Motion should communicate, never decorate at users\u2019 expense \u2014 this query encodes that ethic.' },
  { t: 'responsive breakpoint strategy', d: 'Mobile-first min-width queries extend upward matching natural CSS cascade addition; breakpoints derive from CONTENT breaking points not device widths (device zoo changes yearly). Fewer, content-driven breakpoints (e.g., 40em/60em/80em) plus fluid techniques minimize maintenance surface.' },
  { t: 'container queries revolution', d: '@container (min-width: 400px) styles components by THEIR container size not viewport \u2014 true component responsiveness for cards/sidebars/widgets reused in varying slots. cqi/cqw units size relative to container. Register containment implicitly via container-type: inline-size on wrappers.' },
  { t: 'media query range syntax', d: '@media (400px <= width <= 800px) replaces min-/max- verbosity reading mathematically. Mixing old/new syntax inconsistently confuses tooling; pick range syntax for new code. Feature queries (@media (hover: hover), (pointer: coarse)) tailor interactions to input modality beyond width alone.' },
  { t: 'print styles essentials', d: '@media print hides nav/chrome, expands links with href after text, forces black-on-white, avoids page-break-inside on cards/tables, sets margins via @page. URL expansion uses ::after content attr(href). Test via browser print preview before shipping invoices/articles.' },
  { t: 'pointer coarse and hover none', d: '@media (hover:none)/(pointer:coarse) detect touch-primary devices: enlarge targets (44px min), disable hover-dependent reveals (use details/tap toggles), avoid hover-only affordances. Desktop assumptions break touch UX invisibly \u2014 these queries surface the divide programmatically.' },
  { t: 'nth-child formulas mastery', d: ':nth-child(an+b) selects patterns: odd/even zebra rows, 3n every third, n+5 from fifth onward, -n+3 first three. :nth-last-child counts backward enabling last-N styling. Combined with :not() builds precise slices without classes.' },
  { t: 'nth-of-type vs nth-child', d: ':nth-child counts ALL sibling elements regardless of type; :nth-of-type counts only matching tag types. Mixed markup (spans among divs) diverges results \u2014 classic bug source. Prefer structural consistency or explicit classes when mixing element types inside selection containers.' },
  { t: ':is :where specificity games', d: ':is() takes complex selectors listing alternatives with highest-argument specificity; :where() zeroes specificity entirely \u2014 perfect for reset defaults overridden effortlessly later. Composing :where(:is(a,b)) balances ergonomics and neutrality deliberately in design-system foundations.' },
  { t: ':has parent selector power', d: ':has() selects ancestors/previous siblings conditionally: form:has(input:invalid) styles forms containing errors; figure:has(img:hover) reacts upward \u2014 the "parent selector" finally shipped. Enables state styling without JS/class toggling; watch performance on huge DOM subtrees.' },
  { t: 'focus-visible vs focus', d: ':focus-visible fires for keyboard/heuristic focus only, sparing mouse-click outlines while keeping keyboard visibility \u2014 replacing hacky focus ring suppression scripts. :focus matches all focus for programmatic restyling. Style both thoughtfully: visible rings for keyboard, custom states for pointer.' },
  { t: 'LVHA link pseudo order', d: 'Link pseudo-classes cascade in LVHA order (:link :visited :active :hover) historically ensuring active/hover override visited styling. Modern minimal usage still trips ordering when composing; group related pseudo-class rules intentionally rather than scattering them.' },
  { t: 'before after content requirement', d: '::before/::after inject generated boxes requiring content (even empty string "") to render; used for decorative icons, clearfix, overlay layers. Positioned absolutely they become independent layers; z-index -1 tucks them behind text. They are NOT real content \u2014 never put meaningful text there (screen readers vary).' },
  { t: 'placeholder and selection styling', d: '::placeholder styles input hints (maintain contrast \u2014 placeholders are not labels); ::selection colors highlighted text brand-consistently. Both historically vendor-prefixed; modern browsers accept unprefixed. Placeholder color contrast failures are common axe findings worth proactive styling.' },
  { t: 'marker and backdrop pseudos', d: '::marker styles list bullets/numbers (color, font) replacing fragile list-style hacks; ::backdrop styles fullscreen/modal top-layers (dialog element dimming). Both target otherwise-unstyleable engine-generated boxes completing modern component polish kits.' },
  { t: 'drop caps with first-letter', d: '::first-letter styles initial character (float:left + font-size + line-height adjustments create drop caps typographically). Punctuation preceding letters includes in selection. Editorial/article layouts regain print-era flourishes purely in CSS.' },
  { t: 'transitions fundamentals', d: 'transition-property/duration/timing-function/delay animate computed-value changes triggered by state flips; cubic-bezier curves shape feel (ease-out entrances, ease-in exits); transition-behavior:allow-discrete animates display toggles newly. Only interpolable properties animate \u2014 height/auto jumps need measured or modern calc-size approaches.' },
  { t: 'transition all cost concern', d: 'transition:all animates EVERY changed property including expensive layout ones (width/top) and accidental ones (color side-effects) \u2014 enumerate intended properties explicitly. Explicit lists document intent, prevent jank, and avoid surprising animation of inherited/unrelated updates.' },
  { t: 'will-change usage discipline', d: 'will-change hints engines promoting layers ahead of animation; overuse wastes memory (each promotion allocates) and can degrade more than help. Apply just-in-time via JS before animations, remove afterward, or scope to persistent interactive elements measured as janky.' },
  { t: 'compositor-only properties', d: 'transform and opacity animate on compositor thread skipping layout/paint \u2014 GPU-cheap 60fps. Animating width/left/height triggers layout cascades per frame. FLIP converts layout changes into transform animations marrying correctness with performance.' },
  { t: 'keyframes animation anatomy', d: '@keyframes define staged property keyframes; animation-name/duration/timing/delay/iteration-count/direction/fill-mode/play-state orchestrate. fill-mode:forwards persists end-state post-animation (else styles snap back). Infinite loops suit ambient motion; entrance animations run once.' },
  { t: 'animation steps timing function', d: 'steps(n) jumps discretely between keyframes producing sprite-sheet frame animation (classic pixel walk cycles via background-position) and typewriter cursor blinks. Contrast with smooth beziers; combining steps with infinite iterations yields retro effects cheaply without JS.' },
  { t: 'animation vs transition decision', d: 'Transitions respond to state CHANGES (hover, class flips) needing defined start/end; keyframe animations play autonomously with midpoints, looping, and choreography. Rule of thumb: interaction feedback \u2192 transition; narrative/ambient sequences \u2192 animation. Web Animations API bridges programmatically.' },
  { t: 'play-state pausing animations', d: 'animation-play-state: paused freezes timelines (carousel hover-pause compliance); running resumes preserving position. Toggle via class/media queries. Pausing decorative loops on hover/focus respects WCAG pause-stop-hide guidance for moving content.' },
  { t: 'scroll-driven animations intro', d: 'animation-timeline: scroll()/view() binds progress to scroll position natively \u2014 parallax, progress bars, reveal-on-scroll without JS rAF loops. Range controls entry/exit windows. Progressive enhancement: feature-detect and keep static fallbacks for unsupported browsers.' },
  { t: 'variable fonts usage', d: 'Variable fonts pack weight/width/slant axes into single files morphing continuously via font-variation-settings or high-level font-weight ranges. One file replaces many cuts saving requests; named instances map to preset combos. Test axis rendering across platforms for hinting differences.' },
  { t: 'font-display strategies', d: '@font-face font-display: swap shows fallback immediately swapping when loaded (FOUT); block hides briefly (FOIT); optional uses font only if instantly available prioritizing stability. Pair with size-adjust metric overrides minimizing layout shift during swap windows.' },
  { t: 'system font stacks', d: 'System stacks (-apple-system, Segoe UI, Roboto...) ship zero network cost with native look; modern font-family: system-ui encapsulates preference. Tradeoff: cross-platform inconsistency. Many design systems pair system-ui for UI chrome with branded webfonts for headings only.' },
  { t: 'icon fonts vs svg icons', d: 'Icon fonts (ligature/charcode mapping) suffer FOUT, accessibility oddities, and single-color limits; inline SVGs style via currentColor, scale crisply, expose proper roles. Sprite sheets (<symbol>/<use>) dedupe markup. SVG won decisively for modern iconography pipelines.' },
  { t: 'srcset and sizes attributes', d: 'srcset lists candidate resolutions (image.jpg 1x/2x or w-descriptors); sizes tells browser rendered slot width letting it choose optimal download. Prevents 3000px desktop images on phones \u2014 biggest mobile perf win with pure markup. Always pair with width/height attrs reserving space.' },
  { t: 'picture art direction', d: '<picture> switches sources per media conditions: cropped square for mobile, wide for desktop (art direction), or format fallbacks (AVIF/WebP first, JPEG last). Unlike srcset\u2019s resolution switching, picture serves DIFFERENT imagery \u2014 choose per intent.' },
  { t: 'modern image formats', d: 'AVIF compresses ~50% better than JPEG (slower encodes, no progressive decode); WebP balances compatibility/perf; JPEG XL pending. Serve via picture format-fallback chains. Quality settings around 60-75 AVIF often beat 90 JPEG visually at fraction of bytes.' },
  { t: 'lazy loading images native', d: 'loading="lazy" defers offscreen image fetches natively; combine with decoding="async" and explicit dimensions (CLS prevention). Avoid lazy-loading LCP hero images \u2014 eager + fetchpriority=high wins there. Below-fold galleries benefit massively with zero JS.' },
  { t: 'svg inline vs img tradeoffs', d: 'Inline SVG styles internally (currentColor, part styling) and animates via CSS but bloats HTML/caching; img/svg references cache cleanly but resist styling. Sprites balance: symbol definitions once, use references everywhere, currentColor propagation intact.' },
  { t: 'currentColor inheritance trick', d: 'fill="currentColor"/stroke inherits text color letting icons recolor via color property contextually (buttons, alerts). Single-path icon sets ship themeable without variant files. Combines beautifully with em-sized viewBoxes scaling relative to surrounding text.' },
  { t: 'clip-path shapes', d: 'clip-path crops elements to polygons/circles/paths enabling angled sections, avatar blobs, reveal animations (circle growing). Animating clip-path values interpolates smoothly for wipe transitions. Reference-box keywords tune coordinate origins; check Safari prefix needs for older targets.' },
  { t: 'masking images css', d: 'mask-image uses luminance/alpha of another image/gradient to hide portions \u2014 fade-out edges (mask linear-gradient), icon tinting via alpha masks. Prefix history (-webkit-mask) lingers; mask-composite combines layers. More flexible than clip-path for soft-edged effects.' },
  { t: 'css shapes text wrapping', d: 'shape-outside floats text along curved boundaries (circle, path) around floated elements \u2014 magazine-style wraps. Requires float context; shape-margin pads. Niche editorial flourish demonstrating CSS layout depth beyond rectangles.' },
  { t: 'multicol columns layout', d: 'columns/column-count splits content into newspaper columns balancing automatically; column-gap/rule style separators. Reading order flows down-then-across (breaks scan patterns for UI); break-inside:avoid keeps cards whole. Best suited for long-form text, not app interfaces.' },
  { t: 'writing modes vertical text', d: 'writing-mode: vertical-rl rotates text flow (CJK traditions, sideways labels); text-orientation fine-tunes glyph rotation. Logical properties (margin-block-start etc.) adapt automatically making layouts direction-robust. Vertical navigation labels/tab strips leverage this elegantly.' },
  { t: 'logical properties adoption', d: 'margin-inline/block, padding-inline/start-end, inset-inline, border-start replace physical left/right/top/bottom adapting automatically under RTL/LTR and vertical writing modes. New code defaults logical; grep audits convert legacy directional hacks during i18n readiness pushes.' },
  { t: 'rtl support essentials', d: 'dir="rtl" on html flips document direction; logical properties handle most spacing automatically; directional icons (arrows, progress) need explicit mirroring ([dir="rtl"] .icon-arrow { transform: scaleX(-1) }). Test real RTL locales (Arabic/Hebrew) \u2014 pseudo-localization tools flag hard-coded directions early.' },
  { t: 'sticky table headers pattern', d: 'thead th { position: sticky; top: 0; background: ... } pins headers within scrolling table containers; border-collapse interferes historically (use border-spacing or box-shadows). Column stickiness combines inset-inline-start offsets for spreadsheet-like freezing.' },
  { t: 'form reset appearance normalization', d: 'appearance:none strips native widget chrome (select arrows, checkbox art) enabling custom styling; re-supply visual affordances (custom chevron via background SVG) since removing appearance removes usability cues too. accent-color styles checkboxes/radios natively with one line when custom art unnecessary.' },
  { t: 'accent-color and caret-color', d: 'accent-color tints native controls (checkboxes, radios, range, progress) matching brand with guaranteed accessible contrast handled by UA; caret-color styles text cursors. Minimal-effort theming respecting platform familiarity \u2014 often superior to full custom rebuilds.' },
  { t: 'valid invalid validation ui', d: ':valid/:invalid/:user-invalid (fires only AFTER user interaction, fixing premature-red problem) style validation states; placeholder-shown detects emptiness. Pair visual states with aria-live messages since color-alone signaling fails accessibility. required asterisks via ::after content.' },
  { t: 'css counters numbering', d: 'counter-reset/increment plus content: counter(item) generate automatic numbering (ordered lists, headings chapters) with styleable markers; counters() nests hierarchically. Replaces manual number maintenance in generated/static content pipelines.' },
  { t: 'attr() in content', d: 'content: attr(data-label) surfaces attribute values in pseudo-elements \u2014 tooltips, responsive table headers (td::before content attr(data-th) on mobile), badge counts. Limited to content property traditionally; typed attr() expansion is emerging spec territory.' },
  { t: 'quotes nesting localization', d: 'quotes property defines language-appropriate quotation marks consumed via open-quote/close-quote keywords in content; nested quotes alternate automatically. Set per :lang() selectors honoring typographic conventions (French guillemets, German low-high pairs) without hard-coding characters.' },
  { t: 'overflow-wrap vs word-break', d: 'overflow-wrap:anywhere breaks long unbroken strings (URLs) preserving readability preferring clean points; word-break:break-all chops aggressively anywhere. hyphens:auto needs lang attributes for dictionary hyphenation. Long-token handling prevents horizontal scroll blowouts in user-generated content.' },
  { t: 'white-space variants', d: 'normal collapses whitespace/wraps; nowrap forbids wrapping; pre preserves exactly; pre-wrap preserves but wraps; pre-line collapses but keeps breaks. Code displays want pre/code pairing; chat inputs pre-wrap. Truncation recipes combine nowrap+ellipsis+overflow-hidden.' },
  { t: 'scrollbar styling', d: 'scrollbar-width/scrollbar-color (standard) plus ::-webkit-scrollbar family customize bars subtly; heavy custom wheels confuse muscle memory \u2014 prefer thin/none overlays for panels, native elsewhere. scrollbar-gutter reserves space preventing layout jump when content grows scrollbars.' },
  { t: 'color-mix modern colors', d: 'color-mix(in oklab, var(--brand), white 20%) derives hover shades/tints from tokens at runtime \u2014 eliminating hand-computed palette ramps. Oklab/oklch spaces yield perceptually uniform mixing. Relative color syntax further adjusts existing colors channel-wise (upcoming breadth).' },
  { t: 'oklch perceptual color space', d: 'oklch(lightness chroma hue) defines colors by perception making equal-L values equally bright \u2014 accessible palettes construct systematically (fix L, sweep hue). Widening-gamut support reaches P3 displays progressively. Gradients interpolate smoother than sRGB counterparts.' },
  { t: 'contrast checking workflow', d: 'WCAG AA demands 4.5:1 normal text (3:1 large/UI components); browser DevTools color pickers surface ratios live; automated axe catches regressions. Design tokens encode approved pairings preventing ad-hoc violations. Contrast failures dominate real-world a11y defect statistics.' },
  { t: 'forced colors adaptation', d: '@media (forced-colors: active) lets designs cooperate with Windows High Contrast: forced-color-adjust:none opts specific elements back while supplying manual contrast; system Colors (CanvasText, LinkText) map semantically. Unchecked, brands vanish or turn unreadable under forced modes.' },
  { t: 'overscroll-behavior control', d: 'overscroll-behavior: contain stops scroll chaining (inner modal scroll no longer scrolls page beneath); none disables bounce/rubber-band too. Fixes the infamous pull-to-refresh-inside-widget accidents and scroll-trap complaints in embedded panes.' },
  { t: 'touch-action manipulation', d: 'touch-action controls gesture handling: none disables browser panning/zoom for canvas apps; manipulation keeps panning/pinch but kills double-tap-zoom delay; pan-y restricts horizontal carousels to vertical page scroll coexistence. Misuse breaks accessibility zoom \u2014 scope narrowly.' },
  { t: 'safe-area insets notch handling', d: 'env(safe-area-inset-*) plus viewport-fit=cover pad content around notches/home-indicators on notched devices \u2014 critical for fixed bars/footers in standalone PWAs. calc() composes with existing paddings. Ignoring insets buries actions under hardware obstructions on iPhone-class devices.' },
  { t: 'user-select and cursor affordances', d: 'user-select:none protects interactive controls from accidental text selection (buttons, drag handles) while copy-critical content stays selectable; cursor:pointer signals clickability (non-native clickable divs NEED it), grab/grabbing communicates draggability. Affordance honesty beats aesthetic purity.' },
  { t: 'pointer-events none passthrough', d: 'pointer-events:none makes elements click-transparent \u2014 overlay decorations stop blocking underlying buttons; re-enable auto on children selectively. Disabled-looking-but-hover-animated elements, drag ghosts, and map-under-overlay patterns rely on this escape hatch.' },
];
