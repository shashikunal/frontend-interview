// DOM & Web APIs — deep interface knowledge with practical examples
// t = topic, d = in-depth answer, c = optional practical example code
export default [
  { t: 'the DOMTokenList interface', d: 'DOMTokenList is the ordered collection of space-separated tokens returned by classList, relList, and similar attributes. It offers add, remove, toggle (with force argument), replace, item, contains, length, and forEach \u2014 all safer than manual className string surgery because they handle whitespace and duplicates automatically. It is live-ish: changes reflect immediately in the underlying attribute.' , c: "const el = document.querySelector('#card');\nconst list = el.classList; // DOMTokenList\nlist.add('open', 'visible');\nlist.remove('hidden');\nlist.toggle('active', window.scrollY > 50); // force param\nconsole.log(list.contains('open'), list.length);\nlist.replace('old-theme', 'new-theme');" },
  { t: 'classList operations compared to className', d: 'className reads/writes the entire class attribute string; classList exposes granular DOMTokenList methods. Overwriting className clobbers classes set elsewhere (libraries, frameworks), while classList.add preserves them. Read className for serialization/debugging; mutate exclusively through classList.' },
  { t: 'AbortController', d: 'AbortController creates an AbortSignal via its signal property; calling abort(reason) flips signal.aborted to true, fires the abort event, and rejects every operation listening to that signal \u2014 fetch requests, event listeners, streams, promises you wire manually. abort() accepts an optional reason exposed as signal.reason. One controller can coordinate cancellation across many consumers, making it the standard cancellation primitive of the web platform.', c: "const controller = new AbortController();\n\nfetch('/api/search?q=react', { signal: controller.signal })\n  .then(r => r.json())\n  .then(console.log)\n  .catch(err => {\n    if (err.name === 'AbortError') console.log('cancelled:', controller.signal.reason);\n    else console.error(err);\n  });\n\ncontroller.abort('user typed again');" },
  { t: 'the AbortSignal static helpers', d: 'AbortSignal.timeout(ms) builds a signal that aborts itself after the delay \u2014 instant fetch timeouts without controllers. AbortSignal.any([signals]) composes multiple signals into one that aborts when any source aborts, ideal for combining user-cancellation with timeouts. Both signals expose aborted, reason, onabort, throwIfAborted().' },
  { t: 'signal.throwIfAborted usage', d: 'throwIfAborted() throws signal.reason if the signal has fired, otherwise returns undefined. Long-running loops call it between chunks enabling cooperative cancellation without wiring event listeners manually \u2014 the cleanest way to honor cancellation in your own async algorithms.', c: "async function process(items, signal) {\n  for (const item of items) {\n    signal?.throwIfAborted(); // cooperative exit point\n    await heavyWork(item);\n  }\n}" },
  { t: 'querySelector and querySelectorAll', d: 'querySelector(css) returns the first element matching a full CSS selector list or null; querySelectorAll returns a STATIC NodeList of all matches in document order. They accept any selector complexity (combinators, pseudo-classes except structural-dynamic quirks in some engines) and can be called on document or any element to scope the search subtree. Prefer them over legacy getters for consistency.' },
  { t: 'the static vs live NodeList distinction', d: 'querySelectorAll yields a static snapshot \u2014 later DOM mutations never alter it. getElementsByClassName/getElementsByTagName yield LIVE HTMLCollections that auto-update as the DOM changes, sometimes mid-loop causing skipped items. Iterating live collections while mutating requires copying ([...collection]) or counting downward.' },
  { t: 'NodeList methods and iteration', d: 'Modern NodeList implements forEach, item, keys, values, entries plus Symbol.iterator so spread/for-of work. It lacks map/filter/reduce \u2014 convert via Array.from(list) or [...list] when array methods are needed. Static NodeLists from querySelectorAll are snapshots; remember conversion copies references, elements themselves stay shared.' },
  { t: 'HTMLCollection vs NodeList', d: 'HTMLCollection (live, element-only, index+name access) comes from legacy getters like children and forms collections; NodeList (static or live depending on source) may contain any node type (rarely non-elements). Modern code standardizes on querySelectorAll + static NodeList, converting to arrays freely for pipeline operations.' },
  { t: 'getElementById special behavior', d: 'document.getElementById is the fastest lookup (hash-indexed internally) returning the FIRST element with matching id or null. It exists only on document \u2014 not elements \u2014 and ignores querySelector syntax entirely (no # prefix). Duplicate ids are invalid HTML but browsers return just the first, masking authoring bugs silently.' },
  { t: 'closest and matches navigation', d: 'matches(selector) tests whether an element satisfies a selector; closest(selector) walks UP the ancestor chain (including the element itself) returning the first match \u2014 essential for event delegation routing clicks to semantic containers. Both throw SyntaxError on invalid selectors.', c: "document.addEventListener('click', e => {\n  const btn = e.target.closest('[data-action]');\n  if (!btn || !e.currentTarget.contains(btn)) return;\n  const action = btn.dataset.action;\n  if (action === 'delete') btn.closest('.row')?.remove();\n});" },
  { t: 'nodeType constants', d: 'Every node carries a numeric nodeType: 1 Element, 3 Text, 8 Comment, 9 Document, 11 DocumentFragment, plus rarer CDATA/processing-instruction types. Modern code rarely checks numbers \u2014 instanceof Element/Text or ELEMENT_NODE named constants communicate intent better than magic integers.' },
  { t: 'childNodes vs children', d: 'childNodes is a LIVE NodeList including text nodes, comments, and whitespace between tags; children is a LIVE HTMLCollection of element-only members. Counting childNodes.length surprises developers because indentation whitespace creates text nodes \u2014 children.length usually matches the intuitive element count.' },
  { t: 'firstChild vs firstElementChild', d: 'firstChild/lastChild/nextSibling/previousSibling navigate raw node boundaries where whitespace text nodes frequently intervene; the Element-suffixed versions (firstElementChild etc.) skip non-element nodes giving predictable traversal regardless of formatting. Default to Element variants unless manipulating comments/text deliberately.' },
  { t: 'Node.contains', d: 'node.contains(other) reports whether other is the node itself or a descendant \u2014 the identity-safe membership test for click-outside detection, drag-drop validation, and focus retention logic. Returns false when either argument is detached from the tree relationships being tested.' },
  { t: 'TreeWalker deep traversal', d: 'document.createTreeWalker(root, filter) yields a cursor-based walker with currentNode navigation: nextNode(), previousNode(), firstChild() etc., honoring NodeFilter callbacks (FILTER_ACCEPT/SKIP/REJECT) evaluated per candidate. SKIP skips the node but descends children; REJECT prunes whole subtrees (elements only). Ideal for filtered deep scans without recursion.' },
  { t: 'NodeIterator simple iteration', d: 'document.createNodeIterator(root, filter) provides forward/backward linear iteration (nextNode/previousNode) over matching nodes with automatic detachment handling. Simpler than TreeWalker when hierarchical control is unnecessary; both accept whatToShow bitmasks narrowing visited node types cheaply.' },
  { t: 'DocumentFragment batching', d: 'createDocumentFragment builds detached node groups; appending children costs nothing visually until the fragment mounts in ONE operation triggering single reflow. After insertion the fragment empties itself (children transfer). Modern alternative: building strings or cloning templates \u2014 fragments remain the canonical batch-insert primitive.' },
  { t: 'the Range interface', d: 'document.createRange() models a contiguous span between boundary points (startContainer/startOffset, endContainer/endOffset). Ranges select text programmatically, wrap contents (surroundContents), extract/clone fragments (extractContents/cloneContents), measure via getBoundingClientRect, and underpin custom selection UIs and rich-text editors.' },
  { t: 'insertAdjacentHTML positions', d: 'element.insertAdjacentHTML(position, html) parses HTML and inserts at four positions: beforebegin, afterbegin, beforeend, afterend \u2014 relative to the element. Faster than innerHTML reassignment because existing children survive untouched; still executes scripts? No \u2014 script tags inserted this way do NOT run, but event-handler attributes become live, so sanitize untrusted input.' },
  { t: 'append and prepend modern insertion', d: 'Parent.append()/prepend()/replaceChildren() accept multiple nodes AND strings (auto-text-converted), unlike appendChild\u2019s single-node contract. append appends after last child; prepend inserts before first; replaceChildren clears-and-fills atomically \u2014 the cleanest refresh pattern for dynamic containers.', c: "const list = document.querySelector('#todos');\nconst li = document.createElement('li');\nli.textContent = 'New task';\nlist.append(li, document.createElement('hr'), 'plain text works too');\n\n// atomic refresh:\nconst fresh = document.createElement('div');\nfresh.textContent = 'Replaced everything';\nlist.replaceChildren(fresh);" },
  { t: 'insertBefore precise placement', d: 'parentNode.insertBefore(newNode, referenceNode) inserts before the given child; passing null appends. Despite the name there is no insertAfter \u2014 emulate via insertBefore(node, ref.nextSibling). Returns the inserted node enabling chained configuration.' },
  { t: 'cloneNode semantics', d: 'cloneNode(false) shallow-copies attributes only; cloneNode(true) deep-clones the whole subtree. Listeners added via addEventListener are NEVER cloned (inline handlers are, as attributes). Cloning template rows for repeated structures beats rebuilding markup strings and keeps type-safety within DOM APIs.', c: "const rowTemplate = document.querySelector('.row');\nfor (let i = 0; i < 3; i++) {\n  const clone = rowTemplate.cloneNode(true);\n  clone.querySelector('.name').textContent = `Item ${i + 1}`;\n  clone.hidden = false;\n  rowTemplate.parentElement.appendChild(clone);\n}" },
  { t: 'textContent vs innerText', d: 'textContent returns ALL text including hidden elements and script/style contents, never triggering layout; innerText approximates RENDERED text, respecting CSS visibility and normalizing whitespace at the cost of forced reflow. Write with textContent (fast, safe \u2014 auto-escapes); read display text with innerText only when CSS-awareness matters.' },
  { t: 'innerHTML security tradeoffs', d: 'innerHTML parses assignment strings executing embedded img onerror handlers and similar vectors \u2014 the classic XSS injection point. Never feed it untrusted input without sanitization (DOMPurify). Legitimate uses: rendering trusted/sanitized rich content, bulk updates outperforming per-node creation. Script tags assigned via innerHTML do not execute.' },
  { t: 'outerHTML replacement behavior', d: 'Reading outerHTML serializes the element including its tag; assigning REPLACES the element in the DOM with newly parsed nodes \u2014 old references die silently, listeners vanish. Useful for wholesale swap patterns; hazardous when holding references expecting persistence afterwards.' },
  { t: 'createElementNS for SVG', d: 'SVG lives in the XML namespace \u2014 plain createElement produces HTMLUnknownElements that render nothing for svg tags. Use createElementNS("http://www.w3.org/2000/svg", "circle") for namespace-correct creation, or clone <template> contents authored in markup. Attributes like xlink:href historically required namespaced setters.' },
  { t: 'importNode and adoptNode', d: 'importNode(node, deep) COPIES a node from another document (iframe) keeping ownership there; adoptNode MOVES ownership into the current document. Necessary because cross-document nodes misbehave until imported/adopted. Same-document usage is redundant \u2014 cloneNode covers copying locally.' },
  { t: 'the dataset interface', d: 'dataset exposes data-* attributes as camelCased DOMStringMap properties: data-user-id becomes dataset.userId. Values are always strings \u2014 parse numbers/JSON explicitly. Assigning null/undefined stringifies literally; use removeAttribute or delete dataset.key to clear. Delegated handlers read targets\u2019 dataset constantly for parameterized actions.', c: "<!-- markup -->\n<!-- <button data-product-id=\"42\" data-plan=\"pro\">Buy</button> -->\ndocument.addEventListener('click', e => {\n  const btn = e.target.closest('[data-product-id]');\n  if (!btn) return;\n  const id = Number(btn.dataset.productId); // string -> number\n  checkout(id, btn.dataset.plan);\n});" },
  { t: 'attribute access versus property access', d: 'setAttribute/getAttribute manipulate STRING attribute values in markup; element properties reflect parsed/live state (input.value tracks typing; input.getAttribute("value") stays at initial markup). Booleans confuse most: checked/disabled attributes only seed initial state. Rule: attributes for markup fidelity, properties for runtime behavior.' },
  { t: 'toggleAttribute boolean handling', d: 'toggleAttribute(name, force?) adds/removes boolean attributes (disabled, hidden, readonly) returning resulting presence \u2014 cleaner than conditional set/remove pairs. Force argument pins outcome declaratively: el.toggleAttribute("disabled", !formValid) expresses intent in one line replacing branching.' },
  { t: 'the hidden attribute versus display styles', d: '[hidden] maps to UA display:none by default BUT loses to explicit display rules (flex/grid classes override it) \u2014 a classic bug. Guarantee semantics with CSS [hidden] { display: none !important } guard, or manage visibility through classes/toggles consistently instead of mixing mechanisms.' },
  { t: 'CSSStyleDeclaration inline styling', d: 'element.style exposes ONLY inline styles: reading returns empty strings for stylesheet-applied values (use getComputedStyle for cascaded results). Property names camelCase (backgroundColor); setting requires units for length values ("10px", not 10). style.setProperty("--custom", v) writes design tokens imperatively \u2014 great for JS-driven theming.' },
  { t: 'getComputedStyle essentials', d: 'window.getComputedStyle(el) resolves the FULL cascade including inherited/UA defaults into a live-ish read-only declaration. Numeric getters (opacity, zIndex) may return strings needing parseFloat; dimensions exclude margins. Reading layout-affecting properties forces synchronous reflow \u2014 cache results inside hot loops.' },
  { t: 'checkVisibility modern detection', d: 'el.checkVisibility({ checkOpacity, checkVisibilityCSS, contentVisibilityAuto }) answers "is this actually rendered?" beyond offsetParent tricks, accounting for display:none, visibility, opacity:0, and content-visibility policies. Replaces fragile heuristic chains scattered through utility libraries for analytics/impression logic.' },
  { t: 'FormData construction and inspection', d: 'new FormData(form) harvests named controls automatically (skipping disabled/nameless/file-vs-text appropriately); programmatic instances append arbitrary Blob/File/string entries for fetch bodies. Inspection: entries()/get()/getAll()/has()/has iteration. Fetching with a FormData body sets multipart encoding automatically \u2014 never hand-set Content-Type then.', c: "const form = document.querySelector('#upload');\nform.addEventListener('submit', async e => {\n  e.preventDefault();\n  const fd = new FormData(form);\n  fd.append('client', 'web-v2');\n  const res = await fetch('/api/upload', { method: 'POST', body: fd });\n  console.log((await res.json()).url);\n});" },
  { t: 'requestSubmit versus submit method', d: 'form.requestSubmit(submitter?) runs full validation and fires submit events honoring the clicked button semantics; legacy form.submit() bypasses validation/events entirely (silent no-validate sends). Programmatic saves should almost always requestSubmit \u2014 submit() reserved for genuinely raw bypass cases.' },
  { t: 'the ValidityState interface', d: 'input.validity exposes flags: valueMissing, typeMismatch, patternMismatch, tooLong/short, rangeUnder/overflow, stepMismatch, badInput, customError, culminating in valid booleans. Drive custom error UI from these flags instead of regex duplication \u2014 the browser already computed constraint violations per its validation attributes.' },
  { t: 'setCustomValidity workflows', d: 'setCustomValidity(message) marks a control invalid with your message surfacing via validationMessage; empty-string clears. Pair with constraint validation events (input/change/invalid) implementing cross-field rules (password confirmation) the native attributes cannot express, remembering to clear promptly as users fix values.' },
  { t: 'HTMLSelectElement manipulation', d: 'select.value reflects selected option; select.selectedIndex gives position; options is an HTMLOptionsCollection supporting add/remove/item. Building options: new Option(text, value) then add(). Multiple selects surface selectedOptions list. Mutating option disabled/selected properties updates live \u2014 cascading country/state pickers exercise these constantly.' },
  { t: 'textarea selection APIs', d: 'selectionStart/selectionEnd index the caret/selected span within textarea.value; setSelectionRange(start, end, direction?) positions programmatically (must run post-focus); setRangeText replaces spans honoring selection modes. Powering autocomplete carets, mention highlighting, and undoable insertions in editors built on plain textareas.' },
  { t: 'EventTarget listener registration', d: 'addEventListener(type, handler, options) \u2014 options object supports capture, once (auto-remove after fire), passive (perf hint promising no preventDefault), and signal (AbortSignal auto-removal \u2014 modern cleanup champion). Duplicate identical registrations are ignored; arrow wrappers break removal identity so keep references.' },
  { t: 'listener cleanup via AbortSignal', d: 'Pass one controller signal into every listener registration belonging to a component lifetime; controller.abort() removes them all simultaneously \u2014 eliminating bookkeeping arrays of remover functions. Supported broadly in evergreen engines for addEventListener across targets, the tidiest teardown pattern today.', c: "const ac = new AbortController();\nwindow.addEventListener('resize', onResize, { signal: ac.signal });\ndocument.addEventListener('keydown', onKey, { signal: ac.signal });\nel.addEventListener('click', onClick, { signal: ac.signal, once: true });\n\nfunction destroy() { ac.abort(); } // removes all three listeners" },
  { t: 'event.target versus currentTarget', d: 'target identifies where the event ORIGINATED (deepest node); currentTarget identifies what the LISTENER is attached to during dispatch. In delegation handlers currentTarget stays constant while target varies \u2014 comparing/routing between them is the essence of bubbled-event architecture. composedPath() exposes the full propagation route.' },
  { t: 'the composedPath and shadow piercing', d: 'event.composedPath() returns the propagation node array including shadow-internal ancestors; without composed:true, events stop at shadow boundaries hiding hosts. Reading path[0] reveals the TRUE deepest target even when target appears retargeted to the host element from outside observers.' },
  { t: 'MouseEvent coordinate families', d: 'clientX/Y relative to viewport; pageX/Y adds scroll offset; screenX/Y relative to physical monitor; offsetX/Y relative to target padding box. Dragging math typically wants client coords + rect deltas; canvas painting wants offsetX. Mixing families causes cursor-jump bugs \u2014 name the frame explicitly in code reviews.' },
  { t: 'KeyboardEvent key versus code', d: 'key reflects PRODUCED character respecting modifiers/layouts ("a", "A", "\u0430"); code reflects PHYSICAL key position (KeyA) independent of layout \u2014 correct choice for game controls/WASD, wrong for text shortcuts users expect localized. keyCode is deprecated legacy \u2014 migrate fully to key/code pairs.' },
  { t: 'InputEvent and composition awareness', d: 'beforeinput/input events carry inputType (insertText, deleteContentBackward, insertCompositionText...) enabling granular interception before edits apply (cancelable beforeinput). IME composition flows emit compositionstart/update/end \u2014 filtering those prevents autocomplete-on-every-romaji-letter bugs in Japanese/Chinese input.' },
  { t: 'PointerEvent unification', d: 'PointerEvents subsume mouse/touch/pen behind one interface adding pointerId, pointerType, pressure, tiltX/Y. setPointerCapture(pointerId) routes subsequent events to the capturing element even off-bounds \u2014 essential for reliable sliders/draggables. Feature-detect via window.PointerEvent before subscribing.' },
  { t: 'pointer capture practical slider', d: 'Capturing on pointerdown lets thumb tracking continue outside bounds; releasePointerCapture (or implicit release on pointerup) ends routing. Combine with touch-action:none CSS preventing scroll gesture theft during horizontal drags.', c: "thumb.addEventListener('pointerdown', e => {\n  thumb.setPointerCapture(e.pointerId);\n});\nthumb.addEventListener('pointermove', e => {\n  if (!thumb.hasPointerCapture(e.pointerId)) return;\n  const pct = clamp((e.clientX - trackRect.left) / trackRect.width, 0, 1);\n  fill.style.width = pct * 100 + '%';\n});" },
  { t: 'touch events versus pointer migration', d: 'TouchEvents (touchstart/move/end with touches lists) preceded PointerEvents; multi-touch scenarios index multiple simultaneous pointers by identifier. New code prefers pointer events + touch-action CSS; maintain touch handlers only for legacy Safari-era support or exotic multi-touch gestures needing per-finger history.' },
  { t: 'Drag and Drop API essentials', d: 'Native DnD: draggable=true sources fire dragstart (set effectAllowed, stash data via dataTransfer.setData), drop zones respond to dragover (MUST preventDefault to allow dropping) and drop (getData). DataTransfer.items expose files during drops. Infamous browser inconsistencies keep libraries popular, but zero-dependency file-drop zones work natively well.' },
  { t: 'DataTransfer file handling', d: 'drop event dataTransfer.files yields FileList of dropped files; items with kind==="file" enable directory traversal via webkitGetAsEntry where supported. Validate extensions/types server-side too \u2014 client MIME checks are advisory only. Upload via FormData appending File objects directly.', c: "zone.addEventListener('dragover', e => { e.preventDefault(); zone.classList.add('over'); });\nzone.addEventListener('dragleave', () => zone.classList.remove('over'));\nzone.addEventListener('drop', e => {\n  e.preventDefault();\n  const files = [...e.dataTransfer.files].filter(f => f.type.startsWith('image/'));\n  upload(files);\n});" },
  { t: 'ClipboardEvent read/write flows', d: 'navigator.clipboard.writeText/readText promise-based (secure contexts); paste events deliver clipboardData.getData("text/html") letting editors sanitize rich pastes BEFORE insertion. copy/cut events populate clipboardData.setData for customized copying (append attribution lines, clean formatting).' },
  { t: 'AnimationEvent and TransitionEvent hooks', d: 'animationstart/iteration/end and transitionrun/start/end/cancel expose timing hooks for sequencing JS with CSS motion (remove nodes after exit animations, chain steps). Listening for the right eventName plus propertyName guards crossfiring; fallback timers cover cancelled animations leaving orphaned states.' },
  { t: 'MutationObserver configuration', d: 'MutationObserver(callback, ) observes childList, attributes, characterData each optionally +subtree/+attributeOldValue; callback receives MutationRecord batches (addedNodes, removedNodes, attributeName, oldValue) queued as microtasks. disconnect() stops delivery; takeRecords() drains pending synchronously. Filter narrowly \u2014 whole-subtree attribute watching on huge trees costs real time.' },
  { t: 'ResizeObserver entry mechanics', d: 'ResizeObserver delivers contentRect (content-box size) and borderBox/devicePixelContentBoxSize variants per entry; loops occur when observing elements resized BY the callback \u2014 guard with size comparison or move measurement targets. Essential for chart redraws and container-responsive components media queries cannot express.', c: "const ro = new ResizeObserver(entries => {\n  for (const entry of entries) {\n    const { width } = entry.contentRect;\n    chart.resize(width);\n  }\n});\nro.observe(chartHost);" },
  { t: 'IntersectionObserver thresholds', d: 'Options: root (viewport default), rootMargin (shrinking/growing box, px/%), threshold array (0\u20131 ratios firing callbacks crossing points). Entries carry intersectionRatio, isIntersecting, boundingClientRect enabling lazy-load (threshold 0.01), infinite scroll sentinels, viewability analytics (0.5 for half-visible).' },
  { t: 'IntersectionObserver lazy images pattern', d: 'Observe placeholder imgs, on first intersect swap data-src into src, unobserve immediately \u2014 the classic progressive image loader predating loading=lazy, still superior when custom fade-ins or LQIP swaps accompany loads.', c: "const io = new IntersectionObserver((entries, obs) => {\n  for (const en of entries) {\n    if (!en.isIntersecting) continue;\n    const img = en.target;\n    img.src = img.dataset.src;\n    img.classList.add('loaded');\n    obs.unobserve(img);\n  }\n}, { rootMargin: '200px' });\ndocument.querySelectorAll('img[data-src]').forEach(img => io.observe(img));" },
  { t: 'PerformanceObserver entry types', d: 'PerformanceObserver watches buffered-or-live performance entries: paint (FCP), largest-contentful-paint, longtask, layout-shift, event timings, resource. buffered:true flag replays entries recorded before subscription \u2014 crucial for early metrics. Powers real-user monitoring SDKs without touching network timing APIs directly.' },
  { t: 'fetch Request object anatomy', d: 'new Request(url, init) bundles method/headers/body/mode/credentials/cache/signal into reusable descriptors \u2014 clone() duplicates for retry-with-body scenarios impossible on consumed requests. Passing Request into fetch inherits settings; interceptors wrap requests adding auth headers uniformly before dispatch.' },
  { t: 'Response interface consumption', d: 'Response exposes ok/status/statusText/headers (guarded immutability), body as ReadableStream, and one-shot consumers json()/text()/arrayBuffer()/blob()/formData(). Body reuse throws \u2014 clone() beforehand when logging AND consuming. redirected flag reveals hop counts; url finalizes post-redirect destination.' },
  { t: 'Headers interface behaviors', d: 'Headers implements append (comma-joins duplicates), set (replaces), get (joined), has, delete, forEach/entries iteration. Guarded headers (forbidden names like Host, Cookie manipulations) throw or ignore per mode \u2014 understanding guards clarifies mysterious header disappearances in service-worker proxies.' },
  { t: 'URL parsing power moves', d: 'new URL(relative, base) validates and decomposes hrefs: protocol/host/port/pathname/hash plus searchParams URLSearchParams with get/set/append/delete/sort and iterator. url.searchParams.set("page", 2); history.replaceState(null,"",url) updates address bars without reloads cleanly \u2014 filter-state-in-URL made trivial.' },
  { t: 'URLSearchParams encoding discipline', d: 'SearchParams percent-encodes correctly per application/x-www-form-urlencoded (spaces as +); URL handles component encoding differences. Manual concatenation breaks on ampersands/unicode \u2014 always route dynamic values through the API. sort() canonicalizes query order enabling cache-key normalization.' },
  { t: 'WebSocket interface lifecycle', d: 'new WebSocket(url[, protocols]) opens readyState journey CONNECTING\u2192OPEN\u2192CLOSING/CLOSED with open/message/close/error events; send() throws before OPEN. Binary via binaryType blob/arraybuffer. Close codes (1000 normal, 1006 abnormal...) diagnose disconnections; heartbeat pings detect half-open TCP zombies.' },
  { t: 'EventSource SSE interface', d: 'EventSource(url) subscribes to text/event-stream receiving message events plus NAMED events (addEventListener("ticket")) parsed from event: fields; readyState mirrors reconnection status, auto-retry built-in with Last-Event-ID resume headers. close() terminates permanently. Server-sent one-way streaming without websocket ceremony.' },
  { t: 'navigator.sendBeacon fire and forget', d: 'sendBeacon(url, data) queues guaranteed-attempt background transmission surviving page unload \u2014 THE analytics flush on pagehide/visibilitychange when fetch keepalive alternatives feel heavy. Payload limits (~64KB) and no response access; pair with navigator.onLine heuristics cautiously since offline beacons vanish silently.' },
  { t: 'the Storage interface quotas', d: 'localStorage/sessionStorage implement getItem/setItem/removeItem/clear/key/length storing UTF-16 strings (~5MB origin-wide); quota-exceeded throws on setItem \u2014 wrap writes defensively. Storage events broadcast cross-tab mutations (same origin, OTHER tabs only) enabling sync patterns without BroadcastChannel.' },
  { t: 'storage event cross-tab sync', d: "window.addEventListener('storage', e => { e.key, e.newValue, e.oldValue, e.url }) fires in OTHER tabs after localStorage mutations \u2014 theme/login-state propagation minus polling. Values arrive as strings; JSON parse defensively since corrupted writers happen.", c: "window.addEventListener('storage', e => {\n  if (e.key !== 'theme') return;\n  applyTheme(JSON.parse(e.newValue ?? '\"light\"'));\n});\n\n// writer tab:\nlocalStorage.setItem('theme', JSON.stringify('dark'));" },
  { t: 'BroadcastChannel messaging', d: 'new BroadcastChannel(name) connects same-origin tabs/frames sharing the channel: postMessage broadcasts structured clones to siblings EXCLUDING sender, onmessage receives. Cleaner than storage-event hacks for tab coordination (logout everywhere, cache invalidation fanout). close() releases the binding.' },
  { t: 'IndexedDB transactional shape', d: 'indexedDB.open(name, version) triggers upgradeneeded for schema creation; operations run inside IDBTransactions (readonly/readwrite modes scoping object stores) whose complete/error events gate success; IDBObjectStore put/get/getAll/delete/index queries return IDBRequests resolving asynchronously. All async surfaces also ship promise wrappers (indexedDB.withTransactionStyle libs or native in modern engines).' },
  { t: 'document.cookie constraints', d: 'Cookie reads return ONE semicolon-joined string needing manual parsing; writes set single cookies (name=value; Path; Max-Age; Secure; HttpOnly invisible to JS; SameSite). Size ~4KB per cookie. Prefer CookieStore API (async get/set/getAll with nice types) where available; keep cookies for server-needed state only \u2014 storage APIs own the rest.' },
  { t: 'Selection API fundamentals', d: 'window.getSelection() returns Selection with anchorNode/Offset (start) focusNode/Offset (end, direction-aware), toString(), rangeCount, and range mutation via getRangeAt/addRange. Collapse to caret, select-all, extend \u2014 powering toolbar positioning, quote-sharing, and editor commands against user highlights.' },
  { t: 'selection change tracking', d: "document.addEventListener('selectionchange', ...) fires as carets/highlights move (document-scoped); debounce before measuring getSelection ranges for floating-toolbar UX \u2014 rapid-fire events otherwise thrash layout reads.", c: "let t;\ndocument.addEventListener('selectionchange', () => {\n  clearTimeout(t);\n  t = setTimeout(() => {\n    const sel = window.getSelection();\n    if (sel.isCollapsed || !sel.toString().trim()) return hideToolbar();\n    const rect = sel.getRangeAt(0).getBoundingClientRect();\n    placeToolbar(rect);\n  }, 150);\n});" },
  { t: 'contenteditable foundations', d: 'contenteditable="true"/plaintext-only turns elements editable exposing caret/selection machinery; inherit value propagates from parents. Browser-generated markup diverges wildly (spans, divs, br debates) \u2014 production editors normalize via execCommand successors or libraries (ProseMirror/Lexical/TipTap) built atop these primitives.' },
  { t: 'execCommand deprecation reality', d: 'document.execCommand("bold"/"insertHTML"/...) drove rich-text editing historically; formally deprecated yet universally supported \u2014 acceptable pragmatically, risky strategically. New implementations target beforeinput interception + manual DOM surgery, or adopt editor frameworks abstracting the mess away entirely.' },
  { t: 'beforeinput interception power', d: 'Cancelable beforeinput events (inputType + data + TargetRanges) let editors veto/redirect edits BEFORE mutation \u2014 markdown shortcuts, mention triggers, paste sanitization \u2014 without post-hoc DOM diffing. The foundation of modern controlled-contenteditable architectures.' },
  { t: 'getBoundingClientRect precision', d: 'Returns DOMRect with x/y/width/height/top/right/bottom/left in viewport coordinates INCLUDING transforms; width/height round to integers in some engines \u2014 prefer right-left for fractional precision. Forces layout flush; batch reads. Scroll compensation: rect.top + scrollY yields page coordinates.' },
  { t: 'offset dimension family', d: 'offsetWidth/Height measure layout boxes INCLUDING borders/padding excluding margins/transforms; offsetLeft/Top position against offsetParent (nearest positioned ancestor or body). Table-cell quirks and inline-element zero-offsets surprise \u2014 rects usually answer layout questions more reliably than offset probes.' },
  { t: 'client dimension family', d: 'clientWidth/Height report visible interior (padding included, borders/scrollbars excluded); clientTop/Left give border widths. Scrollbar presence subtracts from clients \u2014 detecting overflow compares scrollWidth>clientWidth. Zero-client-size indicates display:none subtrees, a common mount-time probe.' },
  { t: 'scroll dimension family', d: 'scrollWidth/Height total content extents; scrollTop/Left current offsets assignable for programmatic scrolling (clamped). Reading scrollTop forces layout \u2014 virtualized scrollers cache aggressively. Overscroll values can go negative with rubber-banding (iOS) \u2014 clamp before math.' },
  { t: 'scrollIntoView options block inline', d: 'scrollIntoView({ behavior:"smooth", block:"start|center|nearest|end", inline }) navigates ancestors as needed; nearest avoids gratuitous jumps. Return-promise support exists in Chromium only \u2014 await defensively. Respect reduced-motion by choosing auto behavior conditionally.' },
  { t: 'elementFromPoint hit testing', d: 'document.elementFromPoint(x, y) returns topmost rendered element at viewport coordinates or null (outside/none-rendered); elementsFromPoint stacks the full z-order \u2014 drag-drop targeting, custom cursors, and click-through debugging lean on these constantly. Pointer-events:none elements are transparent to hits.' },
  { t: 'caretPositionFromPoint text targeting', d: 'document.caretPositionFromPoint(x,y) (standard) / caretRangeFromPoint (webkit legacy) resolve the text insertion point under coordinates \u2014 drop-text-here indicators, click-to-position editors, annotation anchors. Normalize across engines with a tiny helper preferring standards where present.' },
  { t: 'matchMedia reactive queries', d: 'window.matchMedia(query) yields MediaQueryList with matches boolean plus change events \u2014 subscribe/unsubscribe pattern for JS-side responsive logic (prefers-reduced-motion, dark scheme, hover capability) mirroring CSS conditions exactly.', c: "const mq = window.matchMedia('(prefers-reduced-motion: reduce)');\nconst apply = () => animationsEnabled = !mq.matches;\napply();\nmq.addEventListener('change', apply);" },
  { t: 'visualViewport mobile realities', d: 'window.visualViewport exposes ACTUAL visible area (excluding pinch-zoom shrinkage, on-screen keyboards): scale, offsetLeft/Top, height + resize/scroll events. Fixed bottom bars colliding with keyboards solve by translating with keyboard delta derived from height changes \u2014 impossible with plain window resize alone.' },
  { t: 'the HTMLDialogElement interface', d: '<dialog>.showModal() renders top-layer (above ALL stacking contexts) with ::backdrop, focus trapping, Escape-cancel (cancel + close events); show() runs non-modal inline instead. returnValue captures closing argument; close(result) passes it. Native accessible dialogs finally without focus-trap dependency juggling.' },
  { t: 'HTMLTemplateElement inert content', d: '<template> children live in an inert document fragment \u2014 scripts don\u2019t run, images don\u2019t load, queries find nothing until .content clones mount. Ideal stamp factories: author markup in HTML (syntax-highlighted, designer-editable), instantiate via cloneNode(true) filling dataset slots.' },
  { t: 'customElements define lifecycle', d: 'customElements.define(tag-name, class) registers autonomous/customized built-ins; callbacks constructor (no children!), connectedCallback, disconnectedCallback, adoptedCallback, attributeChangedCallback(observedAttributes gated) orchestrate lifecycle. Upgrades apply retroactively to existing matching nodes \u2014 definitions may postdate markup.' },
  { t: 'attachShadow encapsulation', d: 'element.attachShadow({ mode:"open"|"closed" }) returns ShadowRoot hosting isolated styles/DOM; open exposes via el.shadowRoot, closed severs (mostly cosmetic privacy). Slot distribution projects light-DOM children; :host styling and CSS custom properties pierce boundaries as sanctioned theming channels.' },
  { t: 'assignedSlots and slotchange', d: '<slot> elements fire slotchange when distributed nodes change; slot.assignedNodes({flatten:true}) inspects projections \u2014 component authors react to consumer-provided content dynamically (empty-slot fallbacks, per-item decoration). Name-matched slots route multiple projection channels.' },
  { t: 'popover attribute API', d: 'popover="auto|manual" + popovertarget buttons declaratively spawn top-layer popovers with light-dismiss (auto), backdrops, and showPopover/hidePopover/togglePopover imperative controls. beforetoggle/toggle events expose state transitions. Replaces bespoke dropdown/popover plumbing with platform accessibility baked in.' },
  { t: 'Fullscreen API flow', d: 'element.requestFullscreen({navigationUI}) enters (must be user-gesture), document.exitFullscreen() leaves; fullscreenchange/fullscreenerror events track outcomes; document.fullscreenElement identifies current holder. Vendor prefixes linger in Safari iOS (webkitEnterFullscreen video-only quirks) \u2014 feature-detect patiently.' },
  { t: 'Notification permission cycle', d: 'Notification.requestPermission() (promise or callback legacy) gates construction; new Notification(title, {body, icon, tag}) posts only when granted; notification.onclick focuses windows via clients.matchAll in SW context. Permission denials persist \u2014 design graceful no-notification UX rather than nagging re-prompts.' },
  { t: 'History pushState responsibilities', d: 'history.pushState(state, title, url) pushes entries WITHOUT loading/navigation events \u2014 SPAs pair with popstate listeners reading event.state to restore views; replaceState amends current entries. State size caps (~640KB serialized) suit ids/filters, not payloads. Browsers skip scroll restoration on pushes \u2014 manage manually.' },
  { t: 'Location interface properties', d: 'window.location decomposes: href (full), origin, protocol, host (with port) vs hostname, pathname, search, hash; assign()/replace() navigate (replace skips history entry); reload(force?) refetches. Assignments trigger navigations synchronously-ish \u2014 validate URLs before redirecting on user-supplied data (open-redirect vulnerabilities).' },
  { t: 'Navigator surface highlights', d: 'navigator exposes userAgent (unreliable), language/languages, onLine (optimistic), hardwareConcurrency, deviceMemory hints, clipboard, geolocation, mediaDevices, credentials, serviceWorker container, storage estimate/persist \u2014 capability hub for feature detection rather than identity sniffing.' },
  { t: 'postMessage structured exchange', d: 'otherWindow.postMessage(message, targetOrigin) queues asynchronous MessageEvents (data, origin, source) \u2014 cross-frame/worker communication backbone. ALWAYS verify event.origin against allowlists before trusting data, and pass explicit origins instead of "*": the two-line discipline preventing postMessage-based attacks.' },
  { t: 'MessageChannel private pipelines', d: 'new MessageChannel() creates port1/port2 paired pipes; transferring ports via postMessage(msg, [port2]) establishes dedicated two-way lanes bypassing global message noise \u2014 service workers reply privately, iframe embeds negotiate handshakes, streaming chunk protocols multiplex cleanly.' },
  { t: 'Worker constructor essentials', d: 'new Worker(url, {type:"module"}) spins background threads; postMessage structured-clones (Transferables zero-copy ArrayBuffers), terminate() kills, error/messageerror surface failures. Workers lack DOM; self.importScripts (classic) or imports (module) load deps. Offload JSON mega-parse, hashing, image filters preserving UI fluidity.' },
  { t: 'CanvasRenderingContext2D overview', d: 'canvas.getContext("2d") yields immediate-mode drawing: paths (beginPath/moveTo/arc/fill/stroke), transforms (translate/rotate/scale/save/restore), pixels (drawImage/getImageData), gradients/shadows. State machine discipline \u2014 save/restore around mutations; DPR scaling via canvas.width = cssWidth * devicePixelRatio then ctx.scale keeps art crisp.' },
  { t: 'aria attribute reflection', d: 'ARIA roles/states map to attributes (aria-expanded, role="switch") with modern IDL reflections (el.ariaExpanded = "true") typed as strings. Toggle BOTH visual state and ARIA together in one update function \u2014 divergence between painted and announced state is the most common a11y defect shipped.' },
  { t: 'ElementInternals customization', d: 'custom elements attachShadow-free can bind internals via ElementInternals: setFormValue participates in forms, states adds custom :--state pseudo classes, setValidity reports constraint violations natively \u2014 bridging custom widgets INTO platform semantics (form association, validation UI) without shadow DOM overhead.' },
  { t: 'requestAnimationFrame timing contract', d: 'rAF callbacks run before NEXT repaint (~16.7ms cadence) receiving DOMHighResTimeStamp; consecutive calls chain animation loops; cancellations via cancelAnimationFrame(handle). Hidden tabs throttle to near-zero \u2014 pause work, resume on visibilitychange, compute deltas from timestamps not assumed frame durations.' },
  { t: 'structuredClone deep copy rules', d: 'window.structuredClone(value, {transfer}) duplicates nested structures (cycles OK, Map/Set/Date/RegExp/TypedArrays preserved) while THROWING on functions, DOM nodes, prototypes/class instances (become plain objects), and property descriptors. transfer list moves buffers instead of copying \u2014 the sanctioned deep-clone primitive replacing JSON hacks.' },
  { t: 'queueMicrotask scheduling niche', d: 'queueMicrotask(fn) defers to microtask checkpoint \u2014 after current synchronous execution, before ANY macrotask/timers/rendering. Use for batching state flushes, guaranteeing ordered deferred initialization ahead of setTimeout-based rivals; avoid long microtask chains starving rendering.' },
  { t: 'requestIdleCallback budgeted work', d: 'requestIdleCallback(cb, {timeout}) invokes cb(deadline) during idle windows; deadline.timeRemaining() guides chunk sizes yielding cooperatively; timeout guarantees eventual execution despite busy pages. Fallback shim onto setTimeout for Safari parity. Perfect for indexing/prefetch/analytics flushes that must never compete with interactions.' },
  { t: 'Image constructor prefetch pattern', d: 'const img = new Image(); img.src = url; primes caches without mounting \u2014 hover-preloading routes/images, measuring intrinsic sizes via onload naturalWidth before layout decisions, decoding() promises coordinating decode-before-display eliminating flash tears on galleries.' },
];
