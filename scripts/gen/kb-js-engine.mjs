// JavaScript engine internals, patterns & tricky semantics — part 3
export default [
  { t: 'execution context and the call stack', d: 'Each function invocation creates an execution context holding locals, scope chain, and this, pushed onto the call stack and popped on return. Stack overflow occurs when recursion exceeds capacity. Synchronous single-stack execution is why blocking loops freeze everything \u2014 async APIs exist to defer work off this stack.' },
  { t: 'lexical vs dynamic scoping', d: 'JavaScript resolves identifiers lexically: a function\u2019s scope chain is fixed by where it was written, not where it runs. That is why callbacks retain access to defining-scope variables even when invoked elsewhere, and why passing functions around preserves their closure environment predictably.' },
  { t: 'scope chain lookup', d: 'Identifier resolution walks outward through nested lexical environments until found, throwing ReferenceError at global miss. Shadowing occurs when inner declarations reuse outer names, hiding them. Deep chains cost lookup time marginally, but clarity argues for shallow nesting and explicit parameter passing over long-distance variable capture.' },
  { t: 'memory leaks common causes', d: 'Top leak sources: forgotten setInterval/setTimeout without cleanup, detached DOM nodes retained in JS variables, ever-growing caches/maps keyed strongly, event listeners never removed, and closures capturing large scopes unnecessarily. Use WeakMap/WeakRef for metadata, clear timers on teardown, and profile heap snapshots comparing allocations over time.' },
  { t: 'garbage collection basics', d: 'Modern engines use generational marking: new objects live in nursery space collected frequently, survivors promote to old space collected via incremental/concurrent mark-sweep with compaction. Reachability from roots determines survival \u2014 deleting references enables collection, but you cannot force GC manually in standard JS.' },
  { t: 'module pattern and revealing module pattern', d: 'The module pattern wraps privates inside a function returning a public API object; the revealing variant defines everything privately first then reveals chosen references, making the surface area obvious at return site. Pre-ESM staple, still useful for widget encapsulation without build tooling.' },
  { t: 'factory functions vs constructors', d: 'Factories return plain objects built however you like \u2014 no new keyword, easy composition, private state via closures \u2014 while constructors/classes share prototypes saving memory across many instances and enabling instanceof polymorphism. Choose factories for varied shapes/one-offs, classes for large homogeneous populations.' },
  { t: 'composition over inheritance', d: 'Composing small focused functions/objects beats deep class hierarchies because behavior changes locally without rippling super chains. Techniques: mixin functions, wrapper delegation, strategy injection, pipe composition. Inheritance still fits genuine is-a taxonomies (Error subclasses, React.Component historically) but should stay shallow.' },
  { t: 'the arguments object', d: 'Non-arrow functions receive array-like arguments with length and indexed access but no array methods; converting via [...arguments] or Array.from fixes that. Arrow functions see enclosing arguments lexically. Rest parameters supersede it in modern signatures, providing true arrays with named intent.' },
  { t: 'named function expressions', d: 'Naming a function expression gives the internal name for recursion and stack traces without polluting outer scope: const f = function fact(n){ ... }. The binding is read-only inside. Anonymous expressions produce anonymous stack frames complicating debugging, so prefer names except trivial inline callbacks.' },
  { t: 'function.length property', d: 'function.length reports parameter count before the first default/rest parameter \u2014 useful for arity-based dispatch, argument validation, and library APIs adapting behavior. Changing signatures alters length, so code depending on it breaks silently during refactors; treat as informational metadata rather than contract.' },
  { t: 'method chaining design', d: 'Fluent APIs chain methods by returning this (or new immutable instances) after each operation, producing declarative pipelines: query.select().where().limit(). Mutable-this chaining couples calls dangerously under shared state; immutable chaining (returning copies) suits concurrent contexts like string/array methods and state libraries.' },
  { t: 'typeof null legacy bug', d: 'typeof null returns "object" due to the original engine tagging null with the object type bits \u2014 fixing would break the web. Detect null via value === null explicitly. This quirk anchors interview trivia and reminds engineers that backward compatibility constrains language evolution permanently.' },
  { t: 'primitive wrappers', d: 'Accessing properties on primitives auto-boxes them into temporary String/Number/Boolean objects discarded immediately, which is why (1).toFixed works yet storing new String("a") creates trouble (truthy object, typeof "object", == vs === surprises). Never instantiate wrappers manually; let coercion happen implicitly.' },
  { t: 'pass by sharing semantics', d: 'JS passes references-to-values: reassigning parameters inside functions never affects caller variables, but mutating shared objects/arrays does. This hybrid explains why defensive cloning at boundaries matters for primitives-like safety with objects, and why const does not prevent content mutation.' },
  { t: 'mutation hazards in shared state', d: 'Aliased mutations cause spooky action at a distance: two variables referencing one array see each other\u2019s pushes/sorts. Copy before mutate (slice/spread/structuredClone) or adopt immutable update discipline. Bugs cluster around sort/splice/reverse because they mutate in place while looking innocent.' },
  { t: 'default parameter evaluation order', d: 'Defaults evaluate left-to-right and may reference earlier params: function f(a, b = a * 2). Each call recomputes expressions \u2014 including side-effectful factory calls \u2014 so keep defaults pure or memoized. Accessing later params in earlier defaults throws TDZ ReferenceError, mirroring let semantics.' },
  { t: 'setTimeout in loops explained', d: 'setTimeout callbacks run after the loop completes, reading whatever the captured variable finally holds (var shares one binding \u2192 last value; let makes per-iteration bindings \u2192 sequential values). The event loop cannot interleave timer callbacks mid-loop because synchronous code always finishes first.' },
  { t: 'this in event handlers', d: 'Traditional function handlers receive this === currentTarget element; arrow-function class properties capture surrounding instance instead \u2014 convenient for React classes, confusing for vanilla DOM where you lose element context. Inline arrow wrappers preserve both needs: el.addEventListener("click", () => this.handleClick(e)).' },
  { t: 'bind chaining limitations', d: 'Bound functions lock this permanently \u2014 rebinding a bound function has no effect since bind output ignores subsequent call/apply this arguments. They also lack prototype (cannot new), carry length trimmed by bound args, and memoize bound results to avoid unbounded wrapper allocation in hot paths.' },
  { t: 'new.target', d: 'Inside constructors new.target is the invoked constructor (undefined if called plainly), enabling dual-purpose functions that self-heal missing new: if (!new.target) return new Foo(...). ES2015+ classes throw without new automatically, so this guard mainly serves legacy-style constructor functions and subclass detection.' },
  { t: 'eval dangers', d: 'eval executes arbitrary strings with full local scope access, destroying optimizations, security boundaries, and tooling statics (bundlers cannot analyze it). Indirect eval loses local scope but still risks injection. JSON.parse/template alternatives cover nearly all legitimate historical uses; CSP blocks eval entirely by default.' },
  { t: 'ASI semicolon insertion gotchas', d: 'Automatic semicolon insertion only inserts at line breaks when the next token cannot continue the statement, causing classic bugs: return\\n{...} returns undefined, and lines starting with ( or [ join the previous expression. Formatters plus consistent explicit semicolons (or no-semicolons with leading-guard style) neutralize ASI surprises.' },
  { t: 'bitwise operators are 32-bit', d: 'All bitwise ops coerce operands to signed 32-bit integers, silently truncating larger numbers and NaN\u21920 \u2014 handy for fast floor (|0), hashing, flags; dangerous for big values and ids. >>> treats operands unsigned. Prefer Math.floor for clarity unless bit-twiddling performance genuinely matters.' },
  { t: 'unary plus and conversion tricks', d: '+value converts to number faster than Number() and shorter than parseFloat, mapping strings, booleans (true\u21921), null\u21920, undefined\u2192NaN. It fails visibly on malformed input returning NaN, making it decent strict parsing. Combined with Date it yields timestamps: +new Date() though Date.now() reads cleaner.' },
  { t: 'Object.is vs === edge cases', d: 'Object.is(NaN, NaN) is true and Object.is(+0, -0) is false \u2014 exact opposite of === on those edges \u2014 otherwise identical semantics. Useful in equality helpers, React\u2019s comparison discussions, and numeric algorithms distinguishing signed zero. Everyday code rarely needs it but interviews probe the distinction.' },
  { t: 'in operator and index checks', d: '"key" in obj tests prototype-inclusive key existence (true even when value undefined); hasOwn limits to own properties. On arrays "0" in arr checks index presence surviving holes. Distinguish existence-vs-truthiness carefully when optional configs use undefined sentinel values.' },
  { t: 'delete operator behavior', d: 'delete removes own configurable properties returning boolean, leaving holes in arrays (length unchanged!) rather than compacting \u2014 use splice/filter for elements. Deleting non-configurable properties fails (false/falsey in strict mode throws). Hot paths suffer perf penalties since engines deoptimize shape caches after deletions.' },
  { t: 'for-in pitfalls with inherited props', d: 'for-in walks the whole enumerable prototype chain, so library-augmented Object.prototypes leak into loops \u2014 historically guarded by hasOwnProperty filters. Modern practice avoids for-in for data entirely: Object.keys/values/entries iterate own keys deterministically (string-keyed insertion order; integer-like keys ascending).' },
  { t: 'object key ordering rules', d: 'Integer-like keys enumerate ascending numerically first; string keys follow insertion order; Symbols last in insertion order. This hybrid ordering surprises developers expecting pure insertion sequence ({"2":x,"1":y}). Numeric ids stored as strings reorder unexpectedly \u2014 pad or prefix when order fidelity matters.' },
  { t: 'creating dictionary-safe objects', d: 'Object.create(null) yields prototype-less maps immune to key collisions ("constructor", "__proto__") and accidental inherited method calls, ideal for user-input-keyed dictionaries. JSON.parse output inherits normally, so sanitize or migrate to Map when keys come from untrusted sources. __proto__ setter attacks exploit literal-object prototypes.' },
  { t: '__proto__ vs prototype', d: 'Every object carries hidden [[Prototype]] (__proto__ accessor); constructor functions additionally expose .prototype used solely as blueprint for instances made via new. Confusing them is canonical junior confusion: fn.prototype exists on all functions, obj.__proto__ points to construction blueprint or null.' },
  { t: 'instanceof across iframes', d: 'Each iframe/realm owns distinct globals and constructors, so [] from another realm fails Array.isArray-free instanceof checks. Cross-realm safe utilities: Array.isArray, Object.prototype.toString.call tags, duck typing, or structuredClone transfer. Libraries must avoid identity assumptions when operating on foreign-realm inputs.' },
  { t: 'deep equality implementation', d: 'Robust deep-equal compares types, handles NaN/+0 edges, recurses through arrays/objects checking key sets symmetrically, treats Dates/RegExps specially, and guards cycles via seen-map (WeakSet). Production code imports fast-deep-equal/lodash.isEqual rather than reinventing; interviews expect the recursive skeleton plus cycle handling awareness.' },
  { t: 'flatten implementation strategies', d: 'Recursive concat spreads handle arbitrary depth; iterative stacks avoid recursion limits; flat(Infinity) is native one-liner. For tree-shaped domain data custom traversal often outperforms generic flattening by preserving parent links. Know depth-limited versus deep semantics when normalizing API responses.' },
  { t: 'once() utility implementation', d: 'once wraps a function so subsequent calls replay the first result: closure boolean flag plus cached value. Edge cases include preserving this and arguments of first call only. Useful for initialization guards (singleton connections, lazy singletons) replacing scattered initialized booleans throughout codebases.' },
  { t: 'sleep/delay helper implementations', d: 'const sleep = ms => new Promise(r => setTimeout(r, ms)); await sleep pauses async flow cooperatively. Add AbortSignal support for cancellable delays in retry loops. Avoid busy-wait loops starving the event loop; timers yield control letting UI breathe between steps.' },
  { t: 'retry with exponential backoff', d: 'Retry wrappers attempt operations N times multiplying delay (base ** attempt) with jitter preventing thundering herds, aborting on non-retryable errors. Combine with timeout-per-attempt and overall deadline via Promise.race/AbortController. Idempotency matters \u2014 never blind-retry POSTs that create resources.' },
  { t: 'promisify conversion pattern', d: 'Wrapping callback-style APIs: const promisify = fn => (...args) => new Promise((res, rej) => fn(...args, (err, data) => err ? rej(err) : res(data))). Node util.promisify ships production-grade handling including multi-args and symbols. Modern Node fs/promises shows ecosystem-wide migration results.' },
  { t: 'event emitter pub-sub implementation', d: 'Minimal emitter stores Map<event, Set<handler>>; on adds, emit iterates copying set (handlers may unsubscribe mid-flight), off removes, once wraps auto-unsubscribing. Support wildcard or async handlers deliberately. Memory leaks lurk when emitters outlive subscribers \u2014 provide dispose hooks and weak patterns where sensible.' },
  { t: 'LRU cache implementation concepts', d: 'LRU evicts least-recently-used entries beyond capacity using Map\u2019s ordered nature: get/delete+re-set moves keys to end; eviction deletes first key when size exceeds limit. O(1) amortized ops. Interviewers love it; production uses Map-based LRU for memoization bounds and client-side response caching.' },
  { t: 'memoize with WeakMap keys', d: 'Keying memo caches by object arguments via WeakMap lets garbage collection reclaim entries when callers drop references, bounding memory naturally for per-instance computations. Composite keys serialize carefully (order-sensitive JSON) or nest WeakMaps per argument. Never cache impure results \u2014 randomness/time break correctness.' },
  { t: 'clamp/random utility functions', d: 'Clamp constrains values: Math.min(Math.max(v, lo), hi) \u2014 essential for sliders, pagination indices. Random ranges: lo + Math.random()*(hi-lo); integer variants floor appropriately; crypto.getRandomValues for security contexts since Math.random is not cryptographically secure. Fisher-Yates shuffles arrays uniformly.' },
  { t: 'unique id generation approaches', d: 'Options ascend in rigor: counter++ (session-only), Math.random().toString(36) (collisions possible), crypto.randomUUID() (standard RFC4122 v4, modern browsers/Node), timestamp+counter hybrids (sortability). React lists need stable ids from data, not generated-on-render ones which break reconciliation.' },
  { t: 'debounce leading/trailing options', d: 'Full debounce config chooses leading-edge fire (instant feedback), trailing-edge fire (final state), or both. Leading+trailing risks double-fires needing suppression window. Search boxes want trailing; buttons preventing spam want leading. Understanding these modes distinguishes senior utility-library answers from naive timer resets.' },
  { t: 'throttling scroll with passive listeners', d: 'Passive listeners ({passive:true}) promise no preventDefault, unlocking immediate scrolling instead of waiting handler completion \u2014 default for touch/wheel in Chrome. Combine passive wheel/touch listeners with rAF-batched position reads to eliminate jank; never passive where you cancel defaults.' },
  { t: 'requestIdleCallback usage', d: 'requestIdleCallback schedules low-priority work during idle periods (deadline.timeRemaining() guides chunking), ideal for analytics flushing, prefetch hints, non-urgent indexing. Fallback shim defers via setTimeout since Safari lagged support. Always timebox tasks yielding regularly so idle work never degrades interaction latency.' },
  { t: 'document fragments batch inserts', d: 'DocumentFragment accumulates nodes off-layout, appending once triggers single reflow versus per-node thrash. Modern alternative builds HTML strings/cloneNode batches. Fragments also serve as lightweight containers moved wholesale. Benchmark before optimizing \u2014 engines optimized direct-append paths substantially, but bulk list building still benefits.' },
  { t: 'innerHTML vs textContent security', d: 'textContent inserts escaped text safely; innerHTML parses markup executing scripts/event-handler attributes embedded in unsanitized input \u2014 the XSS vector. Sanitize with DOMPurify when HTML is required, prefer textContent/createElement otherwise. Template engines escape by default; raw-output escapes hatches bypass that protection.' },
  { t: 'dataset attributes bridging DOM state', d: 'data-* attributes expose camelCased via element.dataset (data-user-id \u2192 dataset.userId), carrying component state in markup for CSS hooks ([data-state="open"]) and delegated handlers reading targets. Strings only \u2014 parse numbers/JSON explicitly. Overuse turns DOM into database; keep authoritative state in JS.' },
  { t: 'classList manipulation', d: 'classList.add/remove/toggle/replace/contains manage classes safely versus className string surgery clobbering siblings. toggle\u2019s second arg forces states declaratively matching boolean conditions. Multiple args accepted by add/remove. CSS-driven state via classes beats inline style mutation for maintainable theming and transitions.' },
  { t: 'getBoundingClientRect and layout forcing', d: 'Reading getBoundingClientRect/offsetHeight forces synchronous layout flush; interleaved writes+reads thrash. Batch reads, then writes, or wrap write phases in rAF. FastLayout pattern: measure-all, mutate-all, measure-next-frame. DevTools Performance panel exposes purple layout blocks exposing offenders.' },
  { t: 'focus management and tabindex', d: 'Programmatic focus (el.focus({preventScroll})) directs keyboards/readers after route/modal changes; tabindex="0" joins natural order, positive values create anti-pattern tab-order traps, -1 enables programmatic-only focus. Modals trap focus cycling Tab within bounds and restore trigger focus on close \u2014 accessibility fundamentals interviews probe.' },
  { t: 'scrollIntoView smooth scrolling', d: 'scrollIntoView({behavior:"smooth", block:"nearest"}) navigates users to content respecting container overflow; nearest prevents needless ancestor jumps. Respect prefers-reduced-motion by choosing auto behavior conditionally. Anchor-click interception combines history.pushState with manual scrolling for SPA-friendly hash navigation without native jump flicker.' },
  { t: 'clipboard API', d: 'navigator.clipboard.writeText/readText promise-based secure-context API replacing execCommand hacks; permissions gate reads while writes usually allow on user gesture. Fallbacks for http contexts use temporary textarea+execCommand. Paste-event interception enables sanitizing rich pastes before insertion into editors.' },
  { t: 'geolocation API essentials', d: 'navigator.geolocation.getCurrentPosition/watchPosition require HTTPS plus user permission prompts; timeouts/maximumAge tune freshness versus latency, highAccuracy trades battery. Handle denial gracefully with manual-entry fallbacks \u2014 permission UX failures strand users. Coordinates are WGS84; distance math needs haversine, not Euclidean.' },
  { t: 'canvas basics versus SVG', d: 'Canvas raster-draws via immediate-mode 2D/WebGL contexts \u2014 fast for thousands of particles, pixel effects; SVG retains vector scene-graph enabling CSS animation, hit-testing per node, crisp zooming \u2014 better for icons/charts under hundreds of nodes. Hybrid apps draw canvas layers under SVG overlays capturing events.' },
];
