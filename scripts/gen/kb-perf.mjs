// Frontend performance & mastery topics
export default [
  { t: 'critical rendering path', d: 'The CRP sequences HTML parse \u2192 DOM construction, CSS parse \u2192 CSSOM, their merge into render tree, layout, paint. Blocking resources stall first paint; optimizing means minimizing bytes, round trips, and render-blocking surface (defer scripts, inline critical CSS, preload fonts).' },
  { t: 'render tree formation', d: 'Render tree combines DOM nodes with matched styles excluding display:none subtrees and including pseudo-elements; visibility:hidden stays rendered. Layout computes geometry per node; changes forcing reflow cascade through affected trees. Understanding what enters the tree explains why some hidden elements still cost layout.' },
  { t: 'defer vs async scripts', d: 'async downloads parallel executing immediately on arrival (order unpredictable); defer downloads parallel executing in order before DOMContentLoaded. Both unblock parsing versus synchronous scripts halting it. App bundles want defer (or module default); independent analytics tolerate async.' },
  { t: 'module scripts behavior', d: '<script type="module"> defers automatically, runs strict, gets own scope, supports imports, executes after parsing but before DOMContentLoaded fires. Legacy UMD tags converted to modules gain deferred semantics free. nomodule serves fallbacks to ancient browsers when dual-shipping.' },
  { t: 'preload prefetch preconnect', d: 'preload fetches critical current-navigation resources early (fonts, hero image) with as-type priority; prefetch hints likely NEXT navigations at idle; preconnect warms DNS+TCP+TLS to third-party origins cutting handshake latency. dns-prefetch is the cheap legacy subset of preconnect.' },
  { t: 'fetchpriority attribute', d: 'fetchpriority=high|low|auto adjusts resource priority beyond browser heuristics: hero LCP image high, below-fold carousel images low. Pairs with loading=eager for LCP heroes contradicting blanket lazy-loading advice. Chrome DevTools network waterfall exposes computed priorities for tuning.' },
  { t: 'modulepreload', d: 'modulepreload fetches AND parses/compiles module graphs ahead resolving dependency chains, unlike plain preload\u2019s fetch-only. Critical route chunks listed in head eliminate cold-start module waterfall cascades that dynamic imports create serially.' },
  { t: 'resource waterfalls reduction', d: 'Waterfalls chain request-after-request multiplying latency; fixes bundle related requests, preload known-critical chains, HTTP/2 multiplexing parallelizes anyway, inline tiny critical assets, and server-push era lessons taught us guessing wastes bytes \u2014 measure then hint selectively.' },
  { t: 'HTTP/2 multiplexing benefits', d: 'H2 streams multiple requests over one connection killing head-of-line blocking at HTTP level, making sprite-sheets/domain-sharding obsolete; server push failed practically. H3/QUIC extends gains fixing TCP-level HOL and improving lossy mobile networks via UDP transport.' },
  { t: 'HTTP/3 QUIC overview', d: 'QUIC over UDP integrates TLS 1.3 handshakes slashing connection setup RTTs, survives IP changes (wifi\u2192cellular handoffs), and avoids TCP head-of-line blocking across streams. Requires UDP-permitting networks/firewalls; fallback to H2 automatic. Real-world mobile latency wins are measurable.' },
  { t: 'caching headers strategy', d: 'Cache-Control max-age governs freshness; immutable + hashed filenames enables year-long caching; ETag/Last-Modified enable revalidation (304s); stale-while-revalidate serves stale instantly refreshing background; s-maxage/CDN tiering separates edge from browser policy. Cache design IS frontend architecture.' },
  { t: 'etag vs last-modified revalidation', d: 'Validators let caches ask "still fresh?" cheaply: ETag fingerprints content (strong/weak variants), Last-Modified timestamps coarse. 304 responses skip bodies saving bandwidth though not round trips. Content-hashed assets skip validation entirely via immutable max-age.' },
  { t: 'stale-while-revalidate pattern', d: 'Cache-Control: max-age=1, stale-while-revalidate=86400 serves cached content instantly while fetching fresh asynchronously \u2014 users get zero-latency reads with eventual consistency. Ideal for semi-dynamic content: dashboards, listings. Not for auth-sensitive or rapidly-mutating data.' },
  { t: 'CDN edge caching essentials', d: 'CDNs replicate content near users cutting RTT dramatically; origin shielding reduces origin load; cache keys normalize query strings deliberately; purging strategies matter for corrections. Even dynamic apps benefit: static assets, API GETs with proper headers, SSR HTML micro-caching.' },
  { t: 'brotli vs gzip compression', d: 'Brotli compresses text 15-25% smaller than gzip at comparable speeds (static precompression excels further); all modern browsers send Accept-Encoding: br. Serve brotli-static where possible, dynamic brotli-1..5 levels balancing CPU; gzip remains the universal floor.' },
  { t: 'image optimization pipeline', d: 'Pipeline: resize to served dimensions, modern formats (AVIF/WebP) with JPEG fallbacks, quality tuning 60-80, responsive srcset/sizes markup, lazy-load below-fold, explicit dimensions reserving space, CDN transforms on-demand. Automate via build/CDN rather than discipline alone.' },
  { t: 'video optimization web', d: 'Poster images carry first impression while video metadata loads; preload=none/metadata defers bytes; muted+autoplay+playsinline satisfies autoplay policies; HLS/DASH adaptive bitrate matches networks; short loops beat long videos for ambience. Video backgrounds cost battery \u2014 respect reduced-motion.' },
  { t: 'font subsetting woff2', d: 'Subset fonts to actually-used glyph ranges (latin subsets standard; CJK savings enormous) via tools/pyftsubset; WOFF2 compression halves sizes further. Self-host with font-display strategy plus preload matching exact URL. Each avoided glyph family variant saves real kilobytes.' },
  { t: 'FOIT FOUT mitigation', d: 'Flash-of-invisible/instyled-text stems from font loading windows; mitigate via font-display swap/optional, metric-compatible fallbacks (size-adjust, ascent-override), preloading primary weights only, and subsetting so swaps happen fast. CLS tracking catches residual shifts from late swaps.' },
  { t: 'layout thrashing diagnosis', d: 'Alternating reads (offsetHeight) and writes (style.width) in loops forces sync layout each iteration \u2014 thrashing. Fix batch reads then writes, use FastDOM-style scheduling, or transform-only animations avoiding layout entirely. DevTools Performance panel purple Layout blocks expose offenders.' },
  { t: 'batch dom reads writes', d: 'Pattern: collect all measurements first, compute next states purely, apply all mutations once \u2014 one layout pass instead of N. rAF boundaries segment phases naturally. Virtual DOM libraries institutionalize this batching; vanilla code must self-discipline.' },
  { t: 'forced synchronous layout triggers', d: 'Reading offsetTop/getBoundingClientRect/scrollHeight after style mutations invalidates layout demanding immediate recompute. Hot paths (scroll handlers reading positions post-write) degrade catastrophically. Cache measurements between frames; invalidate intentionally not accidentally.' },
  { t: 'compositor thread advantages', d: 'Compositor handles transform/opacity layers off main thread \u2014 animations stay smooth even during JS stalls. Layer promotion costs memory/GPU upload; excessive layers (will-change spam) backfire. Promote strategically: scrolling panes, animating elements, nothing else without measurement.' },
  { t: 'reflow vs repaint costs', d: 'Reflow (layout) recomputes geometry cascading subtree-wide \u2014 expensive; repaint redraws pixels without geometry \u2014 cheaper; compositor-only updates cheapest. Property change categories determine cost tiers: geometry properties worst, color medium, transform/opacity best.' },
  { t: 'content-visibility auto', d: 'content-visibility:auto skips rendering/painting offscreen sections until approached (with contain-intrinsic-size reserving scroll space) \u2014 massive initial-render wins for long pages. Browser support solid evergreen; fallback harmless (always-rendered). Pair contain:size explicitly where intrinsic sizing unknown.' },
  { t: 'css containment property', d: 'contain: layout/paint/style/size declares subtree isolation letting engines skip outside-in invalidation work. content-visibility composes contain internally. Strategic containment fences third-party widgets and list items bounding reflow blast radius measurably.' },
  { t: 'virtual scrolling implementation math', d: 'Compute visible window: startIndex = floor(scrollTop / rowHeight), render slice + overscan buffer, position via translateY container offset; variable heights need measured-height caches/binary search (react-virtual). Spacer divs maintain scrollbar honesty. Overscan balances smoothness against node count.' },
  { t: 'windowing overscan tuning', d: 'Overscan renders extra rows beyond viewport hiding flash during fast scrolls; too little flickers, too much recreates full-render cost. Dynamic heights complicate estimation \u2014 remeasure-on-render corrects drift. Scroll-jump-to-anchor requires offset recalculation handling.' },
  { t: 'debouncing scroll handlers', d: 'Scroll events fire per-frame-ish; handlers doing layout reads/writes jank visibly. Patterns: passive listener flag, rAF-throttle coalescing to frame boundaries, IntersectionObserver replacing scroll-position polling entirely for visibility logic. Debounce suits resize more than scroll (final-state matters).' },
  { t: 'passive event listeners impact', d: '{passive:true} promises no preventDefault letting browsers scroll immediately without handler-completion waits \u2014 wheel/touch defaults now passive in Chrome. Non-passive listeners on document scroll remain a Lighthouse warning worth auditing. Always passive unless cancellation genuinely needed.' },
  { t: 'requestAnimationFrame batching recap', d: 'rAF aligns work to frame budget (~16.7ms @60fps): read-measure phase then write phase within single callback prevents mid-frame thrash. Long rAF callbacks blow frames regardless \u2014 chunk heavy loops across frames or move off-thread. Missing deadlines shows as dropped-frame gaps in traces.' },
  { t: 'long tasks and TBT', d: 'Tasks exceeding 50ms block input responsiveness accumulating Total Blocking Time; find via DevTools Performance/Lighthouse. Fixes: code-splitting shrinking eval, breaking mega-tasks into yielded slices (scheduler.yield/setTimeout), moving computation to workers. TBT correlates strongly with INP frustration.' },
  { t: 'scheduler yield and yielding patterns', d: 'await scheduler.yield() (or setTimeout fallback) surrenders main thread letting pending input/tasks interleave \u2014 cooperative multitasking inside long operations. Chunk data processing: process batch, await yield, continue keeping INP low under load. isInputPending offers opportunistic early yields.' },
  { t: 'web workers offloading compute', d: 'Workers isolate JSON parsing, crypto, image resizing, search indexing off main thread communicating via structured clone/postMessage or SharedArrayBuffer for heavy payloads. Transferables (ArrayBuffer) move ownership zero-copy. Module workers support imports cleanly modernizing ergonomics.' },
  { t: 'offscreen canvas rendering', d: 'OffscreenCanvas moves canvas drawing into workers keeping complex visualizations frame-budget-free on main thread; commit() pushes frames. Feature-detect support; fallback renders main-thread. Heavy particle systems/charts regain input responsiveness dramatically.' },
  { t: 'wasm appropriate use cases', d: 'WebAssembly suits compute-dense kernels: image/video codecs, physics, cryptography, game engines \u2014 near-native speed with predictable perf. JS interop crossing costs marshaling; keep hot loops inside wasm. Not a general app-language replacement; DX/toolchain overhead demands justification.' },
  { t: 'memory leak hunting workflow', d: 'DevTools Memory heap snapshots: take baseline, interact, take second, compare retaining paths finding detached DOM, growing arrays, leaked listeners. Allocation timeline pins churn sources (GC pressure jank). Allocation instrumentation on timeline catches transient spikes invisible to snapshots.' },
  { t: 'detached node leaks', d: 'JS references (arrays, closures, stores) holding removed DOM nodes prevent GC \u2014 heap snapshots label them Detached. Common culprits: caches keyed by elements, infinite undo stacks of removed subtrees, framework cleanup missed in manual integrations. Null-out references on teardown.' },
  { t: 'performance panel profiling workflow', d: 'Record interactions on throttled CPU/network reproducing target devices; read flame charts bottom-up (self-time), identify long tasks/layout storms/excessive GC; compare before/after commits verifying wins. Screenshots track filmstrip; FPS lane exposes jank visually. Profiles beat speculation every time.' },
  { t: 'lighthouse audits interpretation', d: 'Lighthouse lab-audits metrics/opportunities/diagnostics under simulated throttle; scores weight Core Web Vitals proxies. Treat opportunities (unused JS/images) as prioritized backlog; diagnostics explain root causes. Lab numbers approximate field reality \u2014 pair with RUM for truth.' },
  { t: 'lab data vs field data crux', d: 'Lab (Lighthouse/WebPageTest) = controlled reproducible simulation; field (CrUX/RUM) = real users\u2019 devices/networks distributionally. Optimizing lab-only chases ghosts (thundering-herd test machines); ignoring lab misses regressions pre-release. Ship both dashboards: CI gates lab, product reviews field percentiles.' },
  { t: 'core web vitals thresholds', d: 'Good/needs-improvement/poor cut-offs at p75: LCP \u22642.5s, INP \u2264200ms, CLS \u22640.1. Meeting thresholds at p75 field percentile defines success (Search Console surfaces these). Sub-metrics (TTFB/FCP/TBT) diagnose which stage drags vitals down.' },
  { t: 'LCP element optimization', d: 'LCP usually hero image/text block: optimize via preload+fetchpriority=high on hero, responsive sized sources, server-rendered text (no client-fetch wait), CDN proximity, cached HTML trimming TTFB. Avoid lazy-loading LCP imagery; avoid CSS background-image LCPs (discovery-delayed).' },
  { t: 'INP interaction breakdown', d: 'INP measures worst-ish interaction latency (input delay + processing + presentation) at p75. Improve: shrink handler work (defer non-UI updates), break long tasks, avoid layout-triggering reads in handlers, transitions over blocked paints. Event timing API instruments phases precisely.' },
  { t: 'CLS cumulative layout shift fixes', d: 'Shifts stem from dimension-less media, late-loading fonts/ads/embeds, dynamically injected banners above content. Fix: explicit width/height/aspect-ratio everywhere, size-adjust font fallbacks, reserved ad slots, transform-based insertions (transform doesn\u2019t shift layout), skeletons mirroring final layouts.' },
  { t: 'TTFB improvement levers', d: 'Time-To-First-Byte covers redirect+network+server think time: trim redirect chains, edge-cache HTML fragments, stream SSR early shell, database/query tuning server-side, persistent connections. TTFB gates everything downstream \u2014 client tricks cannot rescue pathological servers.' },
  { t: 'FCP meaning and improvements', d: 'First Contentful Paint marks first text/image visibility: shrink render-blocking CSS/JS, inline critical CSS, font-display swap, preload key requests. Differs from LCP (largest element completes). Progressive rendering (streaming) improves perceived FCP even before data arrives.' },
  { t: 'third-party script facades', d: 'Embeds (video players, chat widgets, maps) cost hundreds of KB eagerly; facades render lightweight lookalikes loading real embed on click. Facade libraries automate YouTube/Vimeo patterns. Audit tag-manager containers ruthlessly \u2014 marketing tags accumulate like sediment.' },
  { t: 'iframe lazy loading and costs', d: 'iframes spawn full browsing contexts costing memory/CPU disproportionately; loading=lazy defers offscreen ones; sandbox attributes constrain privileges. Communication postMessage-only. Consider link-outs or facade placeholders where embed value rarely justifies constant tax.' },
  { t: 'route-based code splitting', d: 'Split bundles at route boundaries mapping user navigation intent: dashboard route never ships checkout code. React.lazy/router lazy routes automate; shared vendor chunks dedupe across splits. Measure route-level bundles independently \u2014 aggregate averages hide bloated outliers.' },
  { t: 'prefetch on hover intent', d: 'Prefetch route chunks on link hover/touchstart/focus (quick-link style libraries) masking navigation latency psychologically; viewport-intersection prefetching suits primary nav. Budget-awareness prevents prefetch storms on link-heavy pages; ignore-on-slow-connections heuristics respect metered data.' },
  { t: 'bundle budgets enforcement', d: 'size-limit/bundlesize CI checks fail builds exceeding per-route/type byte budgets converting perf regressions into PR blockers. Budgets encode architectural intent (route < 150KB gzip etc.). Track trend charts \u2014 slow creep kills faster than cliff drops.' },
  { t: 'source maps production safety', d: 'Shipping source maps publicly exposes original source; hide via private uploads to error trackers, or serve behind auth/IP allowlists. Hidden maps still symbolicate stack traces server-side. Debugging production needs maps \u2014 balance observability against IP exposure deliberately.' },
  { t: 'precompress static assets', d: 'Build-time brotli/gzip precomputation (.br/.gz files) lets CDNs serve perfect-ratio compressed bytes without runtime CPU; compression-level tradeoffs vanish. Vite plugins automate emission. Dynamic compression reserves CPU for actual dynamics \u2014 static deserves static treatment.' },
  { t: 'immutable hashed filenames', d: 'Content-hash filenames ([name].[hash].js) let caches store forever safely; deploys ship new hashes invalidating precisely changed files. Combined with immutable directive eliminates revalidation round trips. HTML stays short-TTL referencing current hashes \u2014 the deployment contract.' },
  { t: 'service worker stale strategies deep dive', d: 'Strategy choice per resource class: app shell precache (install-time) guarantees offline boot; hashed assets cache-first immutable; HTML network-first with cache fallback preserving freshness/offline balance; APIs stale-while-revalidate or network-only per sensitivity. Versioned cache names enable atomic upgrades.' },
  { t: 'workbox declarative sw', d: 'Workbox packages routing/strategies/precache manifest tooling abstracting SW boilerplate: registerRoute with plugins (expiration, cacheableResponse, broadcastUpdate). GenerateSW mode suffices simple cases; custom SW code for exotic flows. Migration path keeps raw-SW knowledge relevant underneath.' },
  { t: 'offline ux communication', d: 'Offline states deserve honest UI: banner indicating cached-mode, queued-actions badges replaying later, disabled-vs-degraded affordances clarity. navigator.onLine lies sometimes (captive portals) \u2014 verify via heartbeat/fetch probes. Silent failures erode trust faster than honest degradation.' },
  { t: 'ssr vs csr vs ssg vs isr', d: 'CSR ships shells hydrating client-side (slow first paint, rich interactivity); SSG prerenders build-time (fast, stale until rebuild); SSR renders per-request (fresh, server cost); ISR regenerates statically after TTL (hybrid). Choose per-route: marketing SSG, personalized SSR, docs ISR, apps CSR+islands.' },
  { t: 'hydration cost islands architecture', d: 'Full-page hydration ships/executes entire component tree JS despite static majority; islands hydrate interactive widgets only (Astro pattern) shipping dramatically less JS. Selective/streaming hydration prioritizes visible/interacting regions progressively. Architecture-level answer to JS-bloat complaints.' },
  { t: 'streaming ssr flush timing', d: 'Streaming sends HTML shell immediately flushing Suspense boundaries as data resolves \u2014 browsers start painting before full document arrives. Flush order matters: shell-first beats data-blocking; out-of-order streaming inserts placeholders swapped later. Perceived speed improves even when total time identical.' },
  { t: 'data waterfalls elimination', d: 'Sequential awaits (auth\u2192user\u2192posts) multiply latencies; parallelize independent fetches (Promise.all), colocate data requirements with components enabling topological scheduling (RSC/loaders), cache/dedupe repeated keys, prefetch predictable next queries during idle. Waterfalls hide inside component trees silently.' },
  { t: 'graphql overfetching context', d: 'GraphQL clients request exact fields curing REST over/underfetching; costs shift to resolver N+1 risks (dataloader batches), persisted-query security, cache complexity vs URL-cacheability. REST field-shaping params/compound documents address similar needs simpler when graphs stay shallow.' },
  { t: 'api pagination strategies', d: 'Offset pagination jumps randomly but drifts under concurrent inserts; cursor pagination (opaque tokens) stays stable scaling infinitely; infinite-scroll UX pairs cursors naturally while page-number UX aids direct linking/deep jumps. Always cap page sizes server-side defending against dump-requests.' },
  { t: 'request race conditions handling', d: 'Out-of-order responses overwrite fresher state (type-ahead classic); fix via AbortController cancelling stale requests, sequence counters ignoring stale resolutions, or keyed result maps. Libraries (React Query) handle cancellation/caching automatically \u2014 hand-rolled fetch effects must self-discipline.' },
  { t: 'retry idempotency discipline', d: 'Retry only idempotent verbs (GET/PUT/DELETE) blindly; POST retries duplicate creations unless idempotency-keys sent (Stripe pattern) letting servers dedupe. Exponential backoff with jitter prevents synchronized retry storms. Respect Retry-After headers honoring rate limits politely.' },
  { t: 'timeout abortcontroller everywhere', d: 'Every network call needs timeout ceilings: AbortSignal.timeout(ms) composes with existing signals (AbortSignal.any) cleanly; hanging sockets otherwise pin UI spinners indefinitely. Timeout errors classify distinctly from user-cancellations for retry logic branching decisions.' },
  { t: 'skeleton screens psychology', d: 'Skeletons mirror incoming layouts reducing perceived wait versus spinners by promising structure; instant-feel requires skeleton\u2192content morph without layout jump (identical dimensions). Overlong skeleton displays signal slowness honestly \u2014 pair with progressive content streaming when possible.' },
  { t: 'optimistic ui patterns summary', d: 'Optimistic updates assume success rendering immediately, reconciling/rolling back on response; conflicts serialize per-entity queues; user-visible pending indicators preserve honesty. Suited to low-risk frequent actions (likes, toggles); unsuited payments/legal flows demanding confirmation-first certainty.' },
  { t: 'rum implementation essentials', d: 'Real-user monitoring samples real sessions reporting vitals/custom timings segmented by device/route/release; beacon on pagehide/visibilitychange (unload unreliable). Sampling rates balance insight vs privacy/cost. Field percentiles drive roadmaps better than any synthetic suite.' },
  { t: 'error tracking release correlation', d: 'Tag exceptions with release versions enabling spike-vs-deploy correlation dashboards; sourcemap uploads restore readable stacks; breadcrumbs reconstruct user journeys pre-crash; alert thresholds page on new-issue velocity. Blameless triage routes frontend crashes into sprint priorities efficiently.' },
  { t: 'synthetic monitoring cadence', d: 'Scheduled synthetic runs (WebPageTest/Lighthouse CI) probe key journeys from global locations catching regional/CDN regressions continuously; alert deltas beyond tolerance bands. Complements RUM\u2019s realism with controlled comparability run-over-run.' },
  { t: 'semantic html seo foundation', d: 'Semantic elements (header/nav/main/article/footer/h1-hierarchy) communicate structure to crawlers/assistants alike; one h1 per view, logical heading depth, descriptive links (no click-here), meaningful alt. SPAs must manage document.title/meta per route since crawlers reward accurate metadata.' },
  { t: 'meta tags social sharing', d: 'Open Graph/Twitter cards control share previews (og:title/description/image dimensions); canonical URLs consolidate duplicates; meta descriptions earn clicks though not rankings directly. Dynamic SSR/head managers keep per-route metadata truthful \u2014 static SPA index.html squanders social presence.' },
  { t: 'structured data json ld', d: 'JSON-LD schema.org markup (Product/Article/FAQ/Breadcrumb) earns rich results: stars, prices, FAQ dropdowns. Validate via Rich Results Test; match visible content (markup mismatch penalizes). Server-render JSON-LD alongside content keeping parity enforced.' },
  { t: 'spa crawlability prerender', d: 'Crawlers increasingly execute JS yet unreliably/budget-limited; prerender critical public routes (SSG/SSR) guaranteeing complete HTML; sitemaps + clean URLs + history-API routing (not hashbangs) complete the picture. Internal-search-driven apps may accept lighter SEO obligations knowingly.' },
  { t: 'security headers csp nonce', d: 'Content-Security-Policy restricts script/style/img origins killing XSS blast radius; nonces authorize inline scripts per-request; report-uri iterates violations safely (report-only rollout first). Frame-ancestors replaces X-Frame-Options; HSTS forces HTTPS persistence. Headers are free defense-in-depth.' },
  { t: 'csrf protection sameSite', d: 'CSRF tricks browsers sending cookies cross-site; SameSite=Lax/Strict cookie flags neutralize most vectors modernly; legacy fallbacks use synchronizer tokens double-submitting. CORS blocks reading responses but NOT state-changing sends \u2014 hence CSRF remains its own concern distinct from CORS.' },
  { t: 'xss sanitization dompurify', d: 'Sanitize untrusted HTML server/client-side (DOMPurify allowlists) before innerHTML; frameworks escape interpolations by default \u2014 dangerouslySetInnerHTML bypasses demanding manual sanitation. Never sanitize with regexes (bypass galore). CSP adds the outer net catching sanitizer misses.' },
  { t: 'jwt storage tradeoffs', d: 'localStorage JWTs survive refresh but XSS-readable; HttpOnly cookies shield from JS inviting CSRF mitigations (SameSite) instead; memory-only tokens balance briefly. Refresh-token rotation in HttpOnly cookies with short-lived access tokens is the common hardened pattern today.' },
  { t: 'oauth pkce flow basics', d: 'PKCE (proof-key-for-code-exchange) secures public clients (SPAs) via code_verifier challenges eliminating client-secret exposure; implicit flow deprecated. BFF (backend-for-frontend) proxy pattern alternatively keeps tokens entirely server-side sidestepping browser token custody debates altogether.' },
  { t: 'supply chain security npm', d: 'Lockfiles pin trees; audit tools/dependabot flag CVEs demanding triage SLAs; provenance attestations verify publish origins; minimal dependencies reduce attack surface philosophically. Post-install scripts warrant review (disable via ignore-scripts where feasible). typosquatting vigilance on fresh packages.' },
  { t: 'subresource integrity sri', d: 'integrity=sha384-... attributes pin CDN script/hash expectations \u2014 tampered CDN serves refuse execution. Applies to third-party scripts/styles you reference cross-origin. First-party hashed bundles rely on cache-immutable naming instead; SRI covers the external-dependency gap.' },
  { t: 'permissions policy feature gating', d: 'Permissions-Policy header disables powerful features (camera/geolocation/fullscreen) for your page and embedded iframes \u2014 defense against malicious ad iframes abusing permissions under your origin\u2019s reputation. Allowlist model: default deny, grant explicitly to trusted embeds only.' },
  { t: 'design tokens pipelines', d: 'Tokens (color/spacing/typography primitives) authored once (Figma/design repo) transform via Style Dictionary into CSS vars/iOS/Android outputs \u2014 single source syncing platforms. Semantic layer (surface-primary) sits atop primitives enabling theme swaps without component edits.' },
  { t: 'visual regression testing', d: 'Screenshot diffs (Playwright snapshots/Chromatic/Percy) catch unintended style drift across components/states/viewports; flake management needs stable animation freezing, font loading waits, deterministic rendering. Integrate per-PR with review-gated baselines updating deliberately.' },
  { t: 'storybook driven development flow', d: 'Story-first authoring isolates component states (variants/loading/error/a11y checks via addons) before integration; stories become living documentation and regression baselines; interaction tests script behaviors inside stories. MSW addons mock data contexts realistically.' },
  { t: 'ci pipeline speed optimizations', d: 'Cache node_modules/build artifacts keyed on lockfile hashes; parallelize lint/test/build matrix shards; affected-only builds in monorepos skip untouched projects; remote caches share across CI runs. Pipeline minutes translate directly into developer feedback loop quality.' },
  { t: 'preview deployments workflow', d: 'Per-PR preview URLs (Vercel/Netlify style) let reviewers/QA/stakeholders exercise changes isolated; seeded demo datasets stabilize reviews; teardown policies manage sprawl. Preview environments convert "works on my machine" debates into shared live evidence.' },
  { t: 'conventional commits semver automation', d: 'Conventional-commit prefixes (feat/fix/chore!) feed semantic-release automating version bumps/changelogs/tags deterministically from history; scope conventions route changelog sections. Discipline enforced via commitlint hooks; automation removes release-day ceremony entirely.' },
  { t: 'code review culture practices', d: 'Small PRs (<400 lines) review meaningfully; review for correctness/readability/architecture-fit not style (linters own that); author-narrated PR descriptions with screenshots/repro steps accelerate understanding; timely reviews are team infrastructure \u2014 treat latency as a bug.' },
  { t: 'technical debt management strategy', d: 'Classify debt: deliberate/prudent versus reckless; allocate fixed capacity (20% rotation) preventing silent accumulation; boy-scout refactors bound scope creep; debt registers make liabilities visible/plannable. Rewrite-versus-incremental decisions weigh freeze-risk against drag economics.' },
  { t: 'estimation uncertainty communication', d: 'Estimate ranges with confidence framing, decompose until uncertainty shrinks, re-forecast at checkpoints openly. Reference-class forecasting (similar past work) beats optimism anchoring. Stakeholders need decision-grade confidence statements, not false precision theater.' },
  { t: 'incident postmortems blameless', d: 'Blameless postmortems reconstruct timelines/systemic causes/action-items with owners; frontend incidents (bad deploy, CDN outage, third-party collapse) warrant same rigor as backend. Runbooks + kill switches (feature flags) shorten future MTTR materially.' },
  { t: 'browser compat baselines browserslist', d: 'browserslist config drives autoprefixer/babel/esbuild targets uniformly; baseline definitions (widely-available/newly-available) formalize support stances. Usage analytics (RUM browser shares) inform pragmatic cutoffs \u2014 supporting IE11-era engines multiplies costs invisibly until audited.' },
  { t: 'feature detection discipline', d: '@supports (CSS), in-operator/typeof guards (JS) branch capabilities gracefully; progressive enhancement stacks experiences atop verified baselines. UA-sniffing rots perpetually \u2014 reserve for telemetry only. Interop dashboards (caniuse) guide adoption timelines confidently.' },
  { t: 'accessibility wcag principles pocp', d: 'WCAG organizes under Perceivable/Operable/Understandable/Robust; conformance levels A/AA/AAA set contractual bars (AA typical legal baseline). Practical pillars: keyboard-complete operation, sufficient contrast, labeled controls, motion respect, semantic structure. Automated tools cover ~30% \u2014 manual passes remain mandatory.' },
  { t: 'screen reader testing quickstart', d: 'Learn one SR deeply: NVDA (free/Windows) or VoiceOver (macOS/iOS): rotor navigation, forms mode, landmarks jumping, live-region announcements. Test critical flows quarterly minimum. Hearing your UI spoken aloud converts abstract a11y guidelines viscerally concrete.' },
  { t: 'axe automated testing limits', d: 'axe-core lints DOM against rule sets (contrast, labels, aria usage) integrable into unit/E2E suites cheaply; catches mechanical violations but misses focus-flow sanity, meaningful-alt judgment, keyboard traps nuance. Treat green axe runs as necessary-not-sufficient evidence.' },
  { t: 'focus trap modal accessibility', d: 'Modals must trap Tab cycling inside, close on Escape, restore trigger focus on dismissal, announce entry via role=dialog/aria-modal, lock background scroll carefully (iOS quirks). Radix/Headless UI solve thoroughly \u2014 prefer adopting over reimplementing subtle behaviors repeatedly.' },
  { t: 'skip links navigation aid', d: 'Visually-hidden-until-focused skip-links jump past repetitive nav straight to main content \u2014 first tabbable element convention. href="#main" anchors require tabindex=-1 targets for reliable focus placement. Tiny effort, outsized keyboard/SR experience dividends.' },
  { t: 'i18n plurals icu messageformat', d: 'ICU MessageFormat encodes plural/gender/select grammar rules per locale ({count, plural, one {...} other {...}}); languages demand wildly varying category sets (Polish/Czech multi-plurals). Libraries compile catalogs lazily per locale; never hand-roll plural English assumptions globally.' },
  { t: 'rtl pseudo localization testing', d: 'Pseudo-localization transforms strings (accents, lengthening +40%, bracket wrapping) exposing hard-coded concatenations/truncation bugs pre-translation; RTL flip previews catch directional icons/layout assumptions. Bake both modes into CI screenshot suites catching regressions mechanically.' },
  { t: 'timezone storage display separation', d: 'Store UTC instants + optional IANA zone; render zone-aware via Intl/zoned libraries; schedule-future events need zone retention (not just offsets \u2014 DST shifts). Calendar arithmetic crosses DST boundaries deceptively: adding 24h != adding a day locally everywhere.' },
  { t: 'intl number formatting nuances', d: 'NumberFormat handles currency (display widths, decimal digits per currency), units (kilometer vs km), compact notation (1.2M), scientific, and locale digit grouping (Indian lakh grouping differs!). Cache formatters; construct per locale+options pair reused across renders.' },
  { t: 'collation locale aware sorting', d: 'String.prototype.sort defaults UTF-16 order mangling accented characters (é after z); Intl.Collator sorts per linguistic rules (strength settings tune accent/case sensitivity). Cache collator instances \u2014 creation cost dominates. User-facing lists deserve linguistic ordering everywhere.' },
  { t: 'unicode grapheme pitfalls', d: 'User-perceived characters (graphemes) span multiple code points: emoji ZWJ sequences, flags, combining accents. String.length miscounts them; Intl.Segmenter segments graphemes correctly for truncation/validation (max-length inputs counting humans\u2019 characters, not UTF-16 units).' },
  { t: 'encoding utf8 emoji surrogates', d: 'Astral emoji encode surrogate PAIRS in UTF-16: naive half-pair slicing corrupts output producing replacement diamonds. Code-point aware ops ([...str], u-flag regex, Intl.Segmenter) preserve integrity. Validation regexes without u flag match half-pairs dangerously accepting broken strings.' },
];
