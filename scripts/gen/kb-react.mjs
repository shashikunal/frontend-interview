// ReactJS topics
export default [
  { t: 'what React is', d: 'React is a JavaScript library for building declarative, component-based user interfaces. You describe UI as a function of state; React computes DOM updates efficiently via reconciliation. It handles the view layer only, leaving routing, data fetching and styling to ecosystem choices.' },
  { t: 'JSX', d: 'JSX is syntax sugar for React.createElement (or jsx runtime calls): expressions compile to plain objects describing elements. Attributes camelCase (className, onClick), children nest naturally. JSX must return a single root or Fragment, and {} embeds any expression including maps of elements.' },
  { t: 'virtual DOM', d: 'The virtual DOM is an in-memory tree of lightweight element objects mirroring desired UI. On state change React diffs new versus previous trees and applies minimal real-DOM mutations. This batching avoids expensive layout thrash while keeping declarative code readable.' },
  { t: 'reconciliation and diffing', d: 'Reconciliation compares element trees assuming: different element types tear down and rebuild subtrees; same types update props in place; keyed lists match children by key enabling moves. These heuristics turn O(n^3) tree diffing into practical O(n) updates.' },
  { t: 'keys in lists', d: 'Keys give elements stable identities across renders letting React reorder instead of recreate. Use unique data ids \u2014 never array indices when lists reorder/filter, which causes state bleed between items and input focus bugs. Keys need uniqueness among siblings only.' },
  { t: 'why index keys are dangerous', d: 'With index keys, inserting at the top shifts every item\u2019s identity: React patches each row\u2019s content rather than moving nodes, so internal state (input values, checkboxes, animations) attaches to wrong rows. Stable ids preserve component instances correctly through mutations.' },
  { t: 'components as functions', d: 'Function components receive props and return description trees; React calls them repeatedly treating each call as pure-ish render. Side effects belong in effects/handlers not render bodies. Function identity changes per render, which is why memoization hooks exist.' },
  { t: 'props', d: 'Props are read-only inputs flowing down from parents. Mutating them breaks one-way data flow assumptions; instead lift changes via callbacks. children prop carries nested elements enabling composition wrappers. Default values via destructuring defaults keep components resilient.' },
  { t: 'state vs props distinction', d: 'State is owned and mutable within a component triggering re-render on change; props are external immutable inputs. The mental test: data changing over time from user/server becomes state; data passed in stays prop. Derived values should be computed during render, not duplicated into state.' },
  { t: 'useState hook', d: 'useState returns [value, setter] pair; calling setter schedules re-render with new value. Initial value evaluates once (pass function for expensive init). Setters with identical Object.is value bail out re-renders. Multiple states compose naturally versus one giant object needing spread merges.' },
  { t: 'functional setState updates', d: 'When new state depends on previous, pass updater: setCount(c => c + 1). Batched event handlers both using stale closure count would overwrite; updaters queue applying sequentially on latest value. Always prefer functional form inside timers, subscriptions, async callbacks.' },
  { t: 'state batching', d: 'React 18 batches all updates automatically \u2014 multiple setters inside promises/timeouts/native handlers commit once, unlike legacy auto-batching limited to React event handlers. flushSync opts out when reading DOM between updates is unavoidable (rare measurement cases).' },
  { t: 'lazy initial state', d: 'Passing a function to useState defers expensive computation until first render only: useState(() => buildFromLocalStorage()). Without it the initializer runs every render wasting work even though result ignored after mount. Arguments to the lazy initializer cannot reference props safely before mount.' },
  { t: 'derived state anti-pattern', d: 'Copying props into state via useEffect duplicates truth causing sync bugs; compute derived values during render instead (const total = price * qty). If derivation is expensive, useMemo caches. Resetting state on prop change legitimately needs key-remount pattern or tracked-prev-state comparison.' },
  { t: 'useReducer', d: 'useReducer manages complex state transitions via reducer(nextState, action) purity plus dispatch stability across renders. Suits multi-field interdependent updates, undo stacks, and state machines. Dispatch identity never changes, making it safe for effect deps without useCallback wrapping.' },
  { t: 'lifting state up', d: 'When sibling components need shared data, move state to nearest common ancestor passing values/callbacks down. This keeps single source of truth though creates prop-drilling at depth. Combine with context or composition (passing components) to relieve drilling pressure selectively.' },
  { t: 'one-way data flow', d: 'Data descends via props; changes ascend via callbacks \u2014 a unidirectional cycle making tracing updates tractable. Two-way bindings like controlled inputs synthesize the round trip explicitly: parent owns value, child reports onChange. Debugging benefits: every mutation has a visible call path.' },
  { t: 'controlled inputs', d: 'Controlled inputs bind value to state and update via onChange, making React the single source of truth enabling validation, formatting, conditional submission. Every keystroke triggers render \u2014 fine for forms, but huge text areas may need debouncing or uncontrolled escape hatch.' },
  { t: 'uncontrolled inputs', d: 'Uncontrolled inputs keep DOM as source of truth read via refs on demand (formData/FormData API shines here). They skip per-keystroke re-renders and integrate legacy plugins easily. File inputs are inherently uncontrolled since value is write-only for security.' },
  { t: 'useEffect basics', d: 'useEffect runs after commit synchronizing with external systems: subscriptions, timers, analytics, manual DOM. Return cleanup reversing setup; dependencies array controls re-runs. Effects run after paint by default keeping interactions snappy, unlike class componentDidMount+Update+WillUnmount merged into one coherent unit.' },
  { t: 'effect cleanup patterns', d: 'Cleanup unsubscribes listeners, clears timers, aborts fetches, closes sockets \u2014 running before next effect execution and on unmount. Forgetting cleanup leaks memory and causes ghost handlers firing on dead components. StrictMode intentionally mounts-unmounts-mounts exposing missing cleanups early in development.' },
  { t: 'empty dependency array meaning', d: '[] tells React the effect touches nothing reactive: run once after mount, cleanup on unmount. Lying with [] while reading props/state captures stale values forever. If you need mount-only behavior with reactive reads, refs hold latest values without re-triggering.' },
  { t: 'stale closures in hooks', d: 'Closures capture render-time values; callbacks registered once (event listeners, intervals) see frozen props/state. Fixes: functional updaters for state, refs mirroring latest values, correct dependency arrays recreating callbacks, or useSyncExternalStore for subscription correctness. Stale closure bugs dominate senior interviews.' },
  { t: 'useEffect dependency array rules', d: 'List every reactive value used inside: props, state, and derived references recreated per render (objects/functions/arrays). Include them or memoize them. ESLint exhaustive-deps enforces honestly. Over-including causes churn; under-including causes staleness \u2014 neither is safe, correctness comes from truthful deps.' },
  { t: 'infinite useEffect loops', d: 'Loops occur when effects setState values listed in their own dependencies, or dependencies are inline objects/functions recreated each render. Break cycles: derive during render instead, stabilize references with useMemo/useCallback, or restructure so effects consume refs. DevTools profiler visualizes the ping-pong commits.' },
  { t: 'useLayoutEffect timing', d: 'useLayoutEffect fires synchronously after DOM mutations before browser paint, enabling measurement-then-mutation without flicker (tooltip positioning, scroll anchoring). Costs blocking paint \u2014 reserve for visual-critical measurements; SSR warns because layout effects never run server-side.' },
  { t: 'useInsertionEffect', d: 'useInsertionEffect runs before layout effects specifically for CSS-in-JS libraries injecting style nodes ahead of paint-dependent reads. App code rarely touches it directly; knowing it exists clarifies why styled-components/emotion versions pin React versions.' },
  { t: 'useRef', d: 'useRef returns stable { current } box surviving renders without triggering re-render on mutation. Primary uses: DOM node access, mutable instance values (timer ids, previous values), latest-value holders for stale-closure immunity. Writing ref.current during render is forbidden \u2014 do it in effects/handlers.' },
  { t: 'ref vs state choice', d: 'Choose state when UI should re-render on change; choose ref when value matters only to event handlers/effects (interval ids, previous props, mounted flags). Rule: rendering output depends on it \u2192 state; imperative logic depends on it \u2192 ref.' },
  { t: 'forwardRef', d: 'forwardRef lets function components accept ref from parents forwarding to inner DOM/custom node, essential for reusable inputs, tooltips, portals. React 19 allows ref as regular prop for function components, but forwardRef remains needed for libraries supporting older versions simultaneously.' },
  { t: 'useImperativeHandle', d: 'useImperativeHandle customizes what forwarded refs expose \u2014 curated methods (focus(), scrollToBottom()) instead of raw nodes, hiding internals and stabilizing API. Dependency array controls when exposed object rebuilds. Prefer props/data flow; imperative handles suit genuinely imperative widgets (canvas editors, virtualized lists).' },
  { t: 'useMemo purpose and misuse', d: 'useMemo caches computation between renders keyed by dependencies, skipping recalculation and preserving referential equality for downstream memo/effect deps. Misuse includes wrapping trivial math adding overhead, and relying on it semantically \u2014 cache may be discarded anytime, so correctness must survive recomputation.' },
  { t: 'useCallback necessity', d: 'useCallback stabilizes function identities feeding memoized children or effect dependency arrays. Without memoized consumers it adds overhead pointlessly. React Compiler aims to automate such memoization, but until ubiquitous, hand-memoize measured hot paths: list rows, context providers\u2019 values, subscription callbacks.' },
  { t: 'React.memo shallow compare', d: 'memo wraps components skipping re-render when props shallow-equal previous. Objects/arrays/functions created fresh per render defeat it unless parent memoizes them too. Custom comparators exist but hide bugs; prefer memoizing upstream values or restructuring props to primitives over deep-compare escapes.' },
  { t: 'context API fundamentals', d: 'Context broadcasts values down the tree bypassing prop drilling: createContext(default), Provider, useContext consumption. All consumers re-render on value change \u2014 split frequently-changing state from stable APIs, memoize provider values, and consider state-management libs for high-frequency stores.' },
  { t: 'context performance pitfalls', d: 'Provider value={ {user, setUser} } allocates fresh object each render invalidating every consumer; wrap in useMemo. Placing providers near root multiplies blast radius; scope contexts tightly around subtrees actually consuming them. Selector-based alternatives (zustand, jotai) solve granular subscriptions elegantly.' },
  { t: 'custom hooks extraction', d: 'Custom hooks are functions calling other hooks sharing stateful logic across components: useToggle, useDebounce, useFetch. Name must start with use enabling lint rules. Each caller gets independent state instance \u2014 they share code, not state (that\u2019s context/store territory).' },
  { t: 'rules of hooks rationale', d: 'Hooks must run unconditionally in identical order every render because React indexes them positionally. Conditionals/loops/nested functions break mapping causing state crossover chaos. Early returns after all hooks complete are fine. The lint plugin mechanically enforces these constraints.' },
  { t: 'conditional hooks problem', d: 'Calling hooks inside if blocks shifts subsequent hook indices when conditions flip, corrupting stored state pairs silently. Restructure: put condition inside hook logic, split components so branches own separate hook sets, or early-return only after all hooks execute.' },
  { t: 'StrictMode double rendering', d: 'Development StrictMode invokes render functions twice and mounts/unmounts/remounts effects once, surfacing impure renders and missing cleanups. Production skips this entirely. Treat it as free audit tooling: code breaking under StrictMode harbors latent production bugs involving side effects in render paths.' },
  { t: 'error boundaries', d: 'Error boundaries catch render/lifecycle errors in child subtrees displaying fallbacks \u2014 implemented via componentDidCatch/getDerivedStateFromError classes or react-error-boundary library. They miss event handlers, async callbacks, and server errors; those need try/catch + state. Boundary placement strategy: route-level coarse plus widget-level surgical recovery.' },
  { t: 'portals use cases', d: 'createPortal renders children into foreign DOM containers escaping overflow/z-index traps: modals, tooltips, dropdown menus. Event bubbling follows React tree not DOM tree, so context and handlers still work naturally. Manage focus and aria attributes manually for accessibility compliance.' },
  { t: 'fragments', d: '<>...</> groups children without wrapper nodes keeping DOM flat (table rows, flex children). Explicit <Fragment key=...> supports keys in loops. Extra divs break table semantics and CSS grid/flex expectations \u2014 fragments sidestep structural pollution cleanly.' },
  { t: 'children prop composition', d: 'Accepting children turns components into layout shells (Card, Sidebar) delegating content decisions upward. Composition beats configuration props explosion: <Modal footer={<Button/>}> expresses arbitrary structure naturally. children can be functions (render-prop style) receiving data arguments.' },
  { t: 'render props pattern', d: 'Render props pass functions as children returning elements: <Mouse>{(pos) => <Cat pos={pos}/>}</Mouse>. They share behavior with inversion-of-control before hooks existed. Modern code prefers custom hooks for reuse, but render props still excel where multiple sibling consumers need synchronized external values.' },
  { t: 'higher-order components legacy role', d: 'HOCs wrap components enhancing props (withRouter, connect). Issues: prop collisions, wrapper hell in DevTools, ref forwarding ceremony. Hooks replaced most HOC use-cases; recognize them when maintaining legacy codebases and prefer incremental hook migration during feature work touching them.' },
  { t: 'compound components pattern', d: 'Compound components (Tabs.List/Tabs.Tab) share implicit state via context letting consumers compose sub-parts flexibly with declarative markup. Implementation: parent provides context, children consume via static properties attached to namespace. Powers headless UI libraries like Radix beautifully.' },
  { t: 'controlled vs uncontrolled components generalization', d: 'Beyond inputs, whole widgets follow the dual contract: value/onChange controlled versus defaultValue/ref uncontrolled. Libraries expose both honoring consumer architecture. Controlled grants validation/sync power; uncontrolled grants performance and simplicity. Offer controlled primarily, document escape hatches clearly.' },
  { t: 'prop drilling mitigation strategies', d: 'Drilling through many layers signals restructuring need: component colocation, composition passing JSX through middle layers, context for genuinely global concerns, or store libraries for cross-cutting state. Middle layers accepting {...rest} stay decoupled from specifics they merely relay.' },
  { t: 'state colocation principle', d: 'Keep state as close to its consumers as possible \u2014 local state stays local, lifting only when sharing demands. Colocation shrinks re-render scope, simplifies reasoning, eases testing. Global stores hoarding everything recreates the spaghetti state management was meant to fix.' },
  { t: 'Redux core concepts flow', d: 'Store holds single state tree; actions describe changes {type, payload}; reducers compute next state purely; dispatch triggers subscribers. Unidirectional flow plus time-travel debugging made Redux dominant; boilerplate criticism birthed Redux Toolkit consolidating slices, immer integration, and thunk defaults.' },
  { t: 'Redux Toolkit advantages', d: 'RTK eliminates action-type strings via createSlice auto-generating actions/reducers, bundles immer for ergonomic immutable updates, configures devtools/thunk defaults, and offers RTK Query for data fetching with caching. New Redux projects start with RTK; hand-rolled legacy patterns persist mainly in old codebases.' },
  { t: 'when to reach for global state libraries', d: 'Reach for stores when server-cache/client-state boundaries blur, many distant consumers mutate shared state, or undo/time-travel matters. Local-first React state covers most apps; zustand/jotai provide minimal bridges; Redux suits strict discipline teams. Avoid premature globalization \u2014 local state scales further than intuition suggests.' },
  { t: 'server state vs client state', d: 'Server state (API data) arrives cached/duplicated/stale \u2014 fundamentally unlike client-only UI state. React Query/SWR specialize: caching, background refetch, deduplication, pagination, optimistic updates. Treating server data as generic reducer state reimplements these poorly; adopt dedicated layers early.' },
  { t: 'React Query essentials', d: 'Queries key-cache responses with staleTime/gcTime tuning freshness; mutations handle side effects with invalidation cascades. Devtools visualize cache health. Infinite queries manage cursors; suspense integration suspends components naturally. Keys form hierarchical identities enabling targeted invalidation after mutations.' },
  { t: 'optimistic updates via query clients', d: 'onMutate snapshots cache, writes expected result immediately; onError rolls back snapshot; onSettled invalidates reconciling server truth. UX feels instant for low-conflict mutations. Concurrent conflicting mutations serialize via queued sequencing preventing interleaved rollback corruption.' },
  { t: 'SWR approach', d: 'SWR popularized stale-while-revalidate naming: serve cached instantly then refresh background. Lighter than React Query with revalidation focus, less mutation machinery. Both eliminate manual loading/error state plumbing; choosing between them often follows team familiarity more than technical deltas.' },
  { t: 'zustand simplicity model', d: 'Zustand stores are hook-accessible vanilla stores: create((set,get)=>({...})) with selector subscriptions minimizing re-renders without providers. Middleware adds persistence/devtools. Its low ceremony makes it today\u2019s default answer for modest global state beyond context capabilities.' },
  { t: 'jotai atomic state', d: 'Jotai models state as atoms composing derivations automatically tracking dependencies: atom(), useAtom(). Bottom-up granularity means components subscribe to precisely the atoms read, eliminating selector boilerplate. Async atoms chain fetching naturally. Excellent fit for complex derived dashboards.' },
  { t: 'MobX observable paradigm', d: 'MobX tracks property access during render auto-subscribing precisely; mutations flow through actions triggering minimal recomputation. Imperative style contrasts Redux purity; decorators optional. Shines with deeply interconnected domain models where manual change propagation would sprawl.' },
  { t: 'context vs store decision matrix', d: 'Context fits low-frequency global config (theme, locale, auth user). Stores fit high-frequency updates, cross-tree selectors, middleware needs. Litmus: does every consumer want every change? Context yes-consumers all rerender; stores filter via selectors. Hybrid architectures combine both deliberately.' },
  { t: 'React Router v7 fundamentals', d: 'Routes map URL patterns to components/layouts via createBrowserRouter + RouterProvider (data mode) or <Routes> declarative mode. Nested routes render Outlet hierarchies inheriting params. Index routes fill parent paths exactly. Framework mode adds SSR conventions atop the same matching engine.' },
  { t: 'nested routes and Outlet', d: 'Parent route components render <Outlet/> marking child slot, sharing layouts (nav, sidebar) across siblings. Params cascade: /users/:userId/posts/:postId accessible via useParams at any depth. Relative links resolve against current nesting simplifying refactors of path prefixes.' },
  { t: 'programmatic navigation', d: 'useNavigate() returns navigate(path, {replace, state}) for post-submit redirects and guards; Link covers declarative cases preferring anchors for SEO/middle-click. navigate(-1) mimics back. Avoid navigating inside render \u2014 do it in handlers/effects after operations settle.' },
  { t: 'search params handling', d: 'useSearchParams reads/writes query strings as URLSearchParams preserving object identity discipline; setSearchParams merges/replaces entries driving filter UIs shareable by URL. Deriving filters exclusively from search params keeps back/forward working free \u2014 state-in-URL philosophy.' },
  { t: 'route loaders concept', d: 'Data-router loaders fetch before rendering resolving navigation-blocking data with pending UI states (useNavigation). Errors bubble to route errorElement boundaries. Loaders parallelize across matched routes eliminating waterfalls that sequential useEffect fetching creates.' },
  { t: 'protected routes implementation', d: 'Auth guards check session in loader (redirect pre-render) or wrapper component rendering Navigate/Navigate-with-state for post-login return. Centralizing checks in loaders prevents flash-of-protected-content. Role-based variants gate nested segments hierarchically inheriting protections.' },
  { t: 'lazy loading routes', d: 'React.lazy(() => import("./Page")) plus Suspense fallback splits route bundles automatically in most routers; data routers support lazy route modules deferring even definitions. Preload on link hover/intent to mask chunk latency. Combine per-route splitting with vendor chunk strategy for balanced caching.' },
  { t: 'scroll restoration handling', d: 'Browsers restore native scrolls on popstate but SPAs pushing history need ScrollRestoration component (router-provided) or manual effect saving positions keyed by location. New navigations scroll top; back/forward restore saved offsets. Animated transitions complicate restoration timing requiring post-transition correction.' },
  { t: '404 and catch-all routes', d: 'Path "*" catches unmatched URLs rendering NotFound pages with helpful navigation. Data routers use errorElement distinguishing thrown 404s from crashes. Server config must forward unknown paths to app entry (SPA fallback) or direct loads of deep links fail before React mounts.' },
  { t: 'Suspense for code splitting', d: 'Suspense wraps lazy components showing fallbacks during chunk load; fallback design matters (skeletons beat spinners). Nested Suspense boundaries stagger reveals independently. Streaming SSR extends the same primitive server-side flushing shell early, data later.' },
  { t: 'useTransition', d: 'useTransition marks updates non-urgent keeping UI responsive during heavy renders: [isPending, startTransition]. Typing into filtered lists stays snappy while list recalculates concurrently. Transitions interrupt cleanly when newer input arrives \u2014 concurrency\u2019s flagship UX win.' },
  { t: 'useDeferredValue', d: 'useDeferredValue(input) returns lagging copy scheduling dependent renders at lower priority \u2014 alternative to transition-wrapping when modifying the producer is inconvenient (child components, third-party inputs). Pair with memoized heavy children so deferred renders actually skip redundant work.' },
  { t: 'useOptimistic', d: 'useOptimistic wraps state showing speculative values during pending async actions, reverting automatically on completion/error. Form-heavy flows feel native-app instant. Combine with useActionState for full optimistic form pipelines including validation feedback loops.' },
  { t: 'form actions and useActionState', d: '<form action={serverOrClientFn}> submits progressively working sans JS; useActionState wraps actions capturing prevState returning state for messages/errors. Pending states arrive via useFormStatus inside submitting components. This stack replaces onSubmit-preventDefault choreography with platform-aligned ergonomics.' },
  { t: 'use hook for promises/context', d: 'use(promiseOrContext) reads values conditionally inside components suspending on pending promises \u2014 usable in branches unlike hooks rules-constrained APIs. Enables render-as-fetch patterns with cached promise creation. Pairs with Suspense/ErrorBoundary for complete loading/error UX declaratively.' },
  { t: 'Server Components overview', d: 'RSCs render exclusively on server shipping zero component JS \u2014 ideal data-bound sections hitting DBs directly. Client Components (\u201cuse client\u201d) own interactivity. Boundaries serialize props crossing directions. Mental model: server renders templates, client hydrates islands of interaction.' },
  { t: 'client boundary directives', d: '\u201cuse client\u201d marks module graphs entering client bundle \u2014 everything imported beneath becomes client code. Props crossing must be serializable (no functions/classes except RSC-as-children passthrough). Keep boundaries leaf-ward maximizing server-rendered surface area and minimizing bundle size.' },
  { t: 'hydration mismatches causes', d: 'Mismatches stem from nondeterministic render (Date.now/Math.random), locale/timezone variance between server/client, browser extensions mutating HTML pre-hydration, and invalid nesting auto-corrections. Fix via deterministic seeds, effect-time client values, suppressHydrationWarning narrowly, valid semantic markup.' },
  { t: 'list virtualization concepts', d: 'Rendering 10k rows murders DOM budgets; virtualization windows visible slice computing offsets from row height math, spacer divs faking full scroll area. react-window/virtuoso handle variable heights, sticky headers, reverse scroll. Tradeoff: find-in-page/print lose hidden rows unless overscan compensates.' },
  { t: 'profiling with React DevTools Profiler', d: 'Profiler records commits highlighting slow components (render duration flamegraph), why-they-rendered prop deltas. Interactions API labels user flows. Optimize measured offenders \u2014 memoization without profiling is superstition. Record realistic interactions; synthetic clicks mislead hot-path identification.' },
  { t: 'why-did-you-render diagnostics', d: 'WDYR logs re-render reasons comparing prev/next props pinpointing unstable references defeating memo. Development-only dependency. Complements Profiler explaining what Profiler observes \u2014 together they close the diagnose loop for render-churn complaints.' },
  { t: 'key remount trick', d: 'Setting key={resetToken} forces subtree teardown rebuilding fresh state \u2014 legitimate reset mechanism for wizards/forms when imperative reset APIs absent. Document intentional remounts; accidental key instability elsewhere causes state loss bugs worth grepping during reviews.' },
  { t: 'defaultProps deprecation', d: 'Function component defaultProps lost support; migrate to parameter defaults (function C({size="md"})) providing identical behavior with better typing inference. Class components retain defaultProps. Codemods exist; typing benefits alone justify proactive migration in TS codebases.' },
  { t: 'component file organization conventions', d: 'Conventions vary: colocate component+styles+tests per folder (feature folders), centralize ui/ primitives, separate container/presentational historically. Consistency beats specific choice; barrelling exports aids imports but watch circularity. Tests adjacent to source encourage maintenance proximity.' },
  { t: 'typing components with TypeScript', d: 'Type props via interface/type annotations: ({title, count}: Props). Extend HTMLAttributes for pass-through polymorphism; ReactNode types children broadly; discriminated unions encode variant props (variant: "solid"|"ghost" with color requirements). Generics enable typed list/select components preserving item types end-to-end.' },
  { t: 'testing React components philosophy', d: 'Testing Library queries by role/text mimicking user perception over implementation selectors; fireEvent/user-event simulate interactions awaiting async updates via findBy/waitFor. Assert observable outcomes (button disabled, message shown), not state internals. A11y roles double as query targets reinforcing inclusive markup.' },
  { t: 'mocking network in tests', d: 'MSW intercepts fetch/XHR at service-worker level serving fixture responses, shared across tests/storybook/dev. Handlers-per-test override defaults. Unlike axios mocks, MSW tests actual client code paths including headers/auth wiring. Reset handlers between tests preventing cross-contamination.' },
  { t: 'snapshot testing caution', d: 'Snapshots catch unintentional markup drift but rot into approve-everything rituals on large trees. Limit to small stable leaves (icons, badges); prefer explicit behavioral assertions elsewhere. Review snapshot diffs like code \u2014 blind approvals neutralize the safety net entirely.' },
  { t: 'E2E testing with Playwright basics', d: 'Playwright drives real browsers with auto-waiting locators (getByRole), traces, screenshots, parallel workers, and multi-browser matrices. Test critical journeys: signup, checkout, search. Keep suites lean \u2014 E2E cost multiplies; push coverage pyramid weight downward into integration/unit layers.' },
  { t: 'accessibility essentials in React', d: 'Semantic HTML first (nav/main/button), labels bound to inputs, alt meaningfulness, focus management on route/modal changes, keyboard operability (no div-onclick), aria-live for async announcements. Testing Library role queries enforce semantics organically. axe lints automated subsets; manual screen-reader passes remain irreplaceable.' },
  { t: 'focus management in SPAs', d: 'Route changes should move focus to page heading (tabIndex=-1 + focus) announcing navigation to SR users; modals trap focus cycling Tab within, restoring trigger focus on close. Skip-links jump past repeated nav. Focus visibility must remain \u2014 never outline:none without :focus-visible replacement.' },
  { t: 'CSS strategies comparison', d: 'Choices span plain CSS/BEM files, CSS Modules (scoped hashes), utility Tailwind (composition speed, purged size), CSS-in-JS (runtime cost, dynamic theming) and vanilla-extract/Panda (zero-runtime typed styles). Team skillset, SSR constraints, and design-token maturity drive selection more than benchmarks.' },
  { t: 'Tailwind adoption tradeoffs', d: 'Tailwind compresses styling into co-located utilities accelerating iteration and killing naming debates; costs include verbose class strings, learning-curve ramp, and abstraction leakage into JSX. extractComponents/componentization tames repetition. Purge produces tiny production CSS despite dev verbosity.' },
  { t: 'theming implementation approaches', d: 'Theme via CSS custom properties switched by data-theme attributes (fast, no re-render) versus context-driven token objects (typed, dynamic). Dark mode respects prefers-color-scheme with manual toggle persistence. Ensure contrast parity across themes and respect forced-colors accessibility modes.' },
  { t: 'animation approaches ranked', d: 'CSS transitions/keyframes cover most micro-interactions cheaply (GPU-friendly transform/opacity only); Web Animations API programmatic control; FLIP technique animates layout changes smoothly by inverting transforms; framer-motion/react-spring add physics/orchestration DX. Respect prefers-reduced-motion globally.' },
  { t: 'FLIP animation technique', d: 'First measure position, Last apply final layout, Invert via transform to original, Play transition removing inversion \u2014 animating compositor-friendly transforms instead of costly layout properties. Powers smooth list reorders/share-element transitions without layout thrash loops.' },
  { t: 'environment configuration in Vite apps', d: 'import.meta.env exposes VITE_-prefixed variables statically replaced at build; .env.development/.env.production layer per mode; secrets must live server-side \u2014 anything bundled ships publicly. Runtime-config endpoints serve deploy-time values. Type augmentation via ImportMetaEnv interface keeps usage typed.' },
  { t: 'proxying APIs in development', d: 'Vite server.proxy forwards /api to backend dodging CORS locally while mirroring prod topology. Configure ws:true for websockets, rewrite paths as needed. Never ship proxy reliance \u2014 production relies on same-origin deployments or proper CORS headers instead.' },
  { t: 'code splitting strategy beyond routes', d: 'Split heavy leaves lazily: chart libraries behind tabs, editors behind edit-mode, admin panels behind role gates. Analyze bundles (rollup-plugin-visualizer) targeting surprise offenders (moment locales era). Manual chunks group stable vendors for long-lived caching independent of app churn.' },
  { t: 'tree shaking verification workflow', d: 'Import production builds into bundle analyzer confirming unused locales/components absent. Common culprits: lodash CJS imports (use lodash-es), barrel re-exports with side effects, date library plugin misconfig. sideEffects field in package.json declares purity enabling aggressive elimination confidently.' },
  { t: 'error monitoring integration', d: 'Sentry-style SDKs capture exceptions with breadcrumbs/releases/source-maps uploads via CI. Wrap router-level boundaries reporting recoverable render failures; instrument unhandledrejection globally. Release tagging correlates error spikes with deploys enabling rapid rollback decisions backed by evidence.' },
  { t: 'web vitals collection', d: 'web-vitals library reports LCP/INP/CLS (+TTFB/FCP) to analytics; field data trumps lab scores for real-user insight. Segment by device/connection/route diagnosing regressions. Alert on percentile regressions (p75) aligning with Search Console thresholds rather than averages masking tails.' },
  { t: 'PWA essentials checklist', d: 'HTTPS, manifest (name/icons/display/theme), service worker precaching appshell with runtime caching strategies, offline fallback route, install prompt UX respecting dismissal. Update flows notify users of new versions gracefully. Audit via Lighthouse PWA category iterating gaps methodically.' },
  { t: 'i18n integration patterns', d: 'react-i18next/next-intl externalize strings keyed hierarchically, ICU plurals handle grammar counts, interpolation escapes safely. Locale detection negotiates navigator/storage/path prefixes; lazy-load translation namespaces per route. RTL flips via dir attribute plus logical CSS properties avoiding directional hacks.' },
  { t: 'form validation architecture', d: 'Schema-first (zod/yup) validates once, feeds types and runtime both: resolver wires into react-hook-form delivering errors per field. Validate on blur/submit balancing nagging vs surprise; server errors merge into field states. Accessibility ties errors via aria-describedby linking messages to inputs.' },
  { t: 'react-hook-form advantages', d: 'Uncontrolled-by-default registration minimizes re-renders versus fully controlled form libraries; Controller bridges controlled widgets (date pickers). Performance shines on giant forms; devtools inspect field states. Combined with zod resolvers yields type-safe end-to-end form contracts.' },
  { t: 'design system integration', d: 'Design systems ship tokens (color/space/radius) consumed by themes plus component libraries enforcing consistency. Version governance, codemods for breaking changes, contribution workflows distinguish mature systems. Wrap third-party primitives adapting APIs rather than fighting their internals during adoption.' },
  { t: 'story-driven component development', d: 'Storybook isolates components with controllable args/state mockings, doubling as living documentation and visual-regression baselines (Chromatic). Stories per variant/state matrix catch edge styling early. Interaction tests script user flows inside stories merging QA into authoring workflow.' },
  { t: 'monorepo considerations for frontend', d: 'Monorepos (turborepo/nx/pnpm workspaces) unify shared UI/config packages with affected-only builds and remote caching speeding CI. Versioning strategy (fixed vs independent) shapes release cadence. Boundaries enforcement keeps app-specific code leaking into shared packages contained.' },
  { t: 'micro-frontends honest assessment', d: 'Micro-frontends partition ownership/deployment across teams via module federation/iframes/routes stitching. Real costs: duplicated dependencies, cross-cutting consistency challenges, complex orchestration. Adopt for organizational scale problems, not technical elegance \u2014 most apps benefit from modular monolith discipline instead.' },
  { t: 'React Compiler direction', d: 'React Compiler auto-memoizes components/values based on static analysis, promising removal of hand-written useMemo/useCallback scaffolding gradually. Understanding underlying memoization remains vital until ubiquity, and compiler escape-hatches (throwaway functions) require awareness during migration phases.' },
];
