import type { Question } from '../models/question';

const ADVANCED_QUESTIONS: Question[] = [
  {
    id: 201,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Explain how JavaScript\'s event loop works, including the microtask and macrotask queues. How does this affect React\'s rendering pipeline?',
    answer: 'The event loop continuously checks the call stack and task queues. Macrotasks (setTimeout, setInterval, I/O) are dequeued one per loop iteration, while microtasks (Promise.then, MutationObserver, queueMicrotask) are drained entirely before the next macrotask. React batches state updates and schedules renders as microtasks (via unstable_scheduleCallback), meaning all state updates in a microtask are batched before a re-render. Understanding this is critical for optimizing React performance and avoiding stale closures.',
  },
  {
    id: 202,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'How does JavaScript handle memory management and garbage collection? Explain weak references and their practical use cases in React applications.',
    answer: 'JavaScript uses mark-and-sweep garbage collection. The GC roots traverse the object graph marking reachable objects, then sweeps unreachable ones. Weak references (WeakMap, WeakSet) allow holding references without preventing GC—keys must be objects and are not enumerable. In React, WeakMap is used internally for caching Fiber nodes to DOM nodes without memory leaks, and can be used for memoization caches that automatically clean up when component instances are destroyed.',
  },
  {
    id: 203,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Explain the concept of closures in depth, including how they interact with the lexical environment, variable hoisting, and the temporal dead zone. How can closures cause memory leaks in React?',
    answer: 'A closure is a function bundled with its lexical environment, capturing variables from its outer scope at creation time. The [[Environment]] slot stores a reference to the outer lexical environment. In React, closures are the root cause of stale closures in hooks: a useEffect or useCallback captures the value at render time, and if dependencies aren\'t specified correctly, the closure references stale data. Memory leaks occur when closures inadvertently keep references to large objects or DOM elements, preventing GC.',
  },
  {
    id: 204,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Describe the differences between Rest parameters, the spread operator, and Object.assign vs object spread. How do these interact with React\'s immutable update patterns?',
    answer: 'Rest parameters collect remaining arguments into an array. Spread operator iterates over iterables and expands them. Object spread ({...obj}) creates shallow copies, same as Object.assign but with different enumerable property handling and it does not trigger setters. In React, shallow copying is fundamental for immutable state updates: setState(prev => ({...prev, key: newValue})). Understanding shallow vs deep copying prevents unintended mutations that break React\'s change detection via Object.is comparison.',
  },
  {
    id: 205,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'How does JavaScript\'s prototype chain work? Compare ES6 class inheritance with prototypal inheritance and explain how React uses prototypal patterns internally.',
    answer: 'Every object has an internal [[Prototype]] property linking to another object. Property access traverses this chain until found or null. ES6 classes are syntactic sugar over prototypes—they use constructor functions and prototype methods. React internally uses prototypal inheritance for Component classes: Component.prototype.setState, Component.prototype.forceUpdate. The React.Component base class defines lifecycle methods on the prototype, and class components extend it, inheriting via the prototype chain. Functional components bypass this entirely, using hooks bound to Fiber nodes instead.',
  },
  {
    id: 206,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Explain the Symbol type and its use cases. How does React leverage Symbol properties internally?',
    answer: 'Symbol is a unique, immutable primitive used as object property keys to avoid name collisions. Symbol.for creates shared symbols across realms. React uses Symbols internally: REACT_ELEMENT_TYPE (Symbol.for(\'react.element\')) to identify React elements, REACT_FRAGMENT_TYPE for fragments, REACT_PORTAL_TYPE for portals, REACT_PROVIDER_TYPE and REACT_CONTEXT_TYPE for context. These Symbols prevent XSS attacks by ensuring that plain objects from the server cannot be mistaken for React elements.',
  },
  {
    id: 207,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'What are JavaScript Iterators, Generators, and Async Iterators? How could you use them in a React context?',
    answer: 'Iterators implement next() returning {value, done}. Generators (function*) produce iterators with yield, maintaining execution state via the [[GeneratorState]] slot. Async iterators (Symbol.asyncIterator) yield promises in for-await-of loops. In React, generators can model complex state machines (useReducer with generator sagas), async iterators can handle infinite scrolling by yielding paginated API responses, and generators in Redux-Saga orchestrate side effects declaratively.',
  },
  {
    id: 208,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Explain the difference between var, let, const, and the Temporal Dead Zone. How does hoisting behave differently for each?',
    answer: 'var hoists to function scope with undefined initialization, accessible before declaration. let and const hoist to block scope but remain in the Temporal Dead Zone (TDZ) until declaration—accessing before declaration throws ReferenceError. const requires initialization and forbids reassignment but allows mutation of object properties. In React, const is standard for components, hooks, and refs to signal immutability of binding; let for loop counters; var is avoided entirely due to unintuitive scoping.',
  },
  {
    id: 209,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'How does the `this` keyword behave in different execution contexts? Explain how React class components handle `this` binding and why arrow functions in class properties work.',
    answer: '`this` depends on invocation context: default binding (global/undefined in strict mode), implicit binding (method call), explicit binding (call/apply/bind), and new binding (constructor). Arrow functions inherit `this` lexically from enclosing scope. In React class components, methods passed as event handlers lose their `this` binding. Solutions: .bind(this) in constructor, arrow function class properties (which bind at instance creation time via class fields), or arrow functions in render (creates new functions each render).',
  },
  {
    id: 210,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'What are Proxies and Reflect in JavaScript? How could they be used for advanced React patterns like state tracking or reactivity?',
    answer: 'Proxy wraps an object and intercepts operations via handler traps (get, set, deleteProperty, has, etc.). Reflect provides methods matching Proxy traps with default behaviors. Proxies enable fine-grained reactivity: Vue 3 uses them for reactive data. In React, Proxy can track state access for automatic dependency injection, create immutable wrappers that throw on mutation, or implement observable stores. Reflect.ownKeys combined with Proxy enables deep reactive systems without explicit dependency arrays.',
  },
  {
    id: 211,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Explain the Module pattern in JavaScript (ESM vs CommonJS vs AMD). How does tree-shaking work with ES modules in a React build?',
    answer: 'ESM (import/export) is static—imports are resolved at parse time enabling tree-shaking, dead-code elimination, and cyclic dependency handling. CommonJS (require/module.exports) is dynamic, resolved at runtime, preventing static analysis. Tree-shaking relies on ES module static structure and side-effect flags (\"sideEffects\": false in package.json). Webpack/Rollup eliminate unused exports by tracing dependency graphs. In React apps, importing {useState} from \'react\' instead of React enables tree-shaking unused APIs.',
  },
  {
    id: 212,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'How does JavaScript handle concurrency with SharedArrayBuffer and Atomics? Could these be used with React\'s concurrent features?',
    answer: 'SharedArrayBuffer allows shared memory between workers. Atomics provides atomic operations (add, load, store, compareExchange) and wait/notify for synchronization. These enable high-performance concurrent data structures. React\'s concurrent features (concurrent rendering, Suspense, transitions) operate at the scheduler level, not shared memory. However, SharedArrayBuffer could be used in Web Workers processing heavy computations for React apps, with Atomics for synchronized communication without postMessage overhead.',
  },
  {
    id: 213,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'What are TypedArrays and ArrayBuffers? How do they differ from regular arrays, and what performance implications do they have for data-heavy React applications?',
    answer: 'ArrayBuffer is a fixed-length binary data buffer. TypedArrays (Int32Array, Uint8Array, Float64Array, etc.) provide views into ArrayBuffers, interpreting bytes as numeric types. They are contiguous in memory, cache-friendly, and avoid GC pressure from object allocations. For React apps processing large datasets (WebGL, canvas, audio, binary protocols), TypedArrays reduce memory overhead significantly—a million numbers in Float64Array takes 8MB vs ~32MB for a regular array of Number objects.',
  },
  {
    id: 214,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Explain the EventTarget interface and how custom events work. How does React\'s synthetic event system differ from native DOM events?',
    answer: 'EventTarget is the interface implemented by DOM elements, window, etc. It supports addEventListener, removeEventListener, and dispatchEvent with CustomEvent. React\'s synthetic event system wraps native events for cross-browser consistency, pools event objects for performance (pre-React 17), and delegates all events to the root (React 17+) or document (pre-React 17) using event delegation. Synthetic events normalize properties like nativeEvent, target, preventDefault. React 17+ uses root node delegation enabling nested React trees and micro-frontends.',
  },
  {
    id: 215,
    category: 'JavaScript Core',
    difficulty: 'Hard',
    question: 'Describe how JavaScript engines (V8, SpiderMonkey) optimize code with JIT compilation, inline caching, and hidden classes. How does this knowledge inform React coding patterns?',
    answer: 'Engines use hidden classes (shapes/maps) to track object structure, inline caching to optimize property access at the same offset, and JIT to compile hot functions to native code. Monomorphic property access (same shape) is fastest; polymorphic access degrades performance. In React, this means: avoid dynamically adding/removing object properties in frequently rendered components, keep props shapes consistent, avoid spreading large dynamic objects, and use stable prop references to help engines optimize event handler dispatch.',
  },
  {
    id: 216,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'Explain TypeScript\'s type system depth: conditional types, mapped types, infer keyword, template literal types, and recursive type aliases. How do they apply to React typing?',
    answer: 'Conditional types (T extends U ? X : Y) enable type-level logic. Mapped types ({[K in keyof T]: ...}) transform object types. infer extracts types from conditionals (e.g., ReturnType<T>). Template literal types (${A}${B}) create string literal unions. Recursive types (JSONValue = string | number | boolean | JSONValue[] | {[k: string]: JSONValue}) model nested structures. In React, these type advanced props: ComponentPropsWithoutRef infers prop types, UseInfiniteQuery result types use conditional types, and template literals type CSS-in-JS prop values.',
  },
  {
    id: 217,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'What are variadic tuple types and template literal types? How can they improve type safety in React hooks and components?',
    answer: 'Variadic tuple types ([...T, ...U]) allow spread in tuple types, enabling typing of arrays with variable-length prefixes/suffixes. Combined with generic constraints, they type functions like useReducer with discriminated unions. Template literal types create precise string unions—useful for typing CSS class variants, event handler names (on${Capitalize<string>}), or prop patterns. Example: type EventName = `on${Capitalize<string>}` constrains callback prop names to React event naming conventions.',
  },
  {
    id: 218,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'Explain covariance, contravariance, and invariance in TypeScript. How does React\'s type system leverage these concepts for component props?',
    answer: 'Covariance preserves subtyping (T extends U => G<T> extends G<U>). Contravariance reverses it (T extends U => F<U> extends F<T>). Invariance means neither applies. TypeScript functions are contravariant in parameters and covariant in return type. React component props are invariant—a component expecting exact props rejects supertypes or subtypes—ensuring prop safety. Understanding variance helps type higher-order components, render props, and generic component wrappers without unsoundness.',
  },
  {
    id: 219,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you type React Context with discriminated unions and type narrowing? Provide an advanced pattern for context that handles multiple dispatch actions.',
    answer: 'Use discriminated unions for context dispatch: type Action = | {type: \'SET_VALUE\'; payload: string} | {type: \'TOGGLE\'}. The reducer narrows action types via switch. For context value, use a generic type parameter and assert with a custom hook. Advanced pattern: create a Context type that excludes null/undefined initial value, use useReducer with the discriminated union, and provide typed dispatch via context. Type narrowing in consumers dispatches type-safe actions without manual assertions.',
  },
  {
    id: 220,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'Explain the `satisfies` operator introduced in TypeScript 4.9. How does it differ from type annotations and `as` assertions when typing React component props?',
    answer: 'satisfies checks that a value\'s type matches a constraint while inferring the most specific type, unlike annotations that widen to the constraint. It\'s stricter than `as` which asserts without verification. For React props: const theme = {primary: \'#333\', sizes: [1,2,3]} as const satisfies Record<string, string | number[]> preserves literal types while ensuring structure. This allows prop objects to retain their narrow types for autocomplete while being validated against required shapes.',
  },
  {
    id: 221,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'What are branded types and nominal typing techniques in TypeScript? How can they prevent common React bugs like passing wrong IDs between entities?',
    answer: 'Branded types use intersection with a unique brand property: type UserId = string & {__brand: \'UserId\'}. TypeScript\'s structural typing treats any string as UserId without the brand. Branding prevents mixing entity IDs: function getUser(id: UserId) rejects plain strings. Creation functions assert the brand. In React, branded types prevent passing PostId to functions expecting UserId, mixing API path parameters, or confusing entity references in state management selectors.',
  },
  {
    id: 222,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you type polymorphic React components (e.g., a Box component that renders as any HTML element)? Explain the pattern using generics and discriminated unions.',
    answer: 'Use a generic `as` prop with JSX.IntrinsicElements: type PolymorphicProps<T extends React.ElementType> = {as?: T} & React.ComponentPropsWithoutRef<T>. The component infers remaining props from the `as` value. Advanced version: separate required `as` from optional with default, use forwardRef with generically typed ref, and handle string/component types. The pattern requires Omit to avoid conflicts and uses React.forwardRef with generic type parameters constrained to ElementType.',
  },
  {
    id: 223,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'Explain how TypeScript\'s module resolution strategies (Node16, bundler, classic) affect React project configuration. What resolution mode do you use for library development?',
    answer: 'Node16 resolves imports with .js extensions (ESM-style), bundler mode allows extensionless imports matching bundler behavior, classic is legacy. For React libraries, Node16 ensures ESM compatibility requires explicit extensions in imports. The resolution affects dx: —jsx import source paths, module exports in package.json (exports field with conditions), and type declarations. For library publishing, use Node16 with proper exports map in package.json for subpath exports and dual CJS/ESM builds.',
  },
  {
    id: 224,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'What are template literal types with recursive inference? How can they enable type-safe CSS-in-JS or tailwind-like class composition in React?',
    answer: 'Recursive template literal types parse string patterns at type level: type ExtractColor<T> = T extends `bg-${infer C}` ? C : never. Combined with recursive conditional types, you can validate composed class names: type ValidClass<S> = S extends `${infer A}-${infer B}` ? ValidClass<A> & ValidClass<B> : S extends keyof Theme ? S : never. This enables build-time validation of CSS utility combinations, preventing invalid Tailwind or style combinations in React components at compile time.',
  },
  {
    id: 225,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you implement type-safe higher-order components with generics? What are the pitfalls with Omit, intersection types, and ref forwarding?',
    answer: 'Type HOCs with generic props: function withLogger<T extends object>(Wrapped: React.ComponentType<T>): React.ComponentType<T & {logLevel?: string}>. Pitfalls: Omit doesn\'t distribute over unions; intersection types can produce never if overlapping props conflict; refs require React.forwardRef with a separate generic type parameter for ref. Solution: use ComponentPropsWithoutRef for prop extraction, avoid overlapping prop names in injected props, and use a separate generic for the ref type in forwardRef.',
  },
  {
    id: 226,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'Explain how TypeScript\'s declaration merging and module augmentation work. How would you augment a third-party React library\'s types?',
    answer: 'Declaration merging allows multiple declarations of the same interface/namespace to combine. Module augmentation uses declare module \'module-name\' { interface ExistingType { newProp: type } }. For React libraries, augment DefaultTheme for styled-components, extend JSX.IntrinsicElements for custom attributes, or augment HTMLAttributes for data attributes. Pattern: create a types.d.ts file with declare module \'@library\' that extends missing exports or fixes incorrect types.',
  },
  {
    id: 227,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'What is the difference between `type` and `interface` in TypeScript? When would you choose one over the other for React component typing?',
    answer: 'Interface supports declaration merging, extends only objects, and is more performant for the compiler in large intersections. Type aliases can represent unions, intersections, primitives, tuples, and mapped types. For React: prefer interface for public API prop types (they merge and have better error messages); prefer type for internal types, union props, conditional types, and computed properties. Interfaces extend cleanly with extends, types use &. Use interface for library components, type for application-specific combinations.',
  },
  {
    id: 228,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you model Redux or Zustand stores with TypeScript generics to ensure type safety across slices, middleware, and selectors?',
    answer: 'Define state interface per slice, compose into RootState. Use generic middleware typing: type Middleware<S> = (store: Store<S>) => (next: Dispatch) => (action: Action) => any. Selectors use parameterized types: const selectUser = (state: RootState) => state.user. For Zustand, use StateCreator typing with generics for middleware composition. The pattern: infer state type from store creator, use type-safe selectors with shallow comparison typing, and typed setState with partial state or updater.',
  },
  {
    id: 229,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'What are TypeScript assertion functions and type predicates? Provide React examples for runtime validation narrowing types.',
    answer: 'Type predicates (paramName is Type) narrow types in conditional blocks. Assertion functions (asserts paramName is Type) throw on invalid state. In React: function assertDefined<T>(val: T | undefined): asserts val is T { if (!val) throw new Error() } guarantees non-null after the call. Type predicates in filter: arr.filter((x): x is DefinedType => x !== null) narrows the result type. These enable runtime validation of API responses, prop types, and context values with compile-time narrowing.',
  },
  {
    id: 230,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you handle circular type references and recursive type aliases in TypeScript? Give examples relevant to React state management.',
    answer: 'TypeScript supports recursive type aliases with indirection via interfaces or object types: type JSONValue = string | number | boolean | null | JSONValue[] | {[k: string]: JSONValue}. For React state, recursive types model nested data: type TreeNode<T> = {value: T; children: TreeNode<T>[]}. Circular references in interfaces (Parent has children referencing Child, Child has parent referencing Parent) work but require careful handling with optional/deferred properties to avoid infinite type instantiation.',
  },
  {
    id: 231,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain React Fiber architecture in detail. How does the Fiber reconciler differ from the stack reconciler, and what problems does it solve?',
    answer: 'Fiber is a complete rewrite of React\'s reconciliation engine. Each Fiber node represents a unit of work and mirrors a component instance. Unlike the stack reconciler (synchronous, recursive, un-interruptible), Fiber enables incremental rendering via a linked list tree (child, sibling, return pointers). Fibers support two phases: render (interruptible, computes work) and commit (synchronous, applies DOM mutations). This enables concurrent mode, Suspense, error boundaries, and prioritization of high-priority updates over low-priority ones.',
  },
  {
    id: 232,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What are the different phases of the React reconciliation algorithm? Explain beginWork, completeWork, and commitWork in detail.',
    answer: 'beginWork processes a Fiber, determines if it needs updates (bailout via === comparison), and returns child Fiber or null. completeWork finalizes the unit, builds effect list for DOM operations. commitWork runs in three sub-phases: before mutation (getSnapshotBeforeUpdate), mutation (DOM insertion/update/deletion via commitMutationEffects), and layout (useLayoutEffect). The work loop processes fibers in a while loop, yielding to the main thread every 5ms (via shouldYield) in concurrent mode, enabling interruption.',
  },
  {
    id: 233,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain the concept of "double buffering" in React Fiber. How does it enable concurrent rendering?',
    answer: 'React maintains two Fiber trees: the current tree (mounted in DOM) and the workInProgress tree (being computed). When an update starts, React clones the current tree to create workInProgress. During concurrent rendering, workInProgress can be partially complete. On commit, the finished workInProgress becomes the new current tree (root.current pointer swap). This double buffering ensures the user always sees a consistent UI—the current tree remains intact until the workInProgress is fully committed.',
  },
  {
    id: 234,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Describe the complete lifecycle of a React component from the perspective of the Fiber tree. How does reconciliation differ for class vs function components?',
    answer: 'For class components, Fiber stores instance and lifecycle hooks: construct => mount => componentDidMount => update => componentDidUpdate => unmount => componentWillUnmount. Functions store hook states in a linked list on Fiber.memoizedState. Reconciliation: class components check shouldComponentUpdate; function components compare dependencies of hooks. Both follow the same Fiber phases but class components use instance methods while function components read hook queue from the Fiber node. This unification enables both to coexist in the same tree.',
  },
  {
    id: 235,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the "key" prop\'s role in reconciliation? Explain how keys work with arrays, fragments, and portals, including edge cases with index-based keys.',
    answer: 'Keys help React identify which children changed by matching elements between renders. During reconciliation, React uses keys to pair old and new children via a diff algorithm that minimizes DOM operations. Index-based keys cause bugs when the array order changes (e.g., filtering, sorting, inserting) because React reuses components with the same index but different data. Keys must be stable, predictable, and unique among siblings. For fragments, keys go on Fragment components. Portals maintain their own DOM subtree but keys still differentiate portal children.',
  },
  {
    id: 236,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain React Portals in depth. What are the use cases beyond modals, and how do portals affect event propagation?',
    answer: 'Portals (createPortal(children, domNode)) render children into a DOM node outside the parent hierarchy while maintaining React tree context (context, props, event bubbling). React event bubbling follows the React tree, not the DOM tree—an event fired in a portal bubbles to the portal\'s React parent, not the DOM parent. Use cases: tooltips in overflowing containers, dropdowns that must break overflow:hidden, full-screen overlays, rendering into sibling iframes, and teleporting content to different parts of the document layout.',
  },
  {
    id: 237,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React handle server-side rendering (SSR) with hydration? What are common hydration mismatches and how do you fix them?',
    answer: 'SSR renders components to HTML string on the server, producing static markup. Hydration attaches event listeners and recreates the Fiber tree on the client by reusing server-generated DOM nodes. Hydration mismatches occur when server HTML differs from client render (e.g., browser-only APIs, random values, timezone differences). React 18\'s hydrateRoot is more lenient, but errors degrade performance. Fixes: suppressHydrationWarning for intentional differences, useEffect for browser-only code, deterministic data generation.',
  },
  {
    id: 238,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What are React Server Components (RSC)? Explain how they differ from client components, their rendering model, and the implications for bundle size.',
    answer: 'RSCs run exclusively on the server, never sent to the client. They can access databases, file systems, and backend services directly without exposing secrets. They reduce bundle size because their dependencies (markdown parsers, date libraries) never ship to the browser. Client components are marked with "use client" and hydrate on the client. RSCs can import client components but client components cannot import RSCs. The architecture enables zero-bundle-size data fetching and automatic code splitting between server and client boundaries.',
  },
  {
    id: 239,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain error boundaries in React. How do they work at the Fiber level, and what are their limitations with event handlers, async code, and server components?',
    answer: 'Error boundaries are class components implementing componentDidCatch or static getDerivedStateFromError. They catch errors during rendering, lifecycle methods, and constructors of the whole subtree below them. Fiber trees track error boundaries via a linked list; when an error propagates, React walks up the return path to find the nearest error boundary Fiber. Limitations: they don\'t catch errors in event handlers, async code (setTimeout, Promises), or SSR. For those, use try-catch or error boundaries wrapping the error source. RSCs error boundaries work differently—errors are handled during rendering.',
  },
  {
    id: 240,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React 18\'s StrictMode behave differently in development? Explain the double-invocation of effects and its purpose.',
    answer: 'StrictMode in React 18 intentionally double-invokes effects (mount-unmount-mount) in development to surface bugs with impure effects, missing cleanup, and stale closures. It also double-invokes reducers and initializers to detect impure state initialization. This simulates component remounting that can happen in concurrent mode or with Suspense. It does not affect production builds. The goal is to help developers write effects that are resilient to remounting—ensuring cleanup functions properly and side effects are idempotent.',
  },
  {
    id: 241,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the `useEvent` hook (React 18 RFC) and what problem does it solve? How does it differ from useCallback?',
    answer: 'useEvent (RFC) returns a stable function identity that always calls the latest implementation without requiring it in dependency arrays. Unlike useCallback which recreates the function reference when dependencies change (causing downstream re-renders), useEvent maintains a stable reference while the function body always accesses the latest props/state via a ref internally. This solves the problem of passing callbacks to memoized child components when the callback depends on frequently-changing values—eliminating the need for useRef-based workarounds.',
  },
  {
    id: 242,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain React\'s Automatic Batching in React 18. How does it differ from pre-18 batching, and what scenarios caused missed batches before?',
    answer: 'React 18 batches all state updates by default, including those inside setTimeout, Promises, native event handlers, and async functions. Pre-18, batching only occurred inside React event handlers; updates outside (Promises, setTimeout, native events) triggered separate re-renders per setState call. Automatic batching wraps updates in unstable_batchedUpdates internally. flushSync can opt out for cases requiring synchronous DOM reads. This improves performance by reducing intermediate renders and ensuring consistent state.',
  },
  {
    id: 243,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does the `act()` testing utility work? Explain how it ensures all state updates and effects are processed before assertions.',
    answer: 'act() wraps rendering, user events, and data fetching in tests, ensuring all pending state updates, effects, and microtasks are flushed before proceeding. It integrates with the scheduler—inside act(), React disables concurrent mode batching and processes all work synchronously. This makes test behavior deterministic. act() recursively flushes effects, including nested ones, and wraps async flows. React Testing Library\'s render and fireEvent are wrapped in act automatically. Custom hooks requiring act must explicitly use it to avoid warnings.',
  },
  {
    id: 244,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What are React\'s different build environments and entry points? Explain the difference between react, react-dom, react/jsx-runtime, and react-dom/client.',
    answer: 'react (core library), react-dom (browser renderer), react/jsx-runtime (automatic JSX transform, React 17+), react-dom/client (React 18 client API: createRoot). react-server provides Server Component primitives. Each has development (development warnings, checks) and production (optimized, stripped warnings) builds. The build system selects entry via package.json exports conditions (development vs production, node vs browser). Understanding entry points is crucial for library bundling to avoid duplicate React instances.',
  },
  {
    id: 245,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain the concept of "lanes" in React. How does the lane prioritization model enable concurrent features like useTransition and useDeferredValue?',
    answer: 'Lanes are a bitmask-based priority system replacing the expiration time model. Each update is assigned a lane (SyncLane, InputContinuousLane, DefaultLane, IdleLane, etc.). The scheduler processes lanes in priority order, with higher-priority lanes (user input) interrupting lower-priority ones (data fetching). useTransition marks updates as TransitionLane (low priority), allowing urgent updates to interrupt them. useDeferredValue debounces a value using lane priority, keeping the UI responsive while computing the deferred render.',
  },
  {
    id: 246,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the Suspense component\'s architecture? Explain how it integrates with the Fiber tree, data fetching, and streaming SSR.',
    answer: 'Suspense captures thrown promises from descendant components (typically via React.lazy or data frameworks). When a promise is thrown, the nearest Suspense boundary shows fallback while the promise resolves. In the Fiber tree, Suspense creates a Offscreen component that preserves the previous render (or shows fallback) while hidden content resolves. In streaming SSR, Suspense boundaries enable out-of-order HTML streaming: the server flushes fallback HTML immediately and replaces it once content resolves, improving time-to-first-byte.',
  },
  {
    id: 247,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React\'s event delegation work in React 17+ vs legacy? What are the implications for nested React apps and micro-frontends?',
    answer: 'Pre-React 17, React delegated all events to document, causing issues with nested React trees (event bubbling between separate React instances). React 17+ attaches event listeners to the root node (createRoot container). This enables multiple React versions on the same page, safer micro-frontend isolation, and aligns with Shadow DOM behavior. The event plugin system normalizes events before delegation. For micro-frontends sharing a page, each React root handles its own event delegation without interference, critical for gradual migration strategies.',
  },
  {
    id: 248,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain the `ref` mechanism in detail. How do refs work with function components using forwardRef, useImperativeHandle, and callback refs?',
    answer: 'Refs provide a mutable reference that persists across renders (useRef). forwardRef passes a ref through to a DOM element in a function component. useImperativeHandle customizes the exposed instance value (replaces parent-accessible methods). Callback refs (ref => instance) give finer control: React calls the callback with null on unmount and the instance on mount. Ref cleanup functions (React 19) enable more predictable resource management. Refs are stored on Fiber.stateNode for class components and Fiber.ref for function components during the commit phase.',
  },
  {
    id: 249,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React handle text content and numbers during rendering? Explain the concept of text nodes in the Fiber tree and their serialization.',
    answer: 'React treats strings and numbers as TextComponent Fibers. In the Fiber tree, they are leaf nodes without children. During reconciliation, adjacent text nodes can be coalesced into a single DOM text node for performance. React serializes text through the host config (hostConfig.createElement for text). In hydration, text nodes are matched by content. Special characters (like React-rendered whitespace, &nbsp;) are preserved. Numbers are converted to strings via String() before rendering. Excessive text node creation can impact performance in large lists.',
  },
  {
    id: 250,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the `__DEV__` global and how does React use it for development-only code? How can you leverage this in your own React library?',
    answer: '__DEV__ is a compile-time flag (set by bundlers) that enables development-only code. React uses it extensively: propTypes validation, StrictMode warnings, component stack trace collection, development warnings, and debug labels. These paths are stripped in production builds (via dead-code elimination since __DEV__ is replaced with true/false at build time). For library development, wrapping invariant checks in __DEV__ provides developer-friendly warnings without production bundle overhead—critical for libraries consumed by production builds.',
  },
  {
    id: 251,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain the complete Hook execution model. How does React match hooks across renders using the linked list on Fiber.memoizedState?',
    answer: 'Each hook call pushes a Hook node onto a singly-linked list stored on Fiber.memoizedState. The hook node contains: memoizedState (current value), baseState, baseQueue, queue (pending updates), and next (pointer to next hook). During render, React reads hooks in order from this list via a cursor (currentlyRenderingFiber.memoizedState). The order must be identical between renders—this is why hooks cannot be called conditionally. After render, the workInProgress hook list becomes the current list on commit via double buffering.',
  },
  {
    id: 252,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useState differ from useReducer internally? When would useReducer be more performant than multiple useState calls?',
    answer: 'Both use the same internal hook structure (update queue and eager state computation). useState is useReducer with a predefined reducer (basicStateReducer) that supports both direct values and updater functions. useReducer is more performant when state logic produces the same updates from multiple actions—it avoids creating multiple useState setters and their associated closures. useReducer with a stable dispatch identity also prevents unnecessary re-renders in child components compared to useState setters that are stable only in React 18+.',
  },
  {
    id: 253,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useEffect\'s timing differ from useLayoutEffect, useInsertionEffect, and useSyncExternalStore? Explain the exact commit phase order.',
    answer: 'Order during commit: 1) useInsertionEffect (fires synchronously before DOM mutations—for CSS-in-JS injection), 2) useLayoutEffect (fires synchronously after DOM mutations but before browser paint), 3) useEffect (fires asynchronously after paint, as a microtask in the passive effects queue). useSyncExternalStore forces a synchronous re-render when the external store changes to prevent tearing. Understanding this order is crucial for timing DOM measurements, injecting styles, and synchronizing with external stores.',
  },
  {
    id: 254,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain the useMemo and useCallback internal caching mechanism. How do they interact with garbage collection and the Fiber tree?',
    answer: 'useMemo caches the computed value in hook.memoizedState, useCallback caches the function reference. Dependencies stored on hook.queue.lastRenderedState are compared via Object.is. If unchanged, the previous memoizedState is returned (bailout). The cache lives on the Fiber node—it is freed when the component unmounts. Memory concerns: large memoized values on rarely-unmounted components can leak if dependencies change frequently, creating orphaned previous caches. React does not clear memoized caches proactively—they persist on the Fiber until overwritten.',
  },
  {
    id: 255,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What are the rules of hooks? Explain the underlying reason why hooks cannot be called inside conditions, loops, or after early returns.',
    answer: 'Hooks rely on call order matching between renders. React stores hooks as a linked list on the Fiber, using a cursor to traverse them. If a hook is conditionally skipped, the cursor position mismatches on subsequent renders—React reads the wrong hook\'s state, causing corruption. The React team chose call-order matching over key-based matching for simplicity and performance (no key lookups). ESLint plugin react-hooks/rules-of-hooks enforces this statically. Violations cause state corruption or infinite loops.',
  },
  {
    id: 256,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain the concept of "stale closures" in hooks. Provide a deep analysis of why useCallback and useEffect capture values at render time.',
    answer: 'Each render creates a new closure with the current props and state. useEffect\'s callback captures the values from the render it was created in—the dependency array tells React when to recreate the closure. If a dependency is omitted, the effect uses the stale captured value. useCallback similarly captures values. The root cause: JavaScript closures capture variables by reference from the execution context. Fixes: use functional state updates, include all dependencies, use useRef for mutable values (ref.current is always current), or use useEvent (RFC).',
  },
  {
    id: 257,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How do custom hooks share state and logic? Explain the difference between sharing via hooks vs HOCs vs render props, including performance trade-offs.',
    answer: 'Custom hooks share logic (not state) by composition—each component calling the hook gets isolated state stored on its own Fiber. HOCs wrap a component, injecting props via a factory—they add nesting and can cause prop name collisions. Render props pass a function as a child—they avoid HOC naming issues but create callback allocations. Custom hooks eliminate wrapper nesting, have clearer data flow, and enable composition without component hierarchy bloat. Performance: hooks minimize component tree depth vs HOCs (which add wrapper components).',
  },
  {
    id: 258,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain how useRef works internally. How does it differ from creating a plain object outside the component?',
    answer: 'useRef initializes {current: initialValue} stored on hook.memoizedState. On subsequent renders, the same object reference is returned—the ref object persists across renders. A plain object outside the component ({current: value}) is a module-level singleton shared across all component instances and persists for the module lifetime—this causes memory leaks and cross-instance state sharing. useRef is per-instance, stored on the Fiber, and cleaned up when the component unmounts. The ref identity stability is guaranteed for the component lifetime.',
  },
  {
    id: 259,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useDeferredValue work under the hood? How does it leverage React 18\'s concurrent mode to keep the UI responsive?',
    answer: 'useDeferredValue accepts a value and returns a deferred copy that lags behind the original. Internally, it schedules the update with a lower lane priority (DeferredLane). When the original value changes, React first commits the current value with high priority, then re-renders with the deferred value at lower priority. This enables keeping previous UI visible while the deferred render computes. If a high-priority update occurs during deferred rendering, React abandons the deferred render and restarts—ensuring the UI stays responsive to user input.',
  },
  {
    id: 260,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the implementation difference between useState and useSyncExternalStore? When should you prefer useSyncExternalStore?',
    answer: 'useState uses React\'s internal update queue and batching. useSyncExternalStore subscribes to an external store and forces synchronous re-renders to prevent tearing (different components seeing different store values). It takes subscribe and getSnapshot callbacks, and an optional getServerSnapshot for SSR. Internally, useSyncExternalStore calls store.getSnapshot during render, and if the value differs from the last render, forces a synchronous re-render. Use it for external state libraries (Redux, Zustand) to guarantee consistency.',
  },
  {
    id: 261,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain the useEffect cleanup function. What happens if the component unmounts before the cleanup, or if the cleanup function throws?',
    answer: 'The cleanup function runs before the component unmounts (for the last effect) and before re-running the effect (due to dependency change). Cleanup order: last-in-first-out (stack order). If the component unmounts before the cleanup runs (e.g., fast unmount after mount), React still runs the cleanup function. If cleanup throws, React logs the error to console.error (via the error boundary infrastructure) but continues cleanup of other effects. Unhandled cleanup errors can cause memory leaks if subscriptions or timers are not cleaned up.',
  },
  {
    id: 262,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How do you test custom hooks? Explain the testing patterns for hooks that use context, refs, and effects.',
    answer: 'Use renderHook from @testing-library/react-hooks (or @testing-library/react v14+ built-in). It creates a TestComponent wrapper, runs the hook, and returns result.current. For context, wrap with a provider via wrapper option. For refs, mock or provide ref objects. For effects, use act() and waitForNextUpdate. Advanced: test concurrent behavior with deferred values using act(async () => ...). For hooks with async operations, use waitFor or waitForValueToChange. The key: renderHook handles unmounting, effect cleanup, and batched updates automatically.',
  },
  {
    id: 263,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the relationship between hooks and the component lifecycle? How do you map class lifecycle methods to hooks?',
    answer: 'componentDidMount: useEffect(() => {}, []). componentDidUpdate: useEffect(() => {}) (no deps) or useRef for skip-first pattern. componentWillUnmount: useEffect(() => fn, []) cleanup. getDerivedStateFromProps: useMemo or useState with useEffect for sync. shouldComponentUpdate: React.memo. getSnapshotBeforeUpdate: useLayoutEffect with DOM measurements. constructor: useState initializer or useRef. There is no exact hook equivalent for componentDidCatch—error boundaries remain class components. The mental model shifts from lifecycle events to synchronization with state.',
  },
  {
    id: 264,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain how React 18\'s useId hook works. How does it generate unique IDs that are consistent between server and client?',
    answer: 'useId generates unique, stable IDs for accessibility attributes (aria-describedby). It uses a tree-based identifier derived from the component\'s position in the Fiber tree. The ID is constructed from the component\'s path: a base ID (root) plus sibling index and depth. On the server, IDs are generated the same way ensuring hydration matches. useId avoids hydration mismatches that manual random IDs cause. The format includes a colon (:) to avoid conflicts with CSS selectors and HTML id restrictions.',
  },
  {
    id: 265,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useTransition work? Explain the isPending state and how it interacts with Suspense and the Scheduler.',
    answer: 'useTransition returns [isPending, startTransition]. startTransition marks updates as transitions (low priority lane). React shows the current UI while transition renders compute. isPending true indicates the transition hasn\'t committed yet—enables showing loading spinners without triggering Suspense fallbacks (which unmount existing UI). Transitions can be interrupted by urgent updates (clicks, typing). The scheduler expires old transitions to avoid starvation. useTransition is key for keeping UI responsive during expensive re-renders and data loading.',
  },
  {
    id: 266,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the useDebugValue hook and how does it integrate with React DevTools? How can you format custom hook values for inspection?',
    answer: 'useDebugValue labels custom hooks in React DevTools. It accepts a value and optional formatter function: useDebugValue(date, d => d.toISOString()). The formatter is called lazily only when DevTools is open. It does not affect production behavior. For compound values, use multiple useDebugValue calls or format into a readable string. Pattern: useDebugValue({count, step}, ({count, step}) => `Count: ${count} (step: ${step})`). This is purely for developer experience with zero production impact.',
  },
  {
    id: 267,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain how to implement a custom hook that uses refs to avoid stale closures. What is the "ref callback pattern"?',
    answer: 'Store the latest callback in a ref: const ref = useRef(fn); ref.current = fn; then use ref.current inside effects/timeouts instead of fn directly. This avoids stale closures since ref.current always points to the latest function. The "ref callback pattern" uses useCallback with a ref to provide a stable callback identity that calls the latest implementation: const cb = useCallback((...args) => ref.current(...args), []). This is essentially what the useEvent RFC formalizes. Trade-off: extra ref assignment per render.',
  },
  {
    id: 268,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How do you optimize re-renders triggered by context with hooks? Explain the useMemo-based context splitting pattern.',
    answer: 'Context re-renders all consumers when any value changes. Split context into multiple smaller contexts (state + dispatch) to narrow re-render scope. Use useMemo for the context value: const value = useMemo(() => ({state, dispatch}), [state]). Without useMemo, every parent render creates a new object reference, causing all consumers to re-render. Pattern: separate "state context" and "dispatch context" (stable dispatch identity avoids unnecessary re-renders). Use useContextSelector (or zustand/use-context-selector) for selector-based subscriptions.',
  },
  {
    id: 269,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the difference between mount-time effects and update-time effects? How does React distinguish them internally?',
    answer: 'React does not distinguish internally—useEffect fires after every render (mount and updates) depending on dependency changes. The "mount-only" pattern ([] deps) works because the effect skips on re-renders when deps haven\'t changed. Internally, each hook has a flag (HookFlags) tracking if it\'s the first render (HasEffect flag). Mount effects run the callback; update effects run cleanup then callback if dependencies changed. The pushEffect function creates the effect object with flags for passive (useEffect) vs layout (useLayoutEffect) timing.',
  },
  {
    id: 270,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain how the React Compiler (React Forget) plans to auto-memoize hooks and components. What are the implications for manual useMemo/useCallback usage?',
    answer: 'React Forget is a compiler plugin that automatically memoizes values and callbacks by analyzing re-computation and referential stability. It identifies pure computations and dependencies, inserting memoization cache reads/writes automatically. The goal: eliminate manual useMemo/useCallback, reducing developer burden and errors (stale deps, over-memoization, under-memoization). Initially, it will coexist with manual hooks; eventually, manual memoization may become optimization for edge cases the compiler cannot prove safe.',
  },
  {
    id: 271,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'Compare Redux, Zustand, Jotai, and Recoil from an architectural perspective. How do their state update and subscription models differ?',
    answer: 'Redux uses a single immutable store with action dispatch and reducer—subscriptions are to the entire store (selectors do shallow comparisons). Zustand uses a single store with direct mutation via set() and selector subscriptions with equality checking. Jotai uses atomic state (atoms) with dependency graph-based subscriptions—only components reading affected atoms re-render. Recoil uses atoms and selectors with a directed acyclic graph and synchronous transaction model. The key differences: Redux/Zustand are flux-like single stores, Jotai/Recoil are atomic with fine-grained reactivity.',
  },
  {
    id: 272,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'Explain the concept of "selector tearing" in state management. How does useSyncExternalStore solve this problem?',
    answer: 'Tearing occurs when a synchronous state update causes some components to see the new value while others still see the old value during the same render pass—resulting in visual inconsistency. This is possible with external stores (Redux) during concurrent rendering where React yields before completing the render. useSyncExternalStore forces a synchronous re-render when the snapshot changes, preventing React from interleaving renders with partially updated external store state. It guarantees all components see consistent external store values during a render.',
  },
  {
    id: 273,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'How does Zustand\'s store subscription system work under the hood? How does it avoid unnecessary re-renders with selector equality?',
    answer: 'Zustand creates a vanilla store with subscribe/getState/setState API. The React binding (create) subscribes to the store via useSyncExternalStore. Selectors extract a derived value; Zustand compares using Object.is (or custom equality). Only when the selected value changes does the component re-render. The subscription model is push-based (store emits to all subscribers), but the selector comparison filters updates. Zustand v5 uses useSyncExternalStoreWithSelector internally for efficient selector-based subscriptions with equality checking.',
  },
  {
    id: 274,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'What is Jotai\'s atom dependency tracking? How does it optimize re-renders compared to Redux\'s selector pattern?',
    answer: 'Jotai tracks dependencies at the atom level using a dependency graph. When an atom\'s value is read during render, Jotai records which atoms were accessed. Updates propagate only to dependent atoms and their subscribers. This is more granular than Redux where selectors run on every dispatched action, requiring memoization (reselect). Jotai eliminates selector memoization because atom derivation is automatic; only reachable atoms re-compute. The trade-off: dynamic dependency tracking introduces overhead for very simple apps.',
  },
  {
    id: 275,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'Explain the concept of "atomic state" vs "flux state." What are the trade-offs for large-scale applications?',
    answer: 'Atomic state (Jotai, Recoil) breaks state into small independent units (atoms) that compose via derived atoms. Benefits: fine-grained re-renders, natural code splitting, lazy initialization. Challenges: complex dependency graphs are hard to debug, circular dependencies possible, serialization requires traversing the atom graph. Flux (Redux, Zustand) keeps all state in a single store. Benefits: predictable, easy to serialize/deserialize, devtools-friendly, reducers are pure. Challenges: selector optimization, action boilerplate, monolithic store growth.',
  },
  {
    id: 276,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'How does Redux middleware work? Implement the thunk middleware from scratch and explain its interaction with the store\'s dispatch chain.',
    answer: 'Redux middleware wraps store.dispatch in a composable chain. Each middleware receives ({getState, dispatch}) => next => action => {...}. Thunk middleware: if (typeof action === \'function\') return action(dispatch, getState); return next(action). Middleware forms a pipeline: action enters the first middleware, passes through next() to subsequent middleware, finally reaching the reducer. Async thunks dispatch regular actions after async operations. The middleware signature enables side effects, logging, crash reporting, and async flows without modifying the core dispatch.',
  },
  {
    id: 277,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'Explain Redux Toolkit\'s createAsyncThunk and createSlice. How does immer integration enable mutable-style updates with immutable state?',
    answer: 'createSlice generates action creators and reducers from a name, initial state, and reducer functions object. Immer\'s produce wraps reducers, allowing "mutating" syntax that produces immutable updates via Proxy-based drafts. Mutations in the draft produce a new immutable state. createAsyncThunk creates three action types (pending, fulfilled, rejected) and thunks for async flows with request status tracking. Extra reducers handle these actions. The builder callback pattern enables type-safe reducers for async thunk actions.',
  },
  {
    id: 278,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'What is the "state colocation" principle? How does it interact with global state management libraries?',
    answer: 'State colocation means keeping state as close as possible to where it\'s used, ideally within the component or its nearest ancestor. Only state shared among distant components should be globalized. This reduces coupling, improves maintainability, and simplifies reasoning. Global state libraries should complement, not replace, local state. Pattern: start with useState/useReducer, lift state up when needed, introduce context for medium sharing, and only reach for Zustand/Redux for truly global or cross-cutting concerns (auth, theme, cache).',
  },
  {
    id: 279,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'How do you handle optimistic updates and rollback in state management? Demonstrate the pattern with Zustand or Redux.',
    answer: 'Optimistic updates apply mutations to the UI before server confirmation, with rollback on failure. In Zustand: save previous state before update, set optimistic state, revert on error. In Redux: dispatch OPTIMISTIC_UPDATE with a unique transaction ID, store backup state. On server success: confirm. On error: dispatch ROLLBACK restoring backup. Key considerations: unique request tracking (transaction IDs), preventing stale optimistic updates (discard old transactions), and handling concurrent optimistic mutations (merge conflicts). Use libraries like TanStack Query for built-in optimistic update support.',
  },
  {
    id: 280,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'Explain the difference between controlled and uncontrolled state in React. When would you use an uncontrolled pattern for performance?',
    answer: 'Controlled: React manages the value via state and onChange handler—single source of truth. Uncontrolled: DOM manages its own state via refs—React reads values only when needed. Uncontrolled improves performance for large forms because it avoids re-renders on every keystroke. However, controlled is necessary for validation, conditional inputs, and programmatic value changes. Hybrid: uncontrolled with ref and useImperativeHandle for parent-triggered operations. React 19\'s useFormStatus and useFormState bridge the gap.',
  },
  {
    id: 281,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'How does TanStack Query (React Query) manage server state? Explain its caching, stale-while-revalidate, and background refetching mechanisms.',
    answer: 'TanStack Query stores server state in a normalized cache (keyed by queryKey). Queries have configurable staleTime (how long data is considered fresh), cacheTime (how long inactive data persists), and refetchOnMount/WindowFocus/Reconnect. Stale-while-revalidate pattern: serve cached data immediately, then refetch in background. The cache uses a hash map with garbage collection for inactive queries. Background refetching triggers on mount, window refocus, and network reconnection. Optimistic updates via queryClient.setQueryData with rollback support.',
  },
  {
    id: 282,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'What is the "state machine" pattern in React applications? How does XState integrate with React for complex state logic?',
    answer: 'State machines model application states as finite states with explicit transitions, guards, and actions. This prevents impossible states and makes state logic deterministic. XState provides state machine creation with actors, services, and hierarchical states. Integration with React: @xstate/react provides useMachine (creates service, subscribes to state, sends events). Actor-based approach enables testing state logic independently of React components. Benefits: inviolable state transitions, visual modeling, time-travel debugging, and orthogonal states for parallel state management.',
  },
  {
    id: 283,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'How does Valtio differ from Zustand in its proxy-based approach? What are the performance characteristics of Proxy-based state?',
    answer: 'Valtio uses Proxy-based state objects: you mutate state directly (state.count++) which triggers re-renders via Proxy traps on set. Zustand requires explicit set() calls with immutable updates. Valtio\'s Proxy approach provides more natural mutation syntax but incurs proxy overhead on every property access/set. Performance considerations: Proxy handles access on deeply nested properties, which can be slower than plain object access (2-5x). However, Valtio optimizes with version counters and snapshot comparison to minimize re-renders.',
  },
  {
    id: 284,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'What are "derived values" and "derived state" in state management? How do libraries like Zustand and Jotai handle computed values differently?',
    answer: 'Derived values are computed from other state. In Zustand, derived values are computed in selectors or getters: getCount: (state) => state.items.length. Selectors recompute on every state change. In Jotai, derived atoms compose automatically: const doubledAtom = atom((get) => get(baseAtom) * 2) with automatic dependency tracking—only re-computes when dependencies change. Redux uses reselect for memoized selectors. The trade-off: Jotai\'s automatic memoization vs Zustand/Redux\'s explicit selector memoization. Both avoid redundant computation.',
  },
  {
    id: 285,
    category: 'State Management',
    difficulty: 'Hard',
    question: 'How do you handle undo/redo functionality in complex state management? Explain the command pattern and state history implementation.',
    answer: 'Implement a history stack: store {past: State[], present: State, future: State[]}. Each mutation pushes the previous present to past, clears future. Undo: set present to past.pop(), push current present to future. Redo: set present to future.pop(), push current present to past. Use Redux middleware (redux-undo) wrapping the reducer. Zustand: middleware that intercepts set() and maintains history. With Zustand immer middleware, clone state before mutation via structuredClone or immer\'s produce. Constraints: limit history size to prevent memory bloat, skip intermediate states for continuous inputs.',
  },
  {
    id: 286,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'Explain React\'s rendering phases in detail: render phase, commit phase, and passive effect phase. What work happens in each, and which can be interrupted?',
    answer: 'Render phase (can be interrupted): beginWork/completWork traversal, fiber diffing, hook evaluation, reconciliation—produces effect list of DOM mutations. Commit phase (synchronous, cannot be interrupted): DOM mutations via commitMutationEffects (insert/update/delete), ref attachment/detachment, useLayoutEffect fire. Passive effect phase (microtask): useEffect fire, useInsertionEffect cleanup. Interruptibility is key: only render phase can be paused (concurrent mode). The commit phase must complete atomically for UI consistency. Passive effects are deferred to avoid blocking paint.',
  },
  {
    id: 287,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'What causes unnecessary re-renders in React? Explain the difference between "render" (function call) and "commit" (DOM mutation).',
    answer: 'A re-render happens when: state changes, context changes, parent re-renders, or forceUpdate is called. Render phase: component function executes, returns JSX, React reconciles—this is cheap if no DOM changes. Commit phase: actual DOM mutations—expensive. Unnecessary re-renders are render calls that produce the same output (React still diffs). React.memo skips re-render if props are === equal. Performance issue is not re-renders per se but wasted reconciliation work and effect re-runs. Profile with React DevTools to identify expensive re-renders.',
  },
  {
    id: 288,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'How does React.memo work internally? Compare its bailout logic with PureComponent and shouldComponentUpdate.',
    answer: 'React.memo wraps a component, returning a memoized version that performs shallow prop comparison (Object.is) to skip re-rendering. Internally, during beginWork, the memo component checks its type (memoized) and does shallowEqual on oldProps vs newProps. If equal, React bails out of reconciliation for that subtree (returns same Fiber, does not process children). PureComponent does shallow comparison of props and state in shouldComponentUpdate. React.memo is the functional equivalent of PureComponent. Both compare only first-level properties—nested mutations bypass the check.',
  },
  {
    id: 289,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'Explain the "key" prop\'s impact on performance. When do changing keys cause unnecessary DOM remounting vs reconciliation benefits?',
    answer: 'Keys identify elements across renders for stable identity. Changing a key forces React to unmount the old component (destroying state, effects, DOM) and mount a new one—expensive but sometimes necessary (reset internal state). Stable keys let React move/reorder DOM nodes without remounting. Anti-pattern: using array index as key when list order changes—causes React to reuse components with stale state (wrong data shown). Use stable unique IDs. Key changes are intentional for "reset component" pattern.',
  },
  {
    id: 290,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'How does React\'s virtual DOM differ from "shadow DOM"? Explain the performance characteristics of the virtual DOM diffing algorithm.',
    answer: 'Virtual DOM is a lightweight JavaScript representation of the real DOM (objects, not browser DOM nodes). React diffs virtual DOM during reconciliation to compute minimal DOM operations. Shadow DOM is a browser API for DOM encapsulation (style scoping, element isolation)—not related to React. Virtual DOM performance characteristics: O(n) diffing heuristic based on type checking, key matching, and props comparison (not full tree diff which is O(n^3)). The heuristic assumes same-type components produce similar subtrees, enabling linear-time reconciliation.',
  },
  {
    id: 291,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'What is "windowing" or "virtualization"? Explain how libraries like react-window and react-virtuoso achieve rendering of large lists with constant DOM nodes.',
    answer: 'Windowing renders only the visible subset of a large list, keeping total DOM nodes constant regardless of list size. react-window uses a container with overflow:auto, measures scroll position, calculates which rows are visible, and renders only those plus overscan buffer. react-virtuoso uses dynamic measurement via IntersectionObserver for variable-height items. Performance gains: O(1) DOM nodes vs O(n). Libraries use absolute positioning for item placement, scroll event listeners, and dynamic measurement (ResizeObserver) for adaptive item heights.',
  },
  {
    id: 292,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'Explain the performance implications of creating functions in render. How does this affect React.memo and downstream memoized children?',
    answer: 'Creating functions in render (onClick={() => ...}) creates a new reference each render. If passed to a React.memo child, the memo check fails (reference inequality), causing re-render. Solutions: useCallback (stable reference if deps unchanged), useEvent (stable reference calling latest closure), ref-stable callbacks. However, function creation itself is cheap (~nanoseconds). The real cost is the cascading re-render of deep component trees when memo boundaries are broken. Profile before optimizing—inline handlers on simple DOM elements are not performance issues.',
  },
  {
    id: 293,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'How does the React Profiler (Profiler component and DevTools) measure render performance? Explain the onRender callback parameters.',
    answer: '<Profiler id="name" onRender={callback}> measures render timing. onRender receives: id, phase ("mount"|"update"), actualDuration (time spent rendering component and children), baseDuration (estimated time without memoization), startTime, commitTime, interactions (from Scheduler tracing). The profiler uses performance.now() for timing. DevTools Profiler records flamegraphs of commit durations, enabling identification of expensive renders and unnecessary re-renders. Interaction tracing correlates renders with user interactions for performance attribution.',
  },
  {
    id: 294,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'What are "render bailouts" in React? List all conditions that cause React to skip rendering a component subtree.',
    answer: 'Bailouts occur when: 1) React.memo/PureComponent shallow equality passes, 2) Class component shouldComponentUpdate returns false, 3) State/context update produces same value (Object.is), 4) Parent bailout skips child reconciliation. During beginWork, React calls updateFunctionComponent which checks props equality for memo types. On bailout, React clones the existing child Fiber without processing its children—bailout propagates down. Bailout does not prevent re-render of the component itself—it prevents reconciliation of its children.',
  },
  {
    id: 295,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'Explain the performance impact of Context on re-rendering. Why does Context cause all consumers to re-render, and how do you mitigate this?',
    answer: 'When context value changes, React marks all Consumer Fibers (useContext or Context.Consumer) in the subtree for update during reconciliation. This happens because React does not know which part of the value each consumer reads—so it must re-render all of them. Mitigations: split into multiple contexts (state, dispatch, specific domains), use useMemo on context value to prevent unnecessary value changes, use React.memo on consumers (but they still re-render if context changes), or use third-party libraries (useContextSelector) that implement selector-based context subscriptions.',
  },
  {
    id: 296,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'How does React handle batching of updates? Explain how React 18\'s automatic batching works under the hood with the Scheduler.',
    answer: 'React batches updates by placing them into the same update queue entry. Pre-18, only React event handlers were batched; setTimeout, Promises, and native events were not. React 18 uses the Scheduler to batch all updates: when setState is called, React enters a batch context (execContext tracks batched vs unbatched). The Scheduler\'s unstable_scheduleCallback enqueues a task that processes all pending updates in a single render pass. flushSync exits the batch, allowing synchronous rendering. useState setters within the same synchronous block are automatically batched.',
  },
  {
    id: 297,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'What is "lazy loading" and "code splitting" in React? Explain how React.lazy, Suspense, and dynamic import() work together at the bundler level.',
    answer: 'React.lazy(() => import("./Component")) wraps dynamic import(), which Webpack/Rollup create a separate chunk at build time. When the lazy component renders, React triggers import() (loading the chunk), and falls back to Suspense fallback during loading. The bundler splits the module graph at the dynamic import boundary. React.lazy resolves the promise and renders the component. HTTP/2 multiplexing and preload hints optimize chunk loading. Suspense boundaries handle loading states declaratively. React.lazy only works with default exports.',
  },
  {
    id: 298,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'Explain the concept of "coalescing" or "debouncing" state updates. How does setState with a function updater differ from setState with a value?',
    answer: 'Function updater (setState(prev => newVal)) queues the function in the update queue—each updater receives the result of the previous updater, ensuring correct state even when multiple updates batch. Direct value (setState(newVal)) overwrites the queue. Batching coalesces multiple setState calls into one render. Debouncing (lodash.debounce with useRef) delays state updates until after a quiet period—reduces intermediate renders. For real-time inputs, consider: controlled with debounced state, uncontrolled with ref, or useDeferredValue for low-priority synchronization.',
  },
  {
    id: 299,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'How do you measure and optimize Core Web Vitals in a React application? Explain LCP, FID/INP, and CLS from a React perspective.',
    answer: 'LCP (Largest Contentful Paint): optimize by lazy-loading non-critical components, preloading hero images, using SSR/SSG for above-fold content, avoiding render-blocking resources. FID/INP (First Input Delay / Interaction to Next Paint): reduce main thread blocking via code splitting, avoid long tasks (>50ms), use useTransition for non-urgent updates, debounce expensive computations. CLS (Cumulative Layout Shift): specify image/video dimensions, avoid dynamic content injection above already-rendered content, use layout-shift-resistant CSS. Use Web Vitals library or Next.js built-in analytics for measurement.',
  },
  {
    id: 300,
    category: 'Rendering & Performance',
    difficulty: 'Hard',
    question: 'How does React\'s "key" prop with lists affect performance when combined with transitions? Explain the "List" pattern in React 18 concurrent mode.',
    answer: 'In concurrent mode, useTransition enables keeping the previous list visible while the new list renders. With stable keys, React can reuse DOM nodes even when the list reorders (via key matching). The transition ensures the list doesn\'t cause jank: the old list stays interactive while React renders the new list at lower priority. The "useDeferredValue" pattern: const deferredList = useDeferredValue(list); renders deferredList while the UI remains responsive. Stable keys prevent unnecessary DOM remounting when the transition finally commits.',
  },
  {
    id: 301,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Compound Component" pattern. How do you implement type-safe compound components with React Context and TypeScript generics?',
    answer: 'Compound components are a set of related components sharing implicit state via Context (e.g., <Select><Select.Option>). Implementation: outer component (Select) manages state and provides it via context. Sub-components (Select.Option) consume context. In TypeScript: use generic constraints for value types, create a context type with typed methods (onSelect, selectedValue). Sub-components are attached as static properties: Select.Option = OptionComponent. Benefits: declarative API, flexible layout, implicit state sharing without prop drilling.',
  },
  {
    id: 302,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the "Render Props" pattern? Compare it with Hooks for sharing cross-cutting concerns, including lifecycle and state management.',
    answer: 'Render props: a component accepts a function prop (render) that returns JSX, allowing the component to inject data. Example: <DataFetcher url="/api" render={(data) => <div>{data}</div>}/>. Hooks replaced many render prop use cases because hooks share logic without wrapper nesting ("wrapper hell"). However, render props still excel when the consumer needs explicit control over rendering order or when hook rules cannot be followed (conditional logic). Render props are also used in library APIs (Formik, React Router).',
  },
  {
    id: 303,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Higher-Order Component" (HOC) pattern. How do HOCs compose, and what are the pitfalls with refs, static methods, and display names?',
    answer: 'HOC is a function that takes a component and returns an enhanced component: const withData = (Wrapped) => (props) => {...}. Composition: withData(withRouter(withTheme(App))). Pitfalls: refs don\'t pass through (use forwardRef), static methods are lost (hoist-non-react-statics), multiple HOCs produce deep nesting in devtools, naming collisions (injected props vs consumer props), and order dependence. Modern alternatives: hooks (useData, useRouter, useTheme) avoid all these issues by keeping logic in the consuming component.',
  },
  {
    id: 304,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How do you implement a "State Reducer" pattern in React (inverted control)? Provide an example with useReducer.',
    answer: 'The state reducer pattern (from downshift) lets consumers override internal state changes by providing a reducer function. Implementation: the hook/component uses useReducer internally but exposes a prop (stateReducer) that wraps the internal reducer: (state, action) => consumerReducer(state, internalReducer(state, action)). This gives consumers control over state transitions without exposing internal state-management logic. Use case: autocomplete components where consumers intercept types or change behavior via reducer overrides.',
  },
  {
    id: 305,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Props Getter" pattern. How does it simplify component API while allowing customization?',
    answer: 'Props getters return objects of props to be spread onto elements, combining default behavior with user overrides: getInputProps({onChange: customHandler}) merges with default onChange. Implemented via composition functions that merge handlers (call both default and custom), merge className, and combine aria attributes. The key is intelligent merging: custom handlers are called after default handlers, className is concatenated, style objects are merged. This pattern avoids prop explosion while providing full customization—common in headless UI libraries (downshift, react-table).',
  },
  {
    id: 306,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the "Control Props" pattern? How do you implement controlled and uncontrolled variants of a component?',
    answer: 'Control props let consumers control component state externally (like value/onChange on input). The component supports both controlled (value prop provided) and uncontrolled (internal state via useState). Implementation: use useControlledState pattern: if value !== undefined, use controlled; else use internal state. Use ref to track if component has been controlled/uncontrolled (warn on switch). Provide defaultOnChange for uncontrolled mode. This pattern is used by Material-UI, Chakra UI for form components. TypeScript: use Value = Controlled | Uncontrolled discriminated union.',
  },
  {
    id: 307,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Provider Pattern" in depth. How do you handle nested providers, provider composition, and performance optimization?',
    answer: 'Providers use React.createContext and shared context via the component tree. Composition: <ThemeProvider><AuthProvider><RouterProvider>...</RouterProvider></AuthProvider></ThemeProvider>. Performance: each provider re-renders all consumers on value change (unless memoized). Split contexts (state + dispatch) to minimize re-renders. Nested providers of the same type merge values (e.g., nested ThemeProvider overriding specific tokens). For library development, use a "Provider composer" utility: composeProviders(providers) renders providers nested. Avoid deeply nested providers for micro-optimizations.',
  },
  {
    id: 308,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How do you implement "Compound Components" with a flexible rendering order using React.Children and cloneElement? What are the pitfalls?',
    answer: 'Iterate React.Children.map to inject props into children: React.Children.map(children, child => React.cloneElement(child, {extraProp: value})). Pitfalls: only works with direct children (not nested), breaks if children are wrapped in fragments or conditional, types with React.cloneElement are difficult, key props are fragile, and it doesn\'t function with static children. Better approaches: use Context for implicit state (compound pattern) rather than cloneElement. cloneElement is reserved for wrapper components (TransitionGroup, AnimatePresence).',
  },
  {
    id: 309,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the "Slot Pattern" (or "Children as Props") in React? How does it differ from the children prop and why is it more performant?',
    answer: 'The slot pattern passes components as named props: <Card header={<Header/>} footer={<Footer/>} body={<Body/>}/> instead of implicit children. Benefits: explicit rendering locations, better TypeScript typing (each slot typed separately), and improved performance—React.memo wrapping the parent prevents re-rendering slot components if their props haven\'t changed (unlike children where the JSX is recreated each render). The pattern enables "slots" similar to Vue or Web Components. Used in reach-ui, Adobe React Spectrum.',
  },
  {
    id: 310,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Adapter Pattern" in React. How do you create an adapter component that bridges different data sources or third-party libraries?',
    answer: 'Adapter components wrap an incompatible API and expose a standard interface. For example, adapting a REST API to a GraphQL-like interface: const UserAdapter = ({userId, children}) => { const {data} = useRestUser(userId); return children(data); }. Or adapting third-party library components to your design system by mapping props, normalizing events, and injecting styling. Adapters isolate integration points, making migration easier (swap adapter instead of all usages). Common in micro-frontends, design system migration, and third-party service integration.',
  },
  {
    id: 311,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How do you implement "Render Time Composition" vs "Compile Time Composition" in React? What are the performance implications?',
    answer: 'Compile-time composition: JSX components are nested statically—<Parent><Child/></Parent>. The bundler can optimize the module graph. Render-time composition: components are chosen dynamically based on state—{condition ? <A/> : <B/>}. This prevents bundler optimizations (code splitting is manual). For performance: compile-time composition enables React to reconcile more predictably. Render-time composition may cause component remounting if the conditional flips frequently. Use key props to maintain component identity, or use layout animations (Framer Motion AnimatePresence) for smooth transitions.',
  },
  {
    id: 312,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Observer Pattern" in React. How do you implement a publish-subscribe system for cross-component communication without Context?',
    answer: 'Implement an EventEmitter: class EventBus { private listeners = {}; on(event, fn) { ... }, emit(event, data) { ... }, off(event, fn) { ... } }. Use useRef to store the bus instance (singleton). Components subscribe via useEffect (add listener, return cleanup with off). This enables decoupled communication without prop drilling or context re-render overhead. Use cases: cross-window communication, WebSocket event routing, analytics events. Performance: subscribers only re-render when their specific event fires. Warning: can lead to unmanageable data flow if overused.',
  },
  {
    id: 313,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the "Micro-Frontend" pattern in React? How do you share components between independently deployed applications?',
    answer: 'Micro-frontends decompose a frontend into independently deployed, owned, and developed applications composed at runtime. Integration strategies: Webpack Module Federation (shared modules at runtime), iframes (isolation), Web Components (framework agnostic), or custom script injection. Shared component libraries are published as packages (npm) or via Module Federation (remote exposes). Key challenges: CSS isolation, shared dependencies (React singleton), cross-app communication (custom events, shared bus), routing coordination, and consistent design systems.',
  },
  {
    id: 314,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain the "IoC Container" (Inversion of Control) pattern in React. How do dependency injection containers like InversifyJS or tsyringe integrate with React?',
    answer: 'IoC containers manage dependency resolution and lifetimes (singleton, transient, scoped). In React, a container provider wraps the app: const container = new Container(); container.bind(TYPES.API).to(ApiService). Use context to inject dependencies: const api = useInjection(TYPES.API). The pattern supports: AOP (middleware on methods), lazy initialization, and testability (mocking via container rebind). Use with tsyringe or InversifyJS. Overuse can obscure data flow; prefer hooks for simple dependencies.',
  },
  {
    id: 315,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How do you implement "Suspense-enabled data fetching" without a framework? Explain the throw-promise pattern and its risks.',
    answer: 'Suspense data fetching throws a promise from render: const resource = {read() { if (status === "pending") throw promise; return result; }}. React catches the thrown promise, shows fallback, and re-renders when resolved. Risks: waterfalls (sequential fetch inside Suspense boundaries), promise caching required (multiple renders call read), error handling (catch thrown errors), and race conditions (stale data after navigation). Frameworks (Relay, Next.js) handle these. For custom implementation, use a resource cache with key-based deduplication and timeout invalidation.',
  },
  {
    id: 316,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'What is the "Singleton Pattern" in React? How do you implement a singleton store or service without causing issues with SSR and testing?',
    answer: 'A singleton is a module-level instance: const store = createStore(). In React, singletons cause issues with SSR (cross-request state sharing) and testing (state leakage between tests). Solutions: for SSR, use a request-scoped container (AsyncLocalStorage); for testing, use factory functions creating fresh instances per test. Zustand\'s createStore returns a factory if called in render. Singleton services (analytics, logging) are acceptable; state stores should be instantiated per app/request. Use context to provide singletons: <Provider value={singleton}>...',
  },
  {
    id: 317,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Factory Pattern" in the context of React component creation. How does it differ from conditional rendering?',
    answer: 'The factory pattern uses a function that returns component instances based on configuration: function createButton(variant: "primary" | "secondary") { return (props) => <button className={variant} {...props}/>; }. This differs from conditional rendering (<Button variant={variant}/>) by creating distinct component types at the module level. Factory pattern benefit: better TypeScript inference (each factory returns a typed component), compile-time optimization (bundler can tree-shake unused variants), and consistent component identity across renders (stable references).',
  },
  {
    id: 318,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How does the "Strategy Pattern" apply to React? Provide an example of swapping algorithms via props or context.',
    answer: 'Strategy pattern defines interchangeable algorithms encapsulated behind a common interface. In React: a component accepts a strategy prop (function or component) controlling behavior: <Sortable strategy={quickSortStrategy}>. Or context-based: const {formatStrategy} = useFormatting(); <DateDisplay date={date}/> (formatStrategy determines output). Use cases: different validation strategies for forms, different serialization for API responses, different rendering strategies (virtualized vs paginated). Strategies can be passed as props, injected via context, or plugged in via IoC containers.',
  },
  {
    id: 319,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Proxy Pattern" in React. How do you create a proxy component that controls access to another component?',
    answer: 'A proxy component wraps another component to control access, add logging, or transform props: const ProxyButton = ({children, ...props}) => { useEffect(() => { log("Button clicked") }, []); return <button {...props}>{children}</button>; }. Advanced: use Proxy (ES6 Proxy) to intercept property access on component props for validation or transformation. Use cases: access control (show/hide based on permissions), lazy rendering (defer mount until visible), performance monitoring (wrap with timing), and deprecated prop warnings.',
  },
  {
    id: 320,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'What is the "Decorator Pattern" in React? Compare it with HOCs and Hooks.',
    answer: 'Decorator pattern adds behavior to an object without modifying its structure. In React, HOCs are a form of decorator (withLogger(MyComponent)). The decorator pattern pre-dates React—it adds cross-cutting concerns. Hooks replaced many decorator use cases by attaching behavior directly: useLogger() inside component instead of withLogger(MyComponent). Pros of decorators: the decorated component remains pure; you can stack multiple concerns. Cons: indirection, naming conflicts, wrapper nesting. Modern preference: hooks for most cross-cutting concerns, decorators for library APIs.',
  },
  {
    id: 321,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Command Pattern" in React. How would you implement undo/redo with commands?',
    answer: 'Command pattern encapsulates an action as an object with execute() and undo() methods. In React: commands are created with state snapshots. Each user action creates a command: const cmd = {execute: () => applyChange(state, change), undo: () => revertChange(state, change)}. Store commands in a history stack. Undo pops last command, calls undo(). Redo stores undone commands for re-execute. Integration: wrap in a custom hook (useCommands) that maintains history and creates commands. Key: commands capture enough state to revert operations deterministically.',
  },
  {
    id: 322,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How does the "Memento Pattern" work in React state management? How does it enable time-travel debugging?',
    answer: 'Memento pattern captures and externalizes an object\'s internal state without violating encapsulation. Redux DevTools implements this: each dispatched action stores the previous state (memento). The memento (state snapshot) enables time-travel debugging—jumping between states by restoring previous snapshots. Implementation: store keeps an array of {action, state, timestamp} mementos. Time-travel: replace current state with a memento\'s state, re-calculating derived data. Performance: immutable state makes mementos cheap (structural sharing: unchanged objects are shared via references).',
  },
  {
    id: 323,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Observer Pattern" vs "Pub-Sub Pattern" in React. How do libraries like Redux and Zustand implement each?',
    answer: 'Observer pattern: subject maintains a list of observers, notifies them directly of state changes (Redux store.subscribe). Pub-Sub: event channel mediates between publishers and subscribers, decoupling them (custom EventEmitter). Redux uses Observer pattern: store notifies subscriber listeners via the dispatch loop. Zustand uses Observer pattern: store.subscribe(selector, listener). Pub-Sub is used for cross-cutting events (analytics, toast notifications). Observer gives finer control (direct subscription to store changes), Pub-Sub enables loose coupling between unrelated modules.',
  },
  {
    id: 324,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'What is the "Bridge Pattern" in React? How does it decouple an abstraction from its implementation?',
    answer: 'Bridge pattern decouples an abstraction from its implementation so both can vary independently. In React: a component\'s prop interface (abstraction) is separate from its rendering logic (implementation). Example: a Button component accepts variant prop; the implementation (styled component, CSS module, inline styles) can change without altering the Button API. A theme system is a bridge: component abstracts "what to render," theme implements "how it looks." This enables multiple themes for the same component library.',
  },
  {
    id: 325,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Facade Pattern" in React. How do you create a simplified API over complex subsystems?',
    answer: 'Facade provides a unified interface to a set of interfaces in a subsystem. In React: a custom hook that wraps multiple hooks and context consumers into a simple API: function useUserProfile() { const {user} = useAuth(); const {data} = useQuery(["profile", user.id]); const prefs = usePreferences(); return {user, profile: data, prefs, update: {...}}; }. Facades hide complexity: data fetching, caching, authentication, preference loading. Libraries like react-query are facades over fetch/cache logic. The pattern improves DX by exposing only what\'s needed.',
  },
  {
    id: 326,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How does the "Flyweight Pattern" apply to React rendering optimization? How does React share objects between renders?',
    answer: 'Flyweight minimizes memory by sharing common state among many objects. React applies this: event handlers are shared via delegation (single handler per event type on root), objects like props objects are reused across commits if unchanged, and Fiber nodes are recycled (pooled) to reduce GC pressure. React\'s keyed reconciliation reuses existing DOM nodes and component instances. For custom implementation, share heavy objects (formatters, style objects) via refs or module-level singletons to avoid recreating them per render.',
  },
  {
    id: 327,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'What is the "Mediator Pattern" in React? How does it centralize complex communication between components?',
    answer: 'Mediator defines an object that encapsulates how objects interact, promoting loose coupling. In React: context can act as mediator—instead of components directly communicating, they dispatch actions to a mediator (reducer) that coordinates state changes. Example: a form mediator (useForm) coordinates between all form fields, validation, submission, and error display without fields knowing about each other. The mediator pattern is the conceptual basis for state management libraries: Redux store mediates between actions and state updates.',
  },
  {
    id: 328,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "Composite Pattern" in React. How does React\'s component tree inherently implement this pattern?',
    answer: 'Composite pattern composes objects into tree structures to represent part-whole hierarchies. React\'s component tree is a composite: components can contain other components (children), and both leaf (DOM elements) and composite (containers) components share the same interface (they render to UI). The recursive nature of React reconciliation traverses this composite tree. The pattern enables uniform treatment: a single component can be composed of many sub-components, yet consumers interact with it as a single unit (e.g., <Card> containing <CardHeader/>, <CardBody/>).',
  },
  {
    id: 329,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How does the "Chain of Responsibility" pattern manifest in React? Consider middleware, error boundaries, and event handling.',
    answer: 'Chain of Responsibility passes a request along a chain of handlers, each deciding to process or pass to the next. In React: error boundaries form a chain—an error propagates up the component tree until caught. Redux middleware is a chain: each middleware handles or forwards the action. React event delegation follows the chain: events bubble up the DOM tree (and React tree for synthetic events). The pattern enables: adding behavior without modifying existing code (new middleware), graceful degradation (fallback boundaries), and extensible processing pipelines.',
  },
  {
    id: 330,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'Explain the "State Pattern" in React. How does it differ from managing state with useState or useReducer?',
    answer: 'State pattern allows an object to alter its behavior when its internal state changes. In React: each state variant is a separate component: function Wizard() { switch(step) { case 1: return <StepOne/>; case 2: return <StepTwo/>; } }. This differs from useState where behavior is conditional within one component. The State pattern encapsulates state-specific logic in separate components/functions, making it cleaner for complex state machines (multi-step forms, game states). Libraries like XState formalize this pattern. Contrast: useState manages values, State pattern manages behavioral modes.',
  },
];

export default ADVANCED_QUESTIONS;
