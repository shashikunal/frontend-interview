import type { Question } from '../models/question';

const questions: Question[] = [
  {
    id: 431,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do you implement a Compound Components pattern in React that shares implicit state via React.Children and cloneElement?',
    answer: 'Compound Components use React.Children.map with cloneElement to pass implicit state to children. The parent component (e.g., Tabs) maintains state (activeTab) and provides it via children.map to each Tab child by cloning the element with additional props. For more flexibility, you can use React.createContext to avoid the limitations of cloneElement (which only works with direct children and doesn\'t re-clone on re-renders). The context approach allows deeply nested children to access parent state. The compound pattern is used by libraries like Reach UI and Radix UI for components like Tabs, Accordion, and Menu that need coordinated behavior without explicit prop drilling.',
  },
  {
    id: 432,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'What is the Render Props pattern and how does it differ from Hooks for sharing stateful logic?',
    answer: 'Render Props is a pattern where a component accepts a function prop (usually called render or children) that returns React elements. The component calls this function with its internal state, allowing the consumer to control rendering. Example: <Mouse render={({ x, y }) => <div>{x},{y}</div>} />. Render Props predates Hooks and was the primary way to share stateful logic before React 16.8. They differ from Hooks in that Hooks are more composable (multiple hooks can be called in a single component), have less nesting, and don\'t require wrapper components. However, Render Props can be more explicit about data flow and are sometimes easier to use in class components or when the logic needs conditional invocation.',
  },
  {
    id: 433,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do Higher-Order Components (HOCs) work and what are the common pitfalls like ref forwarding and static method hoisting?',
    answer: 'An HOC is a function that takes a component and returns a new component with enhanced props or behavior. Common HOCs include withRouter, connect (Redux), and withStyles. Pitfalls include: 1) Refs are not passed through to the wrapped component unless using forwardRef. 2) Static methods on the original component are lost unless manually copied (hoist-non-react-statics library). 3) HOCs create wrapper DOM elements/nesting that can complicate debugging. 4) Props may collide if the HOC injects props with the same name as consumer props. 5) The displayName should be set for debugging: `WrappedComponent.displayName = withHOC(${Component.displayName || Component.name})`.',
  },
  {
    id: 434,
    category: 'Components & Props',
    difficulty: 'Medium',
    question: 'How do React Portals work and what are their practical use cases for modals, tooltips, and dropdowns?',
    answer: 'ReactDOM.createPortal renders children into a DOM node outside the parent component\'s hierarchy while preserving the React context tree. The portal content exists in a different DOM location but behaves like a child of the React component that rendered it for event bubbling. Portals are essential for: 1) Modals that need to overlay everything without z-index stacking context issues. 2) Tooltips that would otherwise be clipped by overflow:hidden parents. 3) Dropdowns that need to escape scroll containers. 4) Notifications/toasts rendered at the document body level. Event propagation works through the React tree, not the DOM tree, so events from portal content bubble up through the portal\'s parent in React.',
  },
  {
    id: 435,
    category: 'Components & Props',
    difficulty: 'Hard',
    question: 'How do Error Boundaries work in React 18/19 and what errors do they NOT catch?',
    answer: 'Error Boundaries are class components implementing componentDidCatch(error, errorInfo) or getDerivedStateFromError(error). They catch rendering errors in their component tree, lifecycle errors, and constructor errors. They do NOT catch: 1) Errors in event handlers (use try-catch instead). 2) Asynchronous errors (e.g., setTimeout, requestAnimationFrame). 3) Server-side rendering errors. 4) Errors thrown inside the error boundary itself. 5) Errors in Server Components (caught on the server). In React 19, error boundaries also catch hydration errors, falling back gracefully instead of crashing. For event handler errors, you should use React 19\'s new onError root option or window-level error handlers.',
  },
  {
    id: 436,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does React.lazy and Suspense enable code splitting and what are the limitations of the current Suspense implementation for data fetching?',
    answer: 'React.lazy(() => import("./Component")) enables dynamic imports where the component bundle is loaded on demand. Suspense wraps the lazy component, showing a fallback while loading. This enables route-level code splitting with frameworks like React Router. Limitations: 1) Suspense for data fetching is fully supported only in frameworks using Server Components or libraries integrated with the Suspense protocol. 2) Suspense does not detect when data is "stale" and needs refetching. 3) Multiple Suspense boundaries loading sequentially can create waterfall effects. 4) Suspense requires a framework-compatible data library (like Relay, SWR, or TanStack Query) for proper data fetching integration.',
  },
  {
    id: 437,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do you type React components with TypeScript generics to create polymorphic components?',
    answer: 'Polymorphic components accept an "as" prop to render different HTML elements. Using generics, you type them as: function Box<T extends React.ElementType>(props: { as?: T } & React.ComponentPropsWithoutRef<T>). The generic T captures the element type, and ComponentPropsWithoutRef derives the correct props. For the ref, use React.ComponentPropsWithRef or forwardRef with the generic. React 19\'s types ship with PolymorphicComponent and ElementType utilities. For stricter typing, you can constrain the allowed elements: type AllowedElements = "div" | "span" | "section"; type BoxProps<T extends AllowedElements> = { as: T } & JSX.IntrinsicElements[T];',
  },
  {
    id: 438,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do Discriminated Unions improve React state management in TypeScript and how do you exhaustively check them?',
    answer: 'Discriminated Unions are TypeScript types with a shared literal property (the "kind" or "type" discriminator) that TypeScript can narrow on. In React, they are ideal for complex state machines: type State = | { status: "loading" } | { status: "success"; data: User[] } | { status: "error"; error: Error }. In the reducer, switch/case on action.type provides narrowing. In the render, checking state.status narrows the type, giving access to state.data or state.error only when appropriate. Exhaustive checking uses the `never` type: const _exhaustive: never = state; at the end of a switch ensures all cases are handled. This catches missing cases at compile time.',
  },
  {
    id: 439,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do TypeScript Template Literal Types enable type-safe event handlers and CSS prop patterns in React?',
    answer: 'Template Literal Types create string literal unions by combining types with template syntax: type EventName<T extends string> = `${T}Change` produces "onChange", "onClickChange". In React, you can type the `on` prop of an event emitter: type OnEvent<T extends string> = `on${Capitalize<T>}`. For CSS-in-JS, you can create type-safe CSS property combinations: type ResponsiveProp<T> = T | { base?: T; sm?: T; md?: T; lg?: T }. For form field names with dot notation: type FieldPath<T> = T extends object ? { [K in keyof T]: `${K & string}${T[K] extends object ? `.${FieldPath<T[K]>}` : ""}` }[keyof T] : "". These types provide autocomplete and compile-time safety for dynamic patterns.',
  },
  {
    id: 440,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you implement higher-order reducers (reducer composition) in TypeScript with full type safety?',
    answer: 'Higher-order reducers are functions that take a reducer and return an enhanced reducer. For example, a "withUndo" HOR wraps a reducer to add undo/redo functionality: function withUndo<S, A>(reducer: (s: S, a: A) => S) { return (state: { present: S; past: S[]; future: S[] }, action: A) => { ... } }. Type safety requires correctly propagating the action type as a generic parameter. The enhanced state type wraps the original state. Utility types like Reducer<State, Action> from React help: type UndoableReducer<S, A> = Reducer<UndoableState<S>, A | UndoAction>. This pattern enables compositional state management where each concern is a separate enhancement layer.',
  },
  {
    id: 441,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do you use TypeScript satisfies operator to validate React component props and CSS objects?',
    answer: 'The satisfies operator checks that a value conforms to a type without widening the inferred type. For React components, you can use it for style objects: const styles = { container: { display: "flex", gap: "8px" } } satisfies Record<string, CSSProperties>. This validates each style object is a valid CSSProperties while preserving the literal keys for autocomplete. For component props mappings: const buttonVariants = { primary: { bg: "blue", color: "white" }, secondary: { bg: "gray", color: "black" } } satisfies Record<string, VariantStyles>. The satisfies operator is preferred over type annotations when you want to retain the exact inferred type for autocomplete and excess property checking.',
  },
  {
    id: 442,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do you implement a controlled vs uncontrolled component pattern and how do you create a hybrid using useControlledState?',
    answer: 'A controlled component receives both value and onChange from the parent, storing state externally. An uncontrolled component manages its own internal state, optionally exposing a ref for imperative access. A hybrid component (like Material-UI\'s TextField) supports both: if value prop is provided, it\'s controlled; otherwise, it manages internal state. This is implemented with a useControlledState hook: if controlledValue !== undefined, use external value and onChange; otherwise, use internal useState. The hook must handle the transition between controlled/uncontrolled states (warn if changing modes). The pattern also supports a defaultValue prop for the uncontrolled initial value and a ref for focus management.',
  },
  {
    id: 443,
    category: 'Components & Props',
    difficulty: 'Medium',
    question: 'What is the children prop and how do you type it correctly for different scenarios in TypeScript?',
    answer: 'The children prop can be typed as: React.ReactNode (most permissive, includes strings, elements, fragments, portals, null, undefined, boolean), React.ReactElement (only elements), or a more specific type like ({ type: "th" } | { type: "td" })[] for table structures. For render props, children can be typed as (props: RenderProps) => React.ReactNode. In React 18+, React.FC no longer includes children implicitly; you must declare children explicitly. For components that require exactly one child: `children: React.ReactElement`. For components that accept only text: `children: string`. For layout components: `children: React.ReactNode`.',
  },
  {
    id: 444,
    category: 'Hooks',
    difficulty: 'Medium',
    question: 'How do you implement useWhyDidYouUpdate to debug unnecessary re-renders?',
    answer: 'useWhyDidYouUpdate compares previous and current props using useRef to store the previous values. On each render, it iterates through current props, comparing each value against the previous using Object.is. Changed props are logged with the old and new values. The implementation: const previousProps = useRef(props); useEffect(() => { const changes: Record<string, { from: unknown; to: unknown }> = {}; Object.entries(props).forEach(([key, val]) => { if (previousProps.current[key] !== val) changes[key] = { from: previousProps.current[key], to: val }; }); if (Object.keys(changes).length) console.log("Re-render caused by:", changes); previousProps.current = props; }); This helps identify props that change every render (inline objects, new arrays, unstable callbacks).',
  },
  {
    id: 445,
    category: 'Design Patterns',
    difficulty: 'Medium',
    question: 'How do you implement the Observer pattern in React using a custom EventEmitter with useSyncExternalStore?',
    answer: 'Create a class EventEmitter that maintains a Map of event names to sets of listeners, with on/off/emit methods. Wrap it in a React hook using useSyncExternalStore. The hook subscribes to events on mount/cleanup and returns the latest emitted value. useSyncExternalStore ensures the component re-renders synchronously when events fire, preventing tearing in concurrent mode. This pattern decouples components across the tree without prop drilling or context: one component calls emit("event", data), and all subscribed components re-render with the new data. Unlike Redux, this is lightweight and suitable for cross-component communication within a feature.',
  },
  {
    id: 446,
    category: 'Components & Props',
    difficulty: 'Hard',
    question: 'What is the "as" prop pattern and how do you implement it with full TypeScript support using ElementType and ComponentPropsWithoutRef?',
    answer: 'The "as" prop pattern allows a component to render as different HTML elements or custom components while preserving type safety. Implementation: type Props<T extends React.ElementType> = { as?: T; } & React.ComponentPropsWithoutRef<T> & { className?: string; children?: React.ReactNode }. The rendering: const Component = as || "div"; return <Component {...rest} />. For full type safety with refs, use forwardRef with generics: const Box = React.forwardRef(<T extends React.ElementType = "div">(props: Props<T>, ref: React.ComponentPropsWithRef<T>["ref"]) => ...). This is used by libraries like styled-components, Chakra UI, and MUI for their "as" prop support.',
  },
  {
    id: 447,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do you use TypeScript Mapped Types with as clauses to transform React component prop interfaces?',
    answer: 'Mapped types transform existing types into new ones. For React props, you can use them to: 1) Make all props optional: type OptionalProps<T> = { [K in keyof T]?: T[K] }. 2) Add prefixes: type PrefixedProps<T, P extends string> = { [K in keyof T as `${P}${Capitalize<K & string>}`]: T[K] }. 3) Exclude certain props: type WithoutClassName<T> = { [K in keyof T as Exclude<K, "className">]: T[K] }. 4) Transform value types: type Stringified<T> = { [K in keyof T]: string }. With the `as` clause (TS 4.1+), you can remap keys using template literal types, enabling patterns like automatic getter/setter prop generation from a data model.',
  },
  {
    id: 448,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do you implement the State Reducer pattern (inverted control) in React for customizable components?',
    answer: 'The State Reducer pattern, popularized by Downshift, allows consumers to override internal state changes. The component accepts a stateReducer prop: (state: State, changes: SuggestedChanges) => State. Internally, the component calls this reducer before applying state changes. The consumer can intercept changes, modify them, or prevent them entirely. Implementation steps: 1) Define internal actions and state. 2) On state change, call props.stateReducer(internalState, changes). 3) Use the returned changes. This gives consumers full control over component behavior without the component library needing to anticipate every customization. Combined with useReducer, the pattern becomes: const [state, dispatch] = useReducer((s, a) => props.stateReducer?.(s, a) ?? defaultReducer(s, a), initialState).',
  },
  {
    id: 449,
    category: 'Hooks',
    difficulty: 'Medium',
    question: 'How do you implement a useDebounce hook and what are the edge cases with leading/trailing calls?',
    answer: 'useDebounce delays updating a value until a specified delay has passed since the last change. Implementation: const [debouncedValue, setDebouncedValue] = useState(value); useEffect(() => { const timer = setTimeout(() => setDebouncedValue(value), delay); return () => clearTimeout(timer); }, [value, delay]). Edge cases: 1) Leading calls (immediate first call, then debounce): use a flag to track whether the leading call has fired. 2) Trailing calls (ensure the last call fires after delay): the standard implementation handles this. 3) Max wait (guarantee update after maxDelay even if calls continue): combine with setInterval. 4) Cancel: return a cancel function from the hook. 5) Race conditions: use refs to track the latest value.',
  },
  {
    id: 450,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the difference between useCallback and useMemo and when would you use one over the other with concrete performance implications?',
    answer: 'useCallback(fn, deps) returns memoized function reference, equivalent to useMemo(() => fn, deps). useMemo(compute, deps) returns memoized computed value. Use useCallback when passing callbacks to optimized child components (wrapped in React.memo) to prevent unnecessary re-renders. Use useMemo for expensive computations (filtering large arrays, complex math, data transformation). Performance implications: useCallback prevents child re-renders by maintaining stable references; useMemo prevents recomputation. However, both have overhead: the deps array comparison runs every render. Profiling should confirm actual gains. Overusing them when not needed adds memory pressure and CPU time for dependency comparison without benefits.',
  },
  {
    id: 451,
    category: 'Hooks',
    difficulty: 'Medium',
    question: 'How do you implement a useMediaQuery hook that responds to CSS media query changes?',
    answer: 'useMediaQuery evaluates a CSS media query string and returns a boolean indicating if it matches. Implementation: const [matches, setMatches] = useState(() => window.matchMedia(query).matches); useEffect(() => { const mql = window.matchMedia(query); const handler = (e: MediaQueryListEvent) => setMatches(e.matches); mql.addEventListener("change", handler); return () => mql.removeEventListener("change", handler); }, [query]). For SSR compatibility, the initial state should default to false (or a configurable default) since window is not available. For performance, you can cache the MediaQueryList object for identical queries. This hook is the foundation for responsive component logic, replacing CSS-only media queries when components need to conditionally render entirely different structures.',
  },
  {
    id: 452,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do you implement the Module Federation pattern in React for micro-frontends with shared dependencies?',
    answer: 'Module Federation (Webpack 5) allows separate builds to compose at runtime. Each micro-frontend is a webpack build exposing components via exposes: { "./Component": "./src/Component" }. A shell app consumes them via remotes. Shared dependencies (React, ReactDOM) are configured in shared: { react: { singleton: true }, "react-dom": { singleton: true } } to ensure a single instance. For dynamic loading, use React.lazy with a dynamic import() that resolves to the remote URL. The shell renders each micro-frontend in its own DOM container with ReactDOM.createRoot. Communication between micro-frontends uses a shared event bus or cross-frame messaging. CSS isolation uses CSS Modules, CSS-in-JS, or shadow DOM.',
  },
  {
    id: 453,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do you use TypeScript branded types to enforce type safety for entity IDs in React state management?',
    answer: 'Branded types use type intersection with a phantom property to prevent mixing up IDs of different entities: type UserId = string & { __brand: "UserId" }; type PostId = string & { __brand: "PostId" }. Functions that accept UserId reject PostId at compile time even though both are strings at runtime. In React state, this prevents bugs like passing a postId where a userId is expected. A constructor function (createUserId(id: string): UserId) casts the string, and consumers use branded types in selectors and action creators. Runtime validation can be added to the constructor to validate the ID format. This pattern is particularly valuable in large codebases with complex entity relationships.',
  },
  {
    id: 454,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useImperativeHandle work and when should you use it instead of lifting state up?',
    answer: 'useImperativeHandle customizes the instance value exposed to parent components via ref. It takes a ref and a factory function returning the exposed methods: useImperativeHandle(ref, () => ({ focus, scrollTo, validate, reset })). Use it when the parent needs imperative actions (focus, scroll, play) rather than data-driven control. It should NOT replace lifting state up for data flow. Common use cases: 1) Form libraries exposing validate/submit methods. 2) Media players exposing play/pause methods. 3) Text editors exposing formatting commands. 4) Scrollable containers exposing scrollTo methods. Overuse breaks the declarative paradigm; prefer props and callbacks when the interaction can be modeled declaratively.',
  },
  {
    id: 455,
    category: 'Components & Props',
    difficulty: 'Medium',
    question: 'How do you implement a generic List component with TypeScript generics that works with any item type?',
    answer: 'A generic List component: function List<T>({ items, renderItem, keyExtractor }: { items: T[]; renderItem: (item: T, index: number) => React.ReactNode; keyExtractor: (item: T) => string | number }) { return <ul>{items.map((item, i) => <li key={keyExtractor(item)}>{renderItem(item, i)}</li>)}</ul> }. Usage infers T: <List items={users} renderItem={(user) => <span>{user.name}</span>} keyExtractor={(user) => user.id} />. For more advanced variants, add loading state, empty state, error state, and pagination props. The generic type can be constrained: <T extends { id: string | number }> with a default keyExtractor. This pattern underpins UI kit table/list components.',
  },
  {
    id: 456,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do you implement the Provider Pattern with Context for dependency injection in React and what are the performance implications?',
    answer: 'The Provider Pattern uses React.createContext and a Provider component to inject dependencies (services, configuration, theme) down the tree without prop drilling. Implementation: const ThemeContext = createContext<Theme>(defaultTheme); const ThemeProvider = ({ children, theme }) => <ThemeContext.Provider value={theme}>{children}</ThemeContext.Provider>. Performance implications: 1) Every consumer re-renders when the provider value changes, even if the consumed data hasn\'t changed. 2) Split contexts by concerns (ThemeContext, UserContext, ConfigContext) to minimize re-renders. 3) Use useMemo on the context value to prevent unnecessary re-renders from object identity changes. 4) For frequently changing values, consider separating into multiple contexts or using useSyncExternalStore.',
  },
  {
    id: 457,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you implement type-safe exhaustive matching with the never type in React reducers and conditional rendering?',
    answer: 'Exhaustive matching ensures all possibilities are handled. For a reducer, after the switch statement, add: const _exhaustive: never = state; or default: const _exhaustive: never = action;. TypeScript will error if a case is missing because the action type would not be narrowed to never. For conditional rendering with discriminated unions: if (state.status === "loading") return <Spinner />; if (state.status === "success") return <Data data={state.data} />; if (state.status === "error") return <Error error={state.error} />; const _exhaustive: never = state;. The never type is assignable to everything but nothing is assignable to it (except never itself). This catches bugs at compile time when new states/actions are added but not handled.',
  },
  {
    id: 458,
    category: 'Design Patterns',
    difficulty: 'Medium',
    question: 'What is the Adapter Pattern when integrating third-party libraries with React and how do you wrap them to prevent tight coupling?',
    answer: 'The Adapter pattern wraps a third-party library\'s API in a React-friendly interface, isolating the rest of your code from vendor lock-in. Create a custom hook or component that encapsulates the library: function Chart({ data, type }) { const ref = useRef(); useEffect(() => { const chart = new ThirdPartyChart(ref.current, { data, type }); return () => chart.destroy(); }, [data, type]); return <div ref={ref} /> }. Changes to the underlying library only require updating the adapter. Benefits: 1) Swap libraries without changing business logic. 2) Provide a consistent API across different libraries. 3) Add React-specific optimizations (memo, useMemo) in one place. 4) Test with mock adapters.',
  },
  {
    id: 459,
    category: 'TypeScript',
    difficulty: 'Medium',
    question: 'How do you type React events and why is MouseEvent<HTMLButtonElement> different from MouseEvent<HTMLDivElement>?',
    answer: 'React events are typed as generic types parameterized by the element type: React.MouseEvent<HTMLButtonElement>, React.ChangeEvent<HTMLInputElement>, React.KeyboardEvent<HTMLDivElement>. The generic parameter provides type safety for event.target and event.currentTarget properties. event.currentTarget is typed as the specific element (HTMLButtonElement), giving access to element-specific properties like button.value. event.target is typed as EventTarget (the actual element that triggered the event, which may differ from currentTarget due to event bubbling). Use currentTarget for the element the handler is attached to, and target for the element that triggered the event. SyntheticEvent<T> is the base type for all synthetic events.',
  },
  {
    id: 460,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How do you create a useEventListener hook that handles cleanup and passive/active options correctly?',
    answer: 'useEventListener(type, handler, element, options) attaches an event listener to a specified element (default window) with proper cleanup. Implementation: useEffect(() => { const target = element ?? window; if (!target?.addEventListener) return; const wrappedHandler = (e: Event) => handler(e as any); target.addEventListener(type, wrappedHandler, options); return () => target.removeEventListener(type, wrappedHandler, options); }, [type, element, options, handler]). The handler should use useCallback for stable reference, or use a ref to always call the latest handler without re-subscribing. This last approach: const savedHandler = useRef(handler); savedHandler.current = handler; then call savedHandler.current(e) in the listener. This avoids re-subscribing when handler changes.',
  },
  {
    id: 461,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'How do you implement the Factory Pattern for creating React components dynamically based on configuration?',
    answer: 'The Factory Pattern uses a configuration object to determine which component to render. Implementation: const componentMap: Record<string, React.ComponentType<any>> = { button: Button, input: Input, select: Select }; function ComponentFactory({ type, ...props }: { type: keyof typeof componentMap } & Record<string, any>) { const Component = componentMap[type]; if (!Component) return null; return <Component {...props} /> }. For type safety, use discriminated unions: type Field = | { type: "text"; label: string } | { type: "number"; min: number; max: number }. Then render each field using the factory. This pattern is used in form builders, CMS-driven UIs, and WYSIWYG editors where component types come from configuration rather than being hardcoded.',
  },
  {
    id: 462,
    category: 'TypeScript',
    difficulty: 'Hard',
    question: 'How do you use TypeScript variadic tuple types for typing React hooks with multiple state values?',
    answer: 'Variadic tuple types (TS 4.0+) allow typing arrays with variable lengths and element types. For a useMultiState hook that manages multiple independent state values: function useMultiState<T extends unknown[]>(...initial: T): { [K in keyof T]: [state: T[K], setter: Dispatch<SetStateAction<T[K]>>] } { return initial.map(v => useState(v)) as any }. This preserves the exact tuple shape so each index has the correct type. For a combineReducers function: function combineReducers<R extends { [K in keyof R]: Reducer<any, any> }>(reducers: R): Reducer<{ [K in keyof R]: React.ReducerState<R[K]> }, React.ReducerAction<R[keyof R]>>. This enables type-safe reducer composition without manual state type declarations.',
  },
  {
    id: 463,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useTransition with startTransition enable concurrent rendering and how do you test pending states?',
    answer: 'useTransition returns [isPending, startTransition]. startTransition wraps state updates as non-urgent transitions that can be interrupted. When a transition is pending, isPending is true, allowing you to show a loading indicator. For testing transitions, use React.act and wait for microtasks: await act(async () => { startTransition(() => { setState(newValue); }); });. To test the pending state, check isPending within the transition callback. Jest and Testing Library can use waitFor to assert that the pending state resolves. Importantly, transitions preserve the previous UI during loading, so the user doesn\'t see a blank screen. Transitions can be nested, and React prioritizes the most recent transition.',
  },
  {
    id: 464,
    category: 'Design Patterns',
    difficulty: 'Hard',
    question: 'What is the Strategy Pattern in React and how do you implement interchangeable algorithms via props or context?',
    answer: 'The Strategy Pattern allows swapping algorithms at runtime. In React, strategies are functions passed as props or provided via context. Example: function DataTable({ data, sortStrategy }: { data: Row[]; sortStrategy: (rows: Row[], column: string) => Row[] }) { const sorted = useMemo(() => sortStrategy(data, sortColumn), [data, sortColumn, sortStrategy]); return <table>...}. Consumers pass different sort strategies: sortStrategy={(rows, col) => rows.sort(...)}. More complex strategies can be objects with multiple methods: interface ValidationStrategy { validate(value: string): boolean; format(value: string): string; errorMessage: string }. The strategy is injected via context and used by form fields. This eliminates conditional logic and makes the component open for extension but closed for modification.',
  },
  {
    id: 465,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How do you implement useMemo with a custom comparator function to control when memoized values recompute?',
    answer: 'React.useMemo uses Object.is comparison on deps. To implement custom comparison: 1) For props comparison, wrap with React.memo which accepts a comparator: React.memo(Component, (prev, next) => shallowEqual(prev.props, next.props)). 2) For individual values, use a ref to store the previous value and compare manually: const prevValue = useRef(value); const isEqual = useMemo(() => customCompare(value, prevValue.current), [value]); useEffect(() => { prevValue.current = value; });. 3) For deep comparison of computed values, use a library like lodash.isEqual: const computed = useMemo(() => expensive(data), deps) combined with a custom equality check via useRef tracking. 4) For immutable data structures (Immer, Immutable.js), structural sharing enables fast reference equality checks.',
  },
];

export default questions;
