import type { Question } from '../models/question';

const questions: Question[] = [
  {
    id: 401,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How do React Server Components differ from Client Components in execution environment?',
    answer: 'React Server Components (RSC) execute exclusively on the server during rendering, never on the client. They can directly access databases, file systems, and backend services without exposing sensitive logic to the browser. Client Components render on both server (SSR) and client, maintain interactivity via hooks and effects, and have access to browser APIs. RSC reduce bundle size by excluding server-only code from the client bundle. The boundary between them is defined by the "use client" directive at the top of a file.',
  },
  {
    id: 402,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the React Server Actions API and how does it enable mutating server state from client components?',
    answer: 'Server Actions are async functions marked with "use server" that run on the server but can be called directly from client components, progressive enhancement forms, or event handlers. They accept FormData or serializable arguments and return serializable values. React serializes the arguments, sends them over the network, executes the function server-side, and returns the result. With useActionState, they integrate with form lifecycle management, providing pending states and automatic revalidation of associated Suspense boundaries. They replace manual API route handlers for simple mutations.',
  },
  {
    id: 403,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain the use() hook in React 19 and how it differs from useEffect and useMemo for consuming promises and context.',
    answer: 'The use() hook is a groundbreaking addition that can be called conditionally and inside loops, unlike all other hooks. It accepts a Promise or Context object directly. When given a Promise, it suspends the component until resolution, integrating with Suspense boundaries. Unlike useEffect, use() does not require cleanup or dependency arrays. Unlike useMemo, it does not require a factory function. When given a Context, it behaves like useContext but can be used conditionally. It can also be used inside early returns, enabling data fetching patterns that were previously impossible without restructuring components.',
  },
  {
    id: 404,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useFormStatus work in React 19 and what problem does it solve for form components?',
    answer: 'useFormStatus is a hook that reads the status of a parent form without requiring prop drilling. It returns { pending, data, action } reflecting the submitting state of the closest ancestor form. This solves the problem of child components needing to know the form submission state without receiving pending/disabled props from the parent. It leverages the native form submission lifecycle and works with both Server Actions and client-side form handlers. The hook must be called from a component rendered inside a form element, and it automatically updates as the form transitions through submit states.',
  },
  {
    id: 405,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is useActionState and how does it replace useReducer for form management with Server Actions?',
    answer: 'useActionState (formerly useFormState in React 19 RC) accepts an action function and an initial state, returning [state, formAction, isPending]. It manages the lifecycle of form submissions whether the action is a Server Action or client-side async function. The action receives the previous state as its first argument and form data as the second, enabling the "reducer pattern" for forms. Unlike useReducer, useActionState integrates directly with HTML form submission, progressive enhancement, and React Server Components. It automatically handles pending states and error recovery, and when used with Server Actions, triggers automatic revalidation of Suspense boundaries.',
  },
  {
    id: 406,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'Explain useOptimistic in React 19 and how it implements optimistic UI updates with automatic rollback.',
    answer: 'useOptimistic accepts a piece of state and an update function, returning [optimisticState, setOptimistic]. When setOptimistic is called with a new value, React immediately renders the optimistic state while the actual mutation is in flight. If the mutation succeeds, the optimistic state is replaced with the confirmed state. If it fails, React automatically rolls back to the original state. The update function receives the current state and the optimistic value, returning the computed optimistic state. This eliminates manual rollback logic and race condition handling that developers previously had to implement with useReducer or external state management.',
  },
  {
    id: 407,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the React Compiler (React Forget) and how does it change the mental model for re-rendering optimization?',
    answer: 'The React Compiler is a build-time tool that automatically memoizes components and hooks by analyzing JavaScript semantics and React rules. It eliminates the need for useMemo, useCallback, and React.memo in most cases by automatically determining when values and components should be memoized. The compiler understands JavaScript scoping, closure semantics, and React\'s immutability conventions. It transforms code during the build phase (via Babel or SWC), adding internal calls to useMemo/useCallback only where necessary. This shifts the optimization mental model from "manually prevent re-renders" to "write simple code and let the compiler handle it."',
  },
  {
    id: 408,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React 19\'s Asset Loading system improve performance for stylesheets, fonts, and scripts?',
    answer: 'React 19 introduces built-in asset loading that integrates with Suspense. When a component renders a <link>, <style>, or <script> in the head, React tracks its load status. The component suspends until the asset is fully loaded, preventing flash-of-unstyled-content (FOUC) and layout shift. React can preload assets discovered during server rendering and inject them in the server response. The preload, preinit, and preconnect APIs allow developers to initiate loading before rendering. This integrates with the new Document Metadata system, ensuring metadata like <title> and <meta> tags are properly hoisted and do not cause layout thrashing.',
  },
  {
    id: 409,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the Document Metadata system in React 19 and how does it handle <title>, <meta>, and <link> tags?',
    answer: 'React 19 natively supports rendering document-level metadata (<title>, <meta>, <link>) anywhere in the component tree. React automatically hoists these elements to the document <head> during rendering, regardless of where they appear in the component hierarchy. This solves the problem of deeply nested components needing to set page titles or meta tags. The hoisting works in both Server-Side Rendering (injected into the server-rendered HTML head) and client-side rendering (moved to the actual document head). This replaces solutions like react-helmet and eliminates the need for portals to the head.',
  },
  {
    id: 410,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React 18\'s Streaming SSR work and what are the benefits over traditional SSR?',
    answer: 'Streaming SSR in React 18 uses renderToPipeableStream (Node) or renderToReadableStream (edge) to send HTML in chunks as it is produced, rather than waiting for the entire tree to render. The initial shell HTML is sent immediately, including Suspense boundaries wrapped in fallback content. As server components complete (including data fetching), React sends additional HTML chunks via script tags that "unblock" the Suspense boundaries on the client. Benefits include: reduced Time-to-First-Byte (TTFB), early HTML parsing and CSS/style loading, progressive hydration where each boundary hydrates independently, and the ability to handle slow data fetches without blocking the entire page.',
  },
  {
    id: 411,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is Selective Hydration in React 18 and how does it improve Time to Interactive?',
    answer: 'Selective Hydration allows React to hydrate components incrementally based on user interaction priority rather than render order. When a page streams in, each Suspense boundary can hydrate independently as its HTML arrives. If a user clicks or interacts with a component that hasn\'t hydrated yet, React can prioritize hydrating that specific boundary over others, making it interactive immediately. React achieves this by maintaining a queue of hydration tasks and allowing events to trigger priority boosting. This significantly improves First Input Delay (FID) and Interaction to Next Paint (INP) by ensuring visible/interactive content hydrates first.',
  },
  {
    id: 412,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useDeferredValue work and what is its role in concurrent rendering for expensive computations?',
    answer: 'useDeferredValue accepts a value and returns a deferred copy that may lag behind by one render cycle. During the initial render with the new value, React renders the old deferred value in a urgent commit, then schedules a transition render with the new deferred value. If the user continues changing the value (e.g., typing in a search input), the deferred render is interrupted and restarted with the latest value, preventing UI jank. This is ideal for filtering large lists, transforming data, or rendering expensive visualizations where the user input must remain responsive while the output can lag slightly. Unlike debouncing, useDeferredValue integrates with React\'s scheduler and can be interrupted.',
  },
  {
    id: 413,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the difference between useTransition and useDeferredValue for marking updates as non-urgent?',
    answer: 'useTransition provides a startTransition function that wraps state updates, marking them as transitions. All state updates inside startTransition are treated as non-urgent and can be interrupted by more urgent updates (clicks, typing). useDeferredValue, conversely, wraps a value that is derived from urgent state. The key difference: useTransition wraps the state update itself, while useDeferredValue wraps the derived/rendered result. useTransition is better when you control the source of the state change (e.g., navigating between pages), while useDeferredValue is better for uncontrolled inputs (e.g., typing in a search field where the input value is state you don\'t control the setter of).',
  },
  {
    id: 414,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'Explain React 19\'s enhanced ref forwarding and how refs can now be passed without forwardRef.',
    answer: 'React 19 allows passing ref as a regular prop named "ref" to function components without using forwardRef. The ref prop is automatically handled by the framework, and components can access it via React.useRef or by destructuring props. This eliminates the forwardRef wrapper that was previously required. For imperative handle patterns, the useImperativeHandle hook still works for controlling the exposed API. This simplification reduces component nesting and makes ref forwarding more intuitive, especially for library authors who had to remember to wrap every interactive component with forwardRef.',
  },
  {
    id: 415,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What are React Server Actions and how do they enable form mutations without API route boilerplate?',
    answer: 'Server Actions are async functions annotated with "use server" that run on the server. They can be invoked from client components via form action attributes, enabling direct server mutations without building REST or GraphQL endpoints. React handles serialization of the FormData, network transport, and result deserialization. Server Actions integrate with progressive enhancement where forms work even before JavaScript loads. Combined with useActionState, they provide pending states, error handling, and automatic revalidation. They can also be called imperatively from event handlers and useEffect, not just from forms, enabling server mutations from any interaction.',
  },
  {
    id: 416,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How does React 18\'s Automatic Batching differ from the previous batching behavior?',
    answer: 'React 18 introduced automatic batching for all state updates, regardless of where they occur. Previously, React only batched updates inside React event handlers. Updates inside setTimeout, fetch callbacks, Promise handlers, and native event listeners triggered separate re-renders for each setState call. In React 18, all updates are automatically batched, including those in async contexts. This reduces the number of re-renders and improves performance. If you need to force a render between updates (e.g., for reading DOM measurements after a state change), you can use ReactDOM.flushSync.',
  },
  {
    id: 417,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is the formal React lifecycle for Server Components and how does it differ from Client Components?',
    answer: 'Server Components have a simplified lifecycle: they render once per request (or per cache revalidation) on the server. They do not mount, update, or unmount. They cannot use useState, useEffect, or any hook that requires interactivity. Their entire lifecycle is: 1) Server receives a request, 2) React traverses the component tree, 3) When encountering a Server Component, it executes the function, 4) If it hits a Client Component boundary, it renders the client component as serialized props and emits it into the stream, 5) The component is done. Client Components have the full lifecycle (mount/update/unmount) on the client after hydration, though they can also render on the server during SSR.',
  },
  {
    id: 418,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How do Suspense Boundaries interact with Server Components and Streaming SSR to enable parallel data fetching?',
    answer: 'Suspense Boundaries wrapping Server Components enable fine-grained streaming. When a Server Component inside a Suspense boundary awaits a data fetch, React streams the fallback UI immediately while the fetch completes on the server. Once resolved, React sends the rendered HTML for that boundary as a chunk. This enables "fetch-as-you-render" where the server concurrently resolves multiple data fetches at different levels of the tree, each wrapped in its own Suspense boundary. On the client, each boundary hydrates independently as its content arrives. This pattern eliminates waterfall requests and allows the page shell to render while deep data fetches complete in parallel.',
  },
  {
    id: 419,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useId work and why is it necessary for generating unique IDs in Server Components and SSR?',
    answer: 'useId generates unique, stable IDs for accessibility attributes (aria-labelledby, etc.) that are consistent across server and client rendering. It is necessary because on the server, you cannot rely on incrementing counters or Math.random() as they would produce mismatches during hydration. useId uses a tree-path-based algorithm that generates IDs from the component\'s position in the tree, ensuring they are deterministic across renders and consistent between server and client. It works in both Server Components and Client Components. Multiple useId calls in the same component generate suffix-based variants to ensure uniqueness within the component.',
  },
  {
    id: 420,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'Explain the "use client" directive and how it determines the Server/Client Component boundary in React 19.',
    answer: 'The "use client" directive at the top of a file marks all exported components as Client Components. When a Server Component imports from a "use client" module, React knows to render those components on the client and serialize their props. The boundary is file-based: once a file has "use client", all components exported from that file are client-side. Server Components can import and render Client Components, passing serializable props. Client Components CANNOT import Server Components (they would try to execute on the client). Server Components can be passed as children (via the "children" prop) to Client Components, enabling the "server component in client component" pattern.',
  },
  {
    id: 421,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React 19 handle form actions with the action prop and what enhancements does it provide over traditional onSubmit?',
    answer: 'React 19\'s action prop on form elements works with both Server Actions and async functions. When the form is submitted, React calls the action with FormData and manages pending states automatically. Unlike preventDefault + onSubmit patterns, React integrates the action with the native form submission lifecycle, enabling progressive enhancement. The action receives a FormData object directly, eliminating manual event.target serialization. React also provides useActionState for form state management and useFormStatus for child components to read the form state. Multiple submit buttons can have different formAction values, enabling complex forms with multiple actions.',
  },
  {
    id: 422,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the new lifecycle for ref cleanup in React 19 and how does it simplify cleanup logic?',
    answer: 'React 19 introduces ref cleanup as a first-class concept. Ref callbacks can now return a cleanup function that React calls when the element unmounts or when the ref changes. This eliminates the need for useEffect cleanups for ref-related side effects. The pattern is: ref={(el) => { if (el) { setup(el); return () => cleanup(el); } }}. React guarantees the cleanup runs before the ref is set to null or set to a new value. This is particularly useful for integrating with third-party libraries that require imperative setup/teardown on DOM elements.',
  },
  {
    id: 423,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does useSyncExternalStore enable reading from external stores and integrating with concurrent rendering?',
    answer: 'useSyncExternalStore is a hook for subscribing to external data sources (like Redux stores, Zustand, or browser APIs) while ensuring consistency with React\'s concurrent rendering. It takes three arguments: subscribe (a function that subscribes to the store and returns an unsubscribe function), getSnapshot (returns the current value), and getServerSnapshot (optional, for SSR). React uses the subscribe function to track external changes and re-render when the store updates. The key feature is that useSyncExternalStore forces synchronous re-renders during concurrent rendering to prevent "tearing" where different parts of the UI see inconsistent store values due to time-slicing.',
  },
  {
    id: 424,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What is Tearing in the context of React Concurrent features and how does useSyncExternalStore prevent it?',
    answer: 'Tearing is a visual inconsistency that occurs when concurrent rendering allows React to render different parts of the tree at different points in time. Without protection, an external store update could be visible to one part of the tree (rendered later) but not another (rendered earlier), causing the UI to show inconsistent data simultaneously. useSyncExternalStore prevents tearing by subscribing to the store using React\'s internal mechanism, which ensures that when the store updates during a concurrent render, React either replays the render with the new value or marks the component as needing re-render, maintaining visual consistency.',
  },
  {
    id: 425,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'How does React 19\'s improved error reporting with Error Boundaries and hydration error handling work?',
    answer: 'React 19 significantly improves error handling. Hydration errors no longer cause a full-page crash; instead, React falls back to client-side rendering for mismatched content, logging a warning with the specific mismatch details. Error Boundaries now catch errors during server rendering and streaming, displaying fallback UI gracefully. React also surfaces more detailed error messages with component stacks, differentiating between rendering errors, lifecycle errors, and event handler errors. Server-side rendering errors are reported with the full component tree path, enabling easier debugging of streaming errors that previously resulted in cryptic HTML parsing failures.',
  },
  {
    id: 426,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'What are the new <title>, <meta>, and <link> element behaviors in React 19 and how do they interact with the document?',
    answer: 'In React 19, <title>, <meta>, and <link> elements rendered anywhere in the component tree are automatically hoisted to the document <head>. React manages deduplication: if multiple components render <title>, the most deeply nested one wins. Metadata components can be rendered inside Client Components, Server Components, or even conditionally based on data. During Server-Side Rendering, React injects these elements directly into the server-rendered HTML head. On the client, it uses document manipulation to move them to the head. This eliminates the need for external libraries like react-helmet-async and makes metadata management trivial for deeply nested route components.',
  },
  {
    id: 427,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'How does the use hook integrate with React.lazy and Suspense for code splitting with data dependencies?',
    answer: 'The use() hook integrates with React.lazy by allowing components to suspend not just on lazy-loaded modules but also on data fetching promises. When used with React.lazy, you can create components that simultaneously wait for both the module to load and the data to resolve. The pattern is: inside a component wrapped in Suspense, call use(fetchData()) and use(React.lazy(() => import("./Component"))). This enables parallel resolution of code and data dependencies, reducing waterfall effects in code-split applications. If either the module or the data is unavailable, the component suspends and React shows the nearest Suspense fallback.',
  },
  {
    id: 428,
    category: 'Advanced Patterns',
    difficulty: 'Hard',
    question: 'What is the pattern for using Server Components inside Client Components via the children prop?',
    answer: 'Server Components cannot be imported into Client Components, but they can be passed as children (or any prop) to Client Components. The pattern is: a Server Component renders a Client Component and passes a Server Component as its children. The Client Component renders {children} without knowing it\'s a Server Component. This works because the Server Component is rendered on the server first, its output serialized, and passed as pre-rendered props to the Client Component. This enables mixing server-rendered content (data fetching, direct DB access) inside interactive Client Component shells (carousels, tabs, modals) without exposing server logic to the client.',
  },
  {
    id: 429,
    category: 'React Core',
    difficulty: 'Hard',
    question: 'How does React 20\'s improved RSC protocol enhance streaming and partial rendering capabilities?',
    answer: 'React 20 advances the RSC protocol with more granular streaming control. The protocol now supports streaming individual props to Client Components, enabling progressive enhancement of interactive components without waiting for all server data. Partial rendering allows Server Components to re-render specific boundaries without re-rendering the entire tree, enabling targeted updates to server-computed content. The protocol uses a more efficient binary serialization format for props, reducing transfer size. React 20 also introduces "pending states" for Server Components that are re-rendering on the server, allowing Client Components to show spinners or skeletons while waiting for new server-rendered content.',
  },
  {
    id: 430,
    category: 'Hooks',
    difficulty: 'Hard',
    question: 'What is the relationship between useOptimistic and useActionState when implementing optimistic updates with form actions?',
    answer: 'useOptimistic and useActionState are designed to work together. useActionState manages the form lifecycle (pending, state, error), while useOptimistic manages the optimistic UI layer. The pattern is: useActionState provides the action function, and useOptimistic wraps the state. When the action is called, useOptimistic immediately applies the optimistic value. If useActionState resolves successfully, useOptimistic reconciles with the confirmed state. If the action throws, useOptimistic automatically rolls back. They share the same pending state: useActionState\'s isPending flag signals that the action is in flight, and useOptimistic shows the optimistic value. Together, they provide a complete optimistic form mutation pattern.',
  },
];

export default questions;
