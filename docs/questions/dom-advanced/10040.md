---
title: "Output tracing 7: what will this code log, and why?"
category: "DOM Advanced APIs"
difficulty: "Hard"
questionId: 10040
totalQuestions: 10500
---

# Output tracing 7: what will this code log, and why?

> **Category:** DOM Advanced APIs | **Difficulty:** Hard | **Question #10040**

## Overview

Expected output: "one"
"two"
"three"

dispatchEvent runs listeners synchronously in registration order; the final console.log waits until both handlers finish, demonstrating deterministic CustomEvent delivery.

## Key Concepts

- Call-site binding
- Implicit, explicit, new, and default binding
- Arrow function lexical this

## Syntax & Examples

### this Binding Rules

```javascript
// Default binding
function foo() { console.log(this) }
foo() // window (or undefined in strict)

// Implicit binding
const obj = { foo: function() { console.log(this) } }
obj.foo() // obj

// Explicit binding
foo.call(obj) // obj

// new binding
new foo() // new instance

// Arrow function (lexical)
const arrow = () => console.log(this)
arrow() // enclosing scope's this
```

this is determined by how a function is called (call-site), not where it's defined. Arrow functions inherit this lexically.

## Interactive Code Example

```javascript
const bus = new EventTarget();
bus.addEventListener('tick', () => console.log('one'));
bus.addEventListener('tick', () => console.log('two'));
bus.dispatchEvent(new CustomEvent('tick'));
console.log('three');
```

**Expected Output:**

```
// Run the code to see output
// The code editor above is interactive
// Click "Run Code" to execute
```

## Specifications

| Specification | Status | Comment |
|---------------|--------|---------|
| [ECMAScript Language Specification](https://tc39.es/ecma262/) | Living Standard | The official JavaScript language specification |
| [MDN Web Docs - JavaScript](https://developer.mozilla.org/en-US/docs/Web/JavaScript) | Documentation | Comprehensive JavaScript reference and guides |

## Browser Compatibility

| Browser | Desktop | Mobile | Notes |
|---------|---------|--------|-------|
| Chrome | Full Support | Full Support | All versions |
| Firefox | Full Support | Full Support | All versions |
| Safari | Full Support | Full Support | All versions |
| Edge | Full Support | Full Support | All versions |
| IE 11 | Full Support | N/A | Legacy support |

> **Note:** Compatibility data is generated based on the JavaScript/ECMAScript features discussed in this question.

## See Also

- [All Questions](../index.md)
- [Previous Question](../dom-advanced/10039.md)
- [Next Question](../dom-advanced/10041.md)
- [Category: DOM Advanced APIs](../dom-advanced/index.md)

---

*Generated from react-interview-prep question database*